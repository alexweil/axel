# axel — Implementación

> Plan a gran escala y estado por feature. La bajada fina de cada feature vive en `docs/implementation/NN-nombre.md`, con sus decisiones y su review log. La posición actual está en [STATUS.md](STATUS.md).

## Cómo se trabaja un feature

Sesión limpia → `/feature` → gate de arranque (resumen breve + confirmación humana) → bajada fina → review → implementación con loop de review → RECAP → OK humano. Reglas en [AGENTS.md](../AGENTS.md); contrato de review en [design/review-contract.md](design/review-contract.md). **Modo lote**: `/feature all` o `/feature NN..MM` — gate de lote al inicio, subagente fresco por feature (quedan «APPROVED — pendiente OK de lote»), RECAP consolidado cuyo OK cierra ([design/batch-features.md](design/batch-features.md)).

## Criterio de orden

1. **Restricciones humanas fijan posiciones**: los pedidos explícitos del humano no se discuten entre agentes (hoy: instalador 01 primero, 2026-07-27; gate de confirmación 05 como único ítem del backlog nuevo, 2026-07-28; one-liner corto 06 como único ítem del backlog nuevo, 2026-07-28; batch de features 07 como único ítem del backlog nuevo — diseño pedido y aprobado por el humano, 2026-07-28; entrada implícita 08–10 como bloque del backlog nuevo — diseño pedido y aprobado por el humano, 2026-07-29; modelo fijo de los subagentes 11 como único ítem del backlog nuevo — pedido autorizado en el gate del pipeline `/build`, 2026-07-29; reporte de cierre de `/adopt` 12 como único ítem del backlog nuevo — pedido autorizado en el gate del segundo pipeline `/build` del día, 2026-07-29; vidriera y onboarding 13–14 como bloque del backlog nuevo, **en ese orden** — pedido autorizado en el gate del tercer pipeline `/build` del día, 2026-07-29).
2. **Valor desbloqueado antes que pulido**: primero lo que habilita uso nuevo (llevar axel a otros proyectos) sobre lo que mejora lo que ya funciona.
3. **Núcleo antes que confort**: desbloqueado el valor nuevo, primero consolidar el código safety-critical de uso diario — prioridad por riesgo × centralidad × radio de daño (`review.sh` y `awake.sh` ejecutan `reset --hard`, `clean -fdx` y señales de procesos en cada ciclo) — y recién después la calidad de vida.

## Features

| # | Feature | Estado | Doc |
|---|---|---|---|
| 00 | Bootstrap de la maquinaria | **Cerrado** — APPROVED de Codex (r4) + OK humano, 2026-07-27 | [implementation/00-bootstrap.md](implementation/00-bootstrap.md) |
| 01 | Instalador: llevar axel a otro proyecto | **Cerrado** — APPROVED de Codex (r11) + OK humano, 2026-07-27 | [implementation/01-installer.md](implementation/01-installer.md) |
| 02 | Instalación remota: one-liner desde GitHub | **Cerrado** — APPROVED de Codex (r10) + OK humano, 2026-07-27 | [implementation/02-remote-install.md](implementation/02-remote-install.md) |
| 03 | Hardening del loop de review | **Cerrado** — APPROVED de Codex (r8) + OK humano, 2026-07-28 | [implementation/03-loop-hardening.md](implementation/03-loop-hardening.md) |
| 04 | Notificaciones y continuidad entre sesiones | **Cerrado** — APPROVED de Codex (r5) + OK humano (terminal), 2026-07-28 | [implementation/04-notifications-continuity.md](implementation/04-notifications-continuity.md) |
| 05 | Confirmación previa a la implementación en `/feature` | **Cerrado** — APPROVED de Codex (r4) + OK humano (terminal), 2026-07-28 | [implementation/05-feature-gate.md](implementation/05-feature-gate.md) |
| 06 | One-liner corto: defaults del bootstrap remoto | **Cerrado** — APPROVED de Codex (r11) + OK humano, 2026-07-28 | [implementation/06-oneliner-defaults.md](implementation/06-oneliner-defaults.md) |
| 07 | Batch de features: `/feature all` y `/feature NN..MM` | **Cerrado** — APPROVED de Codex (r8) + OK humano, 2026-07-28 | [implementation/07-batch-features.md](implementation/07-batch-features.md) (diseño en [design/batch-features.md](design/batch-features.md)) |
| 08 | Reentradas de fase y feature: inventario completo fail-closed | **Cerrado** — APPROVED de Codex (r5) + OK humano del lote, 2026-07-29 (lote [implementation/batch-2026-07-29.md](implementation/batch-2026-07-29.md)) | [implementation/08-reentry-inventory.md](implementation/08-reentry-inventory.md) (diseño en [design/implicit-entry.md](design/implicit-entry.md)) |
| 09 | Ruteo implícito: triggers y despacho por contexto | **Cerrado** — APPROVED de Codex (r7) + OK humano del lote, 2026-07-29 (lote [implementation/batch-2026-07-29.md](implementation/batch-2026-07-29.md)) | [implementation/09-implicit-routing.md](implementation/09-implicit-routing.md) (diseño en [design/implicit-entry.md](design/implicit-entry.md)) |
| 10 | Pipeline `/build`: unidades tipadas con gate único | **Cerrado** — APPROVED de Codex (r7) + OK humano del lote, 2026-07-29 (lote [implementation/batch-2026-07-29.md](implementation/batch-2026-07-29.md)) | [implementation/10-build-pipeline.md](implementation/10-build-pipeline.md) (diseño en [design/implicit-entry.md](design/implicit-entry.md)) |
| 11 | Modelo fijo de los subagentes por tipo de unidad | **Cerrado** — APPROVED de Codex (r6) + OK humano del pipeline, 2026-07-29 (pipeline [implementation/pipeline-2026-07-29.md](implementation/pipeline-2026-07-29.md)) | [implementation/11-subagent-models.md](implementation/11-subagent-models.md) (diseño en [design/implicit-entry.md](design/implicit-entry.md) §«Modelos por unidad») |
| 12 | Reporte de cierre de `/adopt`: inventario de archivos tocados | **Cerrado** — APPROVED de Codex (r7: bajada en r4, implementación en r7) + OK humano del pipeline, 2026-07-29 (pipeline [implementation/pipeline-2026-07-29-2.md](implementation/pipeline-2026-07-29-2.md)) | [implementation/12-adopt-close-report.md](implementation/12-adopt-close-report.md) |
| 13 | Vidriera: LICENSE MIT + README en inglés + `docs/install.md` | **En curso** — unidad `feature` del pipeline 2026-07-29 (3) ([implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md)); bajada fina escrita, en review | [implementation/13-public-showcase.md](implementation/13-public-showcase.md) (diseño en [design/public-surface.md](design/public-surface.md)) |
| 14 | Onboarding y feedback: CONTRIBUTING + `.github/` + métricas versionadas | **Pendiente** — unidad `feature` del pipeline 2026-07-29 (3) ([implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md)) | — (diseño en [design/public-surface.md](design/public-surface.md)) |

**Orden acordado** entre generador y reviewer: APPROVED del ciclo de `/plan` en su ronda 5 (2026-07-27, base `5b1bc02`) — "queda acordado el plan: 01 instalador → 02 hardening con suite de regresión central → 03 notificaciones, derivado de los tres criterios documentados". OK humano del plan: recibido el 2026-07-27 (registrado en `860c80b`). **Nota de numeración**: esa cita usa la numeración original del plan; con el OK del 01 (2026-07-27) el humano insertó *instalación remota* como 02 (criterio 1), corriendo hardening a 03 y notificaciones a 04 — el orden relativo acordado entre agentes no cambió.

**Extensión 2026-07-28 (feature 05)**: APPROVED del ciclo de `/plan` que agregó el 05 en su ronda 2 (base `60cf68d`) — único ítem del backlog nuevo, posición fijada por pedido humano (criterio 1); ambos agentes acuerdan la entrada, el bootstrap autoaplicado y el origen derivado del resumen. OK humano de la extensión: recibido el 2026-07-28.

**Extensión 2026-07-28 (feature 06)**: APPROVED del ciclo de `/plan` que agregó el 06 en su ronda 2 (base `4336f4d`) — único ítem del backlog nuevo, posición fijada por pedido humano (criterio 1; además alinea con el criterio 2: reduce la fricción de llevar axel a otros proyectos); ambos agentes acuerdan la entrada, los dos tradeoffs registrados en r1 (semántica de fallo del transporte, destino ambiental) y las decisiones que quedan explícitamente para la bajada. OK humano de la extensión: recibido el 2026-07-28 (sin restricciones adicionales: las dos decisiones abiertas quedan para la bajada).

**Extensión 2026-07-28 (feature 07)**: APPROVED del ciclo de `/plan` que agregó el 07 en su ronda 2 (base `0c2ee58`) — único ítem del backlog nuevo; posición fijada por la secuencia que el propio humano marcó (criterio 1: pidió el diseño del batch, lo aprobó el 2026-07-28 y ordenó incorporarlo al plan como paso siguiente), con encaje además en el criterio 3 (núcleo del loop: `review.sh`, skill `feature`, la regla dura de continuidad). Ambos agentes acuerdan el orden, la entrada, el deslinde diseño-fijado vs. bajada y la superficie estimada (completada en r1: cobertura pre-OK en dos tramos, skill `recap` desde el último OK humano, señal terminal en el contrato canónico). OK humano de la extensión: recibido el 2026-07-28.

**Extensión 2026-07-29 (features 08–10, entrada implícita)**: bajada del diseño cerrado de entrada implícita ([design/implicit-entry.md](design/implicit-entry.md), APPROVED de Codex r3 + OK humano, 2026-07-29) a tres features. La posición del **bloque** la fija la secuencia del humano (criterio 1: pidió el diseño, lo aprobó y ordenó `/plan` como paso siguiente). El **orden interno** 08 → 09 → 10 lo derivan los agentes de dos fuentes: (a) dependencia técnica — el orden de despacho del ruteo entrega todo estado pendiente a la reentrada de la skill dueña, así que el inventario de reentradas debe estar completo **antes** de que el ruteo dependa de él (08 antes que 09; fail-closed sin huecos), con encaje además en el criterio 3 (las reentradas son robustez del núcleo con valor propio inmediato, sin ruteo); (b) el propio diseño fija los dos niveles como incrementales **en ese orden** (09 antes que 10), y el 10 reusa contratos que el 09 deja referenciables desde el despacho. APPROVED del ciclo en su ronda 2 (2026-07-29, base `91f0f00`): Codex acordó desde r1 el corte en tres features, el orden interno y el interinato multifase fail-closed; su único pedido — tres superficies faltantes del feature 10 (skills `design`/`plan` como hijos de pipeline, allowlist del instalador para que `build` viaje, generalización del contrato de `AXEL_REVIEW_ID`) — se verificó contra el código y se incorporó en r2. OK humano de la extensión: recibido el 2026-07-29.

**Extensión 2026-07-29 (pipeline)**: delta de plan escrito como unidad `plan-delta` del pipeline `/build` del 2026-07-29 — ledger: [implementation/pipeline-2026-07-29.md](implementation/pipeline-2026-07-29.md) —, autorizado por su gate el 2026-07-29 («dale, autorizado», sin ajustes de alcance). Pedido literal del gate: «fijar el modelo de los subagentes por tipo de unidad: los hijos que corren las skills design y plan (unidades design-delta y plan-delta de un pipeline) con Fable 5, y los hijos que corren feature (tanto los del modo lote de /feature all como las unidades feature de un pipeline) con Opus 5. Hoy todos heredan el modelo de la sesión padre, y la skill build tiene un párrafo «Modelos por unidad» que documenta esa decisión y habría que reescribir. El backlog del plan está vacío, así que el pedido necesita entrada de plan + implementación.» La confirmación liviana de `/plan` la absorbió ese gate; restricción de alcance de la ruta autorizada: el delta entra como **un** feature (el 11 — partirlo sería divergencia ⇒ corte). **Acuerdo entre agentes**: APPROVED del ciclo de review de esta unidad en su **ronda 3** (2026-07-29, base `2c64c8a`, head `e6d26e2`). Codex acordó desde r1 la sustancia del delta —un único feature (el 11), su posición por criterio 1, la reversión *registrada* (no borrada) de la decisión §10 del feature 10, la superficie estimada y las tres preguntas que quedan para la bajada— y sus dos pedidos fueron de forma, ambos corregidos: la §11 quedaba insertada dentro del cuerpo del feature 10 y arrastraba bajo su título los dos párrafos finales del 10 (se movió completa al final del doc, texto idéntico, en r2), y la línea «Esperando» de STATUS describía la espera en prosa libre en vez del vocabulario fijo del contrato (r3). El OK humano no es individual — llega con el RECAP consolidado del pipeline.

**Extensión 2026-07-29 (pipeline 2)**: delta de plan escrito como unidad `plan-delta` del **segundo** pipeline `/build` del 2026-07-29 — ledger: [implementation/pipeline-2026-07-29-2.md](implementation/pipeline-2026-07-29-2.md) —, autorizado por su gate el 2026-07-29 («a) ok con tu recomendacion · b) dejalo sin registrar»). **Pedido literal del gate** (transcripción completa; el mismo texto vive en el bloque Gate del ledger):

> «Que la skill `/adopt` cierre reportándole al humano el **inventario completo de archivos que tocó**, no solo la narrativa de las decisiones que le parecieron importantes.
>
> **Alcance: solo esto.** El humano acotó el pedido explícitamente a este cambio. Fuera de alcance, no lo agregues ni lo propongas como parte del pipeline: la reescritura del README público de axel (va en su propio pipeline, después) y el fix de la colisión `build/` del instalador (decidido dejar para después). Si el diseño te sugiere tocar otras skills, ver «Decisión abierta» abajo — pero el default es acotar.
>
> **El hueco.** `.claude/skills/adopt/SKILL.md` tiene 19 líneas y seis pasos. Ninguno pide un reporte al humano. El paso 6 (línea 17) dice: «Cerrar: borrá `docs/ADOPTION.md`, actualizá `docs/STATUS.md` y commiteá». Y la regla de cierre (línea 19) dice que los movimientos de archivos «van en el commit de cierre, visibles en el diff» — pero el humano no mira el diff: lee el mensaje con que la sesión termina el turno. Sin un paso que lo exija, cada corrida improvisa qué contar.
>
> **La evidencia: qué pasó realmente.** Se cerró la primera adopción real de axel fuera de sí mismo, en `/Users/alexweil/src/inquirylab` (commit `4908bfb`). El trabajo fue correcto — se auditó de punta a punta: cero pérdida de contenido en la mudanza de docs, 55 links relativos y 0 rotos, `ADOPTION.md` borrado, cuatro docs canónicos y symlink OK, `settings.json` cumpliendo `axel-policy.json`, árbol limpio, y un STATUS derivado del estado real (no plantilla). El problema fue el reporte, no la ejecución. El commit tocó **8 archivos**: `AGENTS.md` (18 ±), `DESIGN.md` (97 −, borrado de la raíz), `README.md` (6 ±), `docs/ADOPTION.md` (34 −, consumido), `docs/DESIGN.md` (131 +), `docs/IMPLEMENTATION.md` (367 +), `docs/STATUS.md` (13 ±), `docs/implementation/bitacora.md` (432 ±, rename desde IMPLEMENTATION.md). El reporte al humano habló de tres, y cerró diciendo «Dos cosas que decidí yo y conviene que mires». Dos omisiones concretas: (1) **`README.md` no se mencionó en absoluto** — el agente reescribió los links a DESIGN/IMPLEMENTATION (que se habían movido), agregó punteros a STATUS y AGENTS, y editó una fila de la tabla de mapa del proyecto; el cambio era correcto y necesario —sin él quedaban links rotos—, pero el humano aprobó a ciegas una edición a su README. (2) **`AGENTS.md` se subdeclaró** — el reporte dijo «agregué un bloque *Disciplina propia del proyecto*», cuando en realidad redactó la sección «Sobre este proyecto» entera: qué es el proyecto, qué se está construyendo, objetivo, alcance y fronteras; es interpretación sustantiva del proyecto ajeno, presentada como un bloque secundario. O sea: el reporte describió la narrativa de las decisiones difíciles en lugar del inventario de lo que tocó. Con `git show --stat` a la vista se ven 8 archivos; el humano escuchó 3.
>
> **Qué se busca.** Que el cierre de `/adopt` sea auditable de un vistazo, sin que el humano tenga que ir a correr `git show --stat` para descubrir qué se editó de su repo. Como mínimo el inventario completo de archivos tocados y qué se le hizo a cada uno (movido / fusionado / creado / borrado / editado), separando de forma visible **lo mecánico** (lo que el handoff mandaba) de **lo interpretativo** (texto que el agente escribió sobre el proyecto del humano y que conviene que ratifique). La forma exacta la decidís en el diseño. Dos criterios que importan: que **no dependa de que el agente se acuerde de contar** —debe salir de lo que el commit tocó, no de su memoria de lo que hizo— y que no infle el cierre hasta volverlo ilegible; en la adopción de inquirylab habrían sido 8 líneas.
>
> **Decisión abierta, para el gate.** El hallazgo salió de `/adopt`, pero las demás skills también cierran con un mensaje al humano (`recap`, `feature`, `design`, `plan`, `build`). Puede que el mismo hueco exista ahí, o puede que `/adopt` sea especial porque es la única que edita docs preexistentes de un proyecto ajeno sin que el humano conozca todavía la convención. Presentá en el gate si el cambio se acota a `/adopt` —que es lo que el humano pidió— o si conviene que sea un contrato común. El default es acotar: no amplíes por tu cuenta.
>
> **Notas de implementación.** El archivo es `.claude/skills/adopt/SKILL.md`. Es maquinaria: el instalador la sobreescribe en los destinos, así que el cambio se propaga solo al re-correrlo. No hay copia en `templates/` (ahí solo viven `AGENTS.md`, `DESIGN.md`, `IMPLEMENTATION.md`, `STATUS.md`, `settings.json`). Verificá igual si `AGENTS.md` y `templates/AGENTS.md` necesitan acompañar el cambio: `AGENTS.md` obliga a mantener las dos copias sincronizadas cuando se toca el proceso o las reglas; si esto es solo comportamiento de una skill, probablemente no aplique — pero decidilo, no lo asumas. El backlog de `docs/IMPLEMENTATION.md` está vacío (los once features cerrados) y `docs/STATUS.md` no tiene nada pendiente, así que este pipeline arranca de estado estable. Hay un segundo pendiente **anotado pero fuera de alcance**, por si el plan quiere dejarlo registrado en el backlog sin implementarlo: la skill `/build` vive en `.claude/skills/build/` y colisiona con el patrón `build/` de todo `.gitignore` de Python/Node/Java, lo que hace rebotar al instalador con un rechazo de preflight. Workaround verificado: agregar `!.claude/skills/build/` al `.gitignore` del destino, después del patrón que lo excluye (`!.claude/` NO funciona — git no re-incluye si un directorio padre intermedio quedó excluido).»

La confirmación liviana de `/plan` la absorbió ese gate. **Ajustes de alcance del gate, que mandan sobre este delta**: (a) la decisión abierta que el gate presentó —acotar a `/adopt` o volverlo contrato común de todas las skills que cierran con un mensaje al humano (`recap`, `feature`, `design`, `plan`, `build`)— se resuelve **acotando a `/adopt`**, con la condición registrada de que el mecanismo quede escrito de forma **reusable**, para que generalizarlo después sea adoptar un contrato y no reinventarlo; (b) el segundo pendiente que el pedido traía anotado —la colisión del directorio `build/` de la skill homónima con el patrón `build/` del `.gitignore` de los destinos, que hace rebotar al instalador— queda **sin registrar**: ni implementado ni anotado como feature del backlog. **Restricción de alcance de la ruta autorizada**: el delta entra como **un** feature (el 12); partirlo, o agregar features que el pedido dejó explícitamente fuera (la reescritura del README público de axel, la colisión `build/`), es divergencia ⇒ corte. **Acuerdo entre agentes**: APPROVED del ciclo de review de esta unidad en su **ronda 3** (2026-07-29, base `74d3f5c`, head `bcf34f3`, sin observaciones accionables). Codex acordó **la sustancia desde la r1** —un único feature 12, su posición por criterio 1, la ausencia de entrada de backlog para la colisión `build/` y para el README, la sección al final del doc, y superficie y preguntas suficientes para la bajada— y sus **siete** pedidos —cinco en la r1 y dos en la r2— fueron todos de forma o de precisión factual, aceptados sin argumentar: (r1) la promesa del resultado visible del ledger daba por hecho que un re-run del instalador sobre inquirylab produciría el inventario, cuando el modo update propaga la skill pero **no reabre una adopción cerrada** (`tests/install.sh` T5) — corregida con el texto original conservado, y la aclaración replicada en la «Verificación prevista» del 12 para que la bajada no repita el error; el pedido literal del gate entraba como núcleo con elipsis y ahora va **completo**; la atribución de cobertura del cierre del pipeline anterior invocaba la excepción post-OK para dos bullets de sustancia; la línea «Esperando» de STATUS volvía a prosa libre; y una línea en blanco al EOF de [implementation/11-subagent-models.md](implementation/11-subagent-models.md) rompía `git diff --check`. (r2) La corrección de la atribución nombraba el «barrido de la ronda 1», pero una ronda `CHANGES_REQUESTED` no otorga cobertura ni mueve la base — la otorga el `APPROVED` final del ciclo; y la línea «Esperando» conservaba prosa tras el literal, cuando el vocabulario del contrato es fijo y no un prefijo. El OK humano no es individual — llega con el RECAP consolidado del pipeline.

**Extensión 2026-07-29 (pipeline 3)**: delta de plan escrito como unidad `plan-delta` del **tercer** pipeline `/build` del 2026-07-29 — ledger: [implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md) —, autorizado por su gate ese mismo día («dale, autorizado — el push y los topics los hago yo»). **Pedido literal del gate** (transcripción completa; el mismo texto vive en el bloque Gate del ledger):

> «Dejar el repo público de axel presentable para compartirlo: README en inglés escrito para quien lo descubre, licencia MIT, y los docs de onboarding y feedback que faltan.
>
> Todo lo de abajo son decisiones ya tomadas con el humano y hallazgos ya verificados en una sesión previa. No están registrados en ningún doc del repo — este pipeline es quien los baja a diseño, plan y features.
>
> **## Alcance: qué NO entra**
>
> El humano decidió explícitamente que este pipeline se ocupa **solo del repo público**. Fuera de alcance, ya decidido, no re-abrir en el gate:
>
> - **El fix del instalador para la colisión `build/`** (ver «Problema conocido» abajo). Queda para después; por ahora los primeros colegas agregan la regla a mano y **el README/`docs/install.md` tienen que documentarlo**.
> - Los dos mensajes de rechazo del instalador que dan fricción (también abajo): documentarlos si ayuda, no arreglarlos acá.
>
> **## Contexto**
>
> El repo es público (github.com/alexweil/axel, creado 2026-07-27, 0 stars) y `main` está pusheado y sincronizado en `88020af`. Estado del loop: estable, backlog vacío, los once features cerrados.
>
> Difusión en **dos etapas**: primero entre colegas para feedback, después publicación amplia (HN, Twitter). La vara final es HN; la urgencia inmediata es que un colega entienda qué es esto y pueda instalarlo.
>
> **## axel ya se instaló en otro repo real — usar esto**
>
> Se instaló axel en `/Users/alexweil/src/inquirylab` (repo activo, 181 commits, con `docs/` propios ⇒ modo adopción), commit `846308f`, 20 archivos. La adopción quedó pendiente de cerrar con `/adopt` ahí (handoff en `inquirylab/docs/ADOPTION.md`, 21 docs preexistentes listados como candidatos a mapear).
>
> Esto importa para el README por dos razones:
> 1. **Responde la objeción más letal** — "¿funciona en algo que no sea axel mismo?". Hasta ahora axel se había validado solo sobre axel, y `tests/install.sh` es sintético.
> 2. **Es la fuente del transcript** de la sección "What a session actually looks like". Un ejemplo sobre un proyecto cualquiera se lee solo; uno de axel diseñándose a sí mismo es recursivo y confuso para quien recién llega. Si `/adopt` ya se cerró en inquirylab cuando corras, hay material real; si no, usar lo que haya y no inventar.
>
> **Lo que funcionó bien en la instalación real** (no romperlo, y sirve como evidencia): el cache `~/.axel` se auto-actualizó por fast-forward, los anuncios de fuente/destino salieron antes de tocar nada, el rechazo fue fail-closed sin escribir un byte, apareció la línea final `── axel · fin: rc=N · … ──`, los 18 archivos quedaron dentro del diff, el symlink `CLAUDE.md → AGENTS.md` correcto y el handoff `ADOPTION.md` claro.
>
> **## Problema conocido que el README debe documentar (no arreglar)**
>
> La skill `/build` vive en `.claude/skills/build/`, y **`build/` es uno de los patrones de `.gitignore` más comunes que existen** — está en el template de Python, Node, Java, Gradle, Maven y C/C++. El preflight del instalador lo detecta y rechaza fail-closed:
>
> ```
> rechazo: .claude/skills/build/SKILL.md: nacería ignorado por las reglas del destino; nada del instalador puede quedar fuera del diff
> ── axel · fin: rc=2 · rechazo (ver el detalle arriba) ──
> ```
>
> Ninguna otra skill colisiona (adopt, design, feature, plan, recap, status). El preflight se comporta **correctamente**: el diseño acertó, lo que falla es el nombre de un directorio.
>
> **Workaround verificado empíricamente**, que es lo que hay que documentar: agregar al `.gitignore` del destino, **después** del patrón `build/`:
>
> ```
> !.claude/skills/build/
> ```
>
> Trampa que hay que decir explícitamente porque es contraintuitiva: **`!.claude/` NO funciona** — git no re-incluye un archivo si un directorio padre intermedio quedó excluido. La negación tiene que nombrar el directorio. `!.claude/**` también funciona pero es más amplio de lo necesario.
>
> Documentarlo como "known issue" con el workaround y con la nota de que se va a arreglar. Es lo primero que le va a pasar a un colega con un repo Python, y sin esto el mensaje de rechazo es críptico.
>
> **## Otros dos puntos de fricción observados (documentar si ayuda, no arreglar)**
>
> 1. El rechazo por árbol sucio dice `el árbol del destino no está limpio; commiteá o stasheá` pero **no lista qué archivos** están sucios, y **`git stash` sin `-u` no toca untracked** — que es el caso más común en un repo activo. El usuario stashea, sigue rechazado y no entiende por qué.
> 2. El instalador anunció `modo: initial` cuando lo que ejecutó fue una **adopción** (detectó 21 docs propios, escribió `ADOPTION.md`, salió con RC=1). `AGENTS.md` documenta tres modos —instalación / adopción / actualización— así que el modo anunciado contradice el ejecutado. Consecuencia visible: inquirylab quedó con `DESIGN.md` e `IMPLEMENTATION.md` en la raíz (suyos) **y** `docs/DESIGN.md` + `docs/IMPLEMENTATION.md` (semillas) — cuatro archivos, dos nombres, sin ningún aviso de cuál manda. Es lo diseñado (las semillas no pisan nada y `/adopt` resuelve), pero nadie lo explica.
>
> **## Decisiones ya tomadas por el humano**
>
> - **Idioma**: README y CONTRIBUTING en **inglés** (es la vidriera de GitHub). `AGENTS.md`, `docs/DESIGN.md`, `docs/IMPLEMENTATION.md`, `docs/STATUS.md`, los subdirectorios y los commits **siguen en español**, como manda la convención del repo. No traducir los docs del método.
> - **Licencia**: **MIT**. Hoy falta por completo — el repo es público sin licencia, o sea legalmente "todos los derechos reservados", lo que contradice que su propuesta central sea "instalalo en tu proyecto".
> - **Difusión en dos etapas**: colegas primero, público amplio después.
>
> **## El activo que hoy no se usa: las métricas del reviewer**
>
> `.claude/state/rounds-log` (no versionado) tiene **70 rondas de review**: 14 sesiones nuevas de Codex + 56 rondas de resume.
>
> - `CHANGES_REQUESTED`: **46** (66%)
> - `APPROVED`: **24**
> - Aprobados en ronda 1: **0**
> - Mediana hasta aprobar: **4 rondas**; peor caso: **11**
>
> Esto responde solo la objeción "el reviewer es un sello de goma que aprueba todo". **Precisión obligatoria**: los 24 APPROVED son *hitos* (bajada fina, implementación), no features — hay más aprobados que features, y enunciarlo mal es exactamente lo que destruye credibilidad en HN. La formulación defendible es "70 rounds, 46 rejections, zero first-round approvals". Como `rounds-log` no está versionado, **extraer las cifras a un doc versionado** para que un tercero pueda auditarlas, y decir de dónde salen.
>
> Ejemplo concreto y citable de una review que atrapó algo real: commit `74d3f5c` — "tres defectos invisibles en la lectura del texto nuevo".
>
> **## Posicionamiento acordado**
>
> El gancho es un hecho verificable, no una promesa: **este repo se construyó a sí mismo con su propio método** — 176 commits en 3 días, 11 features, cada uno con su loop hasta APPROVED, con ledgers de lote y pipeline versionados en `docs/implementation/`.
>
> Los cuatro diferenciales frente a los cincuenta "AI coding agent" que salen por semana:
> 1. Review **cross-vendor** (Claude contra GPT), no un modelo revisándose a sí mismo ni dos instancias del mismo.
> 2. El reviewer **ejecuta**: corre tests y builds en un worktree snapshot clavado al commit. Verificación, no confianza.
> 3. La memoria es **el repo**, no la ventana de contexto. Sesión nueva = leer tres archivos.
> 4. Se construyó a sí mismo y es **auditable**.
>
> Estructura propuesta para el README (~120 líneas, inglés), a validar y refinar en la unidad de diseño:
>
> 1. Título + una línea + las tres cifras del reviewer.
> 2. **The problem** (~6 líneas): un agente solo no se audita a sí mismo y pierde el hilo entre sesiones.
> 3. **What a session actually looks like** — transcript real en markdown: pedido sin comando → gate → un `CHANGES_REQUESTED` verdadero → RECAP → OK. Va **arriba**, antes de instalar: es la sección que convierte.
> 4. **Requirements, honestly** — Claude Code **y** Codex CLI (dos suscripciones, dos proveedores distintos), macOS (`caffeinate`), git, python3, y que las reviews en xhigh tardan >10 min. Hoy no está declarado en ningún lado.
> 5. **Install** — el one-liner, el known issue de `build/` con su workaround, y nada más.
> 6. **The six commands** — tabla de una línea cada uno.
> 7. **How it works** — el diagrama de flujo de `docs/DESIGN.md` + los 5 principios comprimidos.
> 8. **What this is not** — sección de honestidad (ver objeciones abajo).
> 9. Links: diseño, contrato de review, cómo dar feedback.
>
> Toda la casuística actual del README (pipefail, exit codes, forks, corridas interrumpidas, y la sección entera "Para agentes (Claude Code)") **se conserva pero se muda a `docs/install.md`**, referenciada desde el README. Hoy el README tiene 70 líneas y ~50 son eso: está escrito para quien ya decidió usarlo, no para quien lo descubre.
>
> **## Objeciones de HN que el README debe contestar antes de que aparezcan**
>
> | Objeción | Respuesta acordada |
> |---|---|
> | "Cuesta una fortuna" | Declararlo de frente en Requirements: dos suscripciones, reviews largas. Es caro y es a propósito. |
> | "El reviewer es otro LLM, ¿por qué confiaría?" | Las métricas + que Codex ejecuta tests, no solo lee el diff. |
> | "Es un montón de markdown, no un framework" | Sí, y es la virtud: sin dependencias, sin lock-in, se lee en una tarde. Decirlo primero uno mismo. |
> | "Está todo en español" | Declararlo sin disculpas: README en inglés, docs del método en español, la maquinaria es agnóstica del idioma. |
> | "macOS only" | Declarar qué se rompe fuera de macOS (`caffeinate`) y qué no. |
> | "¿Funciona en algo que no sea axel mismo?" | Ahora sí hay respuesta: la instalación real en inquirylab. Ser honesto sobre su alcance (un repo, del mismo autor). |
>
> **## Archivos nuevos previstos**
>
> `LICENSE` (MIT) · `docs/install.md` (todo lo que sale del README + el known issue) · `CONTRIBUTING.md` en inglés (cómo dar feedback — es lo que el humano busca de esta ronda) · `.github/ISSUE_TEMPLATE/` · un doc versionado con las métricas del reviewer · topics y homepage en el repo de GitHub.
>
> Sobre demo visual: se descartó asciinema por ahora (las reviews tardan >10 min y habría que editarlo). Empezar con el **transcript en markdown** — cero infra, funciona en móvil, se lee en 20 segundos.
>
> **## Orden sugerido de features (a acordar en el plan)**
>
> 1. **Vidriera** — LICENSE MIT + README en inglés + `docs/install.md` con el known issue.
> 2. **Onboarding y feedback** — CONTRIBUTING + `.github/` + métricas del reviewer versionadas.»

**Datos del literal que ya no son vigentes.** El pedido venía de una sesión previa; la transcripción de arriba se conserva **tal cual se pidió** y lo vigente es esto. La tercera columna importa tanto como la segunda: **no todo se corrigió en el mismo momento ni con la misma autoridad** — el gate re-verificó unas cifras antes de autorizar, y otras se descubrieron mal **después**, ya dentro de este pipeline.

| Lo que dice el pedido | Vigente | Cuándo y dónde se corrigió |
|---|---|---|
| 176 commits en 3 días | **196** | verificado al abrir el gate (`39b377e`) |
| 11 features cerrados | **12** | ídem |
| 70 rondas · 46 `CHANGES_REQUESTED` · 24 `APPROVED` · 14 sesiones nuevas + 56 resume | **80** · **53** · **27** · **16** + **64** | ídem |
| La adopción de inquirylab quedó **pendiente** de cerrar con `/adopt` | **cerrada** (commit `4908bfb`): hay material real para el transcript | ídem |
| Mediana hasta aprobar: **4** rondas | **4,5 por ciclo** y **3 por hito** | **después del gate**, en la review de la unidad `design` de este pipeline: el «4» contaba como completo el ciclo **parcial** con que arranca el `rounds-log`, y el gate lo había dado por bueno |
| Aprobados en ronda 1: **0** | se sostiene **por ciclo**; **por hito hay uno** sin ningún rechazo en su tramo (`2dbbdfc`) | el claim es del gate; la **unidad** la precisó la unidad `design` |
| Peor caso: **11** | **11 por ciclo**, **5 por hito** | ídem |
| «Hoy el README tiene 70 líneas» | **69** | verificado en **esta** unidad contra la base `31471be` |

Se sostienen sin cambio: la precisión obligatoria de que los `APPROVED` son **hitos y no features**, y el workaround de `build/`, confirmado en un repo real (`98c70c0`). Las tres filas que nombran la unidad la nombran a propósito: el diseño la exige en cada cifra publicada, y las dos formulaciones del pedido («cero en ronda 1», «peor caso 11») son ciertas **por ciclo** y falsas por hito. La tabla del gate, con su procedencia y con el texto original tachado en vez de borrado, está en el bloque «Hechos del pedido que el padre verificó al abrir el gate» del [ledger](implementation/pipeline-2026-07-29-3.md).

La confirmación liviana de `/plan` la absorbió ese gate. **Ajustes de alcance del gate, que mandan sobre este delta**: (b) **el pipeline no toca GitHub ni pushea** — ni `git push`, ni topics, ni homepage, ni ninguna otra acción sobre el remoto o sus settings; la unidad `14` **deja los comandos exactos listos** y los corre el humano al leer el resultado, y ejecutarlos desde el pipeline es divergencia ⇒ corte. (a) y (c) **sin ajuste**: la corrección de las cifras del pedido queda como la registró el bloque «Hechos del pedido…» del ledger, y el **origen del transcript** —que el gate delegó explícitamente a la unidad de diseño— ya lo resolvió esa unidad: dos bloques citables con procedencia propia y ninguna línea inventada ([design/public-surface.md](design/public-surface.md)).

**Restricción de alcance de la ruta autorizada**: el delta entra como **dos** features, 13 y 14, con el corte que la ruta les fijó. Partirlos, fusionarlos, o agregar features que el pedido dejó explícitamente fuera —el fix de la colisión `build/`, los dos mensajes de rechazo del instalador— es divergencia ⇒ corte.

**Dos cosas que este delta deliberadamente NO registra en el backlog**, para que «no entró» quede como decisión asentada y no como descuido:

- **Los tres puntos de fricción del instalador** (colisión `build/`; rechazo por árbol sucio que no lista los archivos sucios; `modo: initial` anunciado cuando lo ejecutado fue una adopción). Se **documentan** —el diseño fija dónde se declara cada uno— y no se arreglan ni reciben entrada de backlog: decisión explícita del humano en este gate y en el anterior.
- **La deuda normativa que declaró el diseño**: la política de idioma vuelve falsa la línea de `AGENTS.md` §Convenciones «Docs, commits y comunicación en español», y corregirla (más su espejo obligatorio en `templates/AGENTS.md`) está **fuera de la ruta autorizada** — ni el 13 ni el 14 la incluyen. El diseño ya cerró que **no se resuelve delegándola al plan**: meterla en el 13 sería ampliar en silencio un scope que el humano fijó. Sus dos únicas vías válidas de cierre —cambio de scope ⇒ corte, o autorización junto con el OK ejecutada en una unidad aparte y posterior— están en [design/public-surface.md](design/public-surface.md) §«Política de idioma»; el pipeline termina con la deuda abierta y declarada.

**Acuerdo entre agentes**: APPROVED del ciclo de review de esta unidad en su **ronda 4** (2026-07-29, base `fa12f41`, head `b0bdf4d`, sin observaciones accionables). Codex acordó **desde la r1** lo que un plan decide —el corte en dos features, el orden 13 → 14 con su criterio, y la ausencia de entrada de backlog tanto para los tres puntos de fricción del instalador como para la deuda normativa— y **no discutió la sustancia en ninguna ronda**: sus **nueve** pedidos —4 en la r1, 3 en la r2, 2 en la r3— fueron de trazabilidad, de coherencia interna y de precisión factual, y se aceptaron **los nueve sin argumentar ninguno**. Los tres de fondo:

- **El literal del gate entraba parcial, con un argumento del generador que no se sostenía**: se invocó la «fuente única» del contrato editorial, y ese contrato rige las cifras de la **vidriera publicada**, no una cita histórica dentro del plan. Entra completo —117 líneas, verificadas idénticas al ledger por diff mecánico y confirmadas por Codex con SHA-256—, con una tabla adyacente que separa **dos procedencias que estaban mezcladas**: lo que el gate re-verificó **antes** de autorizar y lo que se descubrió mal **después**, ya dentro de este pipeline. La tabla suma además la fila que faltaba (el literal dice «70 líneas» del README y son **69**) y nombra la **unidad** en las cifras que la exigen.
- **El contrato entre el 13 y el 14 era internamente incoherente y estaba incompleto**: el 13 dejaba un link roto a propósito mientras su propio criterio de verificación exigía cero links rotos; movía al 13 la elección de paths que el diseño le había asignado al 14; faltaba una **segunda** dependencia hacia adelante (`CONTRIBUTING.md`, destino del «cómo dar feedback» de la estructura aprobada del README); y no fijaba la **coincidencia temporal** entre el corte con que el 13 publica cifras y el snapshot que crea el 14 — con las rondas de review de ambos features moviendo el log mientras tanto. Quedó como **estado intermedio verificable** (referencias no activas y marcadas, paths elegidos por el 14, activación y chequeo final en el 14) sobre **un solo corte**, declarado por el 13 y reconstruido por el 14.
- **Una corrección del generador en la r3 excedió la autoridad de la unidad, y la r4 la revirtió**: había degradado los «comandos exactos» de GitHub que el gate autorizó a un «mecanismo documentado». La promesa está **autorizada** y una unidad `plan-delta` no la reescribe porque resulte incómoda de cumplir; además, cerrarla en el RECAP no la satisfacía ni en sus propios términos, porque para entonces el 14 ya estaría aprobado y cerrado. Restaurada, con los valores propuestos por la bajada del 14 —proponer no es decidir: el humano conserva la decisión porque es quien los corre— y con la salida declarada de **fuentes insuficientes ⇒ corte** si no hubiera propuesta defendible.

El OK humano no es individual — llega con el RECAP consolidado del pipeline.

### 01 — Instalador

Un comando que instala la maquinaria en un repo destino: copia `.claude/skills/`, `scripts/` (review.sh, awake.sh) y `.claude/settings.json`, y siembra `AGENTS.md` + symlink `CLAUDE.md` + estructura `docs/` con plantillas. axel es la fuente de verdad; los proyectos consumidores se actualizan re-corriendo el instalador.

Requisito clave (pedido humano 2026-07-27): **modo adopción** para proyectos existentes que ya vienen siguiendo el proceso a mano — detectar docs preexistentes (DESIGN/IMPLEMENTATION o equivalentes), no pisar nada, mapear lo que ya hay a la convención de axel, sembrar solo lo que falte y derivar el `STATUS.md` inicial del estado real del proyecto.

### 02 — Instalación remota (one-liner)

Pedido humano (2026-07-27, al dar el OK del 01): instalar axel en un destino **sin clon local previo** — un solo comando que baje axel (GitHub) y corra la instalación de una, más la sección del README pensada para que un agente en Claude Code la lea y la siga ("instalá axel siguiendo <url>"), cubriendo también el camino desde adentro de una sesión (las skills se cargan en caliente; el settings rige pleno en la sesión siguiente). Prerequisito **cumplido** (2026-07-27, pedido y visibilidad confirmados por el humano): axel publicado en https://github.com/alexweil/axel (público, `main` como remote `origin`). Enfoque decidido en la bajada (2026-07-27): `install.sh --from <url>` con cache fail-closed en `AXEL_HOME` (default `~/.axel`) — solo ff-update de un cache limpio en el branch default real del remoto (ancestría explícita contra `ls-remote`/fetch por URL, sin confiar en metadata local), disjunción cache/lock↔destino pre-clone, lock atómico sin auto-reclaim — que delega en el `install.sh` del clon propagando solo sus exit codes contractuales (0/1/2); el instalador local no cambia de contrato. Bajada fina: [implementation/02-remote-install.md](implementation/02-remote-install.md).

### 03 — Hardening del loop

Pieza central (pedido del reviewer en el ciclo de plan): **suite de regresión reproducible** para el estado del loop y sus caminos de seguridad — las siete clases de falla que el ciclo 00 encontró a mano y quedaron sin prueba automatizada: parser de veredicto + gate de RC, consistencia del estado (`last-verdict`, base, racha), bloqueo de deadlock, `wt_valid` contra directorios impostores, tri-estado y `kill_confirmed` de awake.sh, movimiento de base a `REVIEW_HEAD`, y **congelamiento de la observación**: el worktree del reviewer queda re-clavado al `REVIEW_HEAD` del pedido aunque el HEAD canónico avance durante la review (regresión distinta del movimiento de base y del rechazo de impostores). Ejecutable sin invocar a Codex (doble del binario vía PATH).

Además: captura robusta del session id (hoy: primer UUID de los eventos JSONL, con fallback `resume --last`), reintentos ante fallas transitorias de Codex, métricas de rondas por feature, y `shellcheck` instalado y corriendo sobre los scripts (el reviewer lo buscó en las 4 rondas del ciclo 00 sin encontrarlo).

### 04 — Notificaciones y continuidad

Push de RECAP consistente en todos los caminos, y facilitar el arranque de la siguiente sesión limpia tras el OK (chip de spawn en desktop / instrucción única).

### 05 — Confirmación previa a la implementación en `/feature`

Pedido humano (2026-07-28, al extender el plan): al arrancar un feature nuevo con `/feature`, **lo primero** es mostrarle al humano un breve resumen de lo que se va a implementar y pedir su confirmación **antes** de ejecutar la implementación. Con la confirmación, el flujo sigue idéntico al actual: bajada fina → review → implementación con loop → RECAP → OK humano de integración. El checkpoint nuevo no reemplaza al OK final — agrega un gate barato al inicio, cuando el humano está presente por construcción (acaba de abrir la sesión o clickear el chip), para validar el rumbo antes de gastar bajada y rondas de review en algo desviado.

**Origen del resumen** (decisión de plan, r1 de este ciclo): `/feature` lo **deriva** de lo disponible al momento del gate — la entrada del feature en este plan (fila de la tabla y, si existe, su sección) más `DESIGN.md` si hace falta — y si las fuentes no alcanzan para un resumen honesto, el gate lo dice explícitamente y presenta lo que hay, sin bloquearse ni inventar. Alternativa descartada: exigirle a `/plan` que garantice una sección por feature — impondría un contrato que los planes adoptados (mapeados de docs preexistentes) no cumplen; `plan/SKILL.md` queda fuera de la superficie.

**Persistencia y bootstrap** (r1 de este ciclo): el estado del gate queda persistido en docs — al presentarlo, STATUS pasa a "esperando confirmación de arranque"; al recibir la confirmación, el doc del feature la registra junto con cualquier corrección de alcance del humano — de modo que una sesión reabierta **re-presenta** el gate en vez de saltearlo (análogo al camino "esperando OK"). Y el 05 se aplica a sí mismo: la sesión `/feature` que lo implemente abre con el gate en modo manual — resumen derivado de esta entrada + confirmación humana antes de su propia bajada fina — porque la skill todavía no lo trae; esta entrada es la instrucción que esa sesión sigue.

Superficie estimada — la bajada fina decide el detalle: skill `feature` (el gate como paso 0 del camino "feature nuevo" + re-presentación al reabrir), la línea de proceso de `/feature` en `AGENTS.md` + `templates/AGENTS.md` (regla de sincronía), el flujo en `DESIGN.md` (diagrama y fila de decisiones), y la línea "Cómo se trabaja un feature" de este doc y de `templates/IMPLEMENTATION.md`. Pregunta restante para la bajada: encaje fino con el protocolo de aviso del feature 04 (la espera del gate se alcanza interactivamente ⇒ sin push; qué pasa si la sesión se reabre después).

### 06 — One-liner corto: defaults del bootstrap remoto

Pedido humano (2026-07-28, al extender el plan): soportar la versión "de verdad corta" del one-liner de instalación:

```bash
curl -fsSL https://raw.githubusercontent.com/alexweil/axel/main/scripts/install.sh | bash
```

Hoy el modo piped exige `--from <url>` y el destino explícito — decisión fail-closed deliberada del feature 02 (piped sin `--from` ⇒ exit 2 con mensaje que apunta a `--from`). El feature la revisa con dos defaults independientes:

- **`--from` ausente** → la URL canónica de axel (`https://github.com/alexweil/axel`), cableada en el script — la misma constante que hoy vive solo en el string del README.
- **Destino ausente** → el toplevel del repo git donde está parado el caller (el equivalente de `git rev-parse --show-toplevel` del cwd); cwd fuera de un repo git ⇒ rechazo con diagnóstico claro.

**Por qué es aceptable revisar el fail-closed del 02**: aquella exigencia no protegía de una acción destructiva sino de la **ambigüedad** (¿qué fuente? ¿qué destino?) en un modo sin `$0` en disco. Los defaults eliminan la ambigüedad declarándola en vez de exigirla: la corrida anuncia fuente y destino asumidos antes de actuar, y **toda** la validación existente queda intacta — cache fail-closed, disjunción cache/lock↔destino, lock, y el preflight del delegado (toplevel, árbol limpio, rechazo de self-install). El instalador sigue sin commitear (el diff queda para el proceso del destino) y las formas explícitas actuales no cambian de contrato: los defaults solo completan argumentos ausentes.

**Tradeoff residual que el default de destino abre** (r1 de este ciclo): el destino pasa a ser **ambiental** — corrido desde el repo limpio *equivocado*, todos los preflights pasan y ese repo se modifica; el anuncio del destino asumido no lo previene por sí solo. Se acepta con registro honesto: árbol limpio exigido + diff visible + instalador que no commitea lo vuelven recuperable (revertir el diff con git), y es un riesgo que las formas explícitas nunca tuvieron — la bajada debe documentarlo (README y guía para agentes: verificá dónde estás parado antes de correrlo, y cómo revertir) y cubrirlo en la matriz.

**Semántica de fallo del transporte** (r1 de este ciclo): el one-liner exacto corre sin `pipefail` — si `curl` falla sin producir script, `bash` recibe entrada vacía y retorna **0**: un falso éxito que la taxonomía 0/1/2 del instalador no puede cubrir, porque el instalador nunca arrancó (repro del reviewer: `sh -c 'exit 22' | bash` ⇒ RC 0). La bajada debe decidir explícitamente entre **modificar el comando recomendado** para propagar el fallo (p. ej. envolverlo — tensiona el one-liner exacto pedido) o **conservarlo tal cual** con un contrato verificable de éxito (p. ej. una línea final inconfundible en la salida del instalador) y el fallo de descarga documentado en la guía y testeado.

**Resolución de las dos decisiones abiertas** (bajada, 2026-07-28): los defaults quedan acotados al **camino de bootstrap** (el modo local conserva su contrato: fuente = el clon, destino obligatorio), y el fallo del transporte se resuelve **conservando el comando exacto** más un contrato verificable de finalización (línea final `── axel · fin: rc=N`, única, firmada solo desde el primitivo terminal, con la incompletitud del delegado saliendo por un RC no contractual). Detalle, fronteras y matriz: [implementation/06-oneliner-defaults.md](implementation/06-oneliner-defaults.md).

Preguntas que la bajada fina debe resolver (estimadas, no exhaustivas): combinaciones intermedias (`--from <url>` sin destino; destino sin `--from` — intuición: cada default es independiente y ambas valen, la bajada lo decide y lo testea); qué pasa con el modo **local** sin argumentos (¿también defaultea el destino al toplevel del cwd, o el default es solo del camino `--from`/piped? — atención al caso de correrlo parado adentro del propio clon de axel, hoy rechazado por el preflight de self-install); la **sutileza del fork** (el script piped de un fork defaultearía a la URL canónica, no al fork — no hay forma de detectar el origen de un script por stdin; probablemente se documenta); testeabilidad **sin red** del default de URL (la matriz actual corre con remotos locales — el default cableado a GitHub necesita override por env para los tests, o cobertura solo en la aceptación real); y la actualización del README + guía para agentes (el one-liner corto como primera opción, las formas explícitas conservadas).

### 07 — Batch de features: `/feature all` y `/feature NN..MM`

Pedido humano (2026-07-28): ejecutar varios features pendientes del plan en una sola corrida, sin OK humano entre features, sin perder los checkpoints — se agrupan en un **gate de lote** al inicio (N resúmenes derivados de los docs, una autorización global, exclusiones puntuales) y un **RECAP consolidado** al final cuyo OK es el que cierra. **El diseño ya está cerrado** (APPROVED de Codex r4 + OK humano, 2026-07-28) en [design/batch-features.md](design/batch-features.md); este feature es su implementación. `/feature` sin argumento no cambia.

Lo fijado por el diseño (la bajada no lo rediscute): arquitectura **orquestador supervisor + un subagente fresco por feature** (la frontera de contexto la aporta la maquinaria; el padre no transporta feedback — empuja al hijo al ver la señal terminal de la review, respaldado por el spike del 2026-07-28); **señal terminal atómica con identidad de invocación** en `review.sh` (todo camino de salida la escribe como último acto — cambio chico y versionado); estado durable en un **ledger versionado por corrida** con protocolo de reentrada fail-closed; semántica de checkpoints (gate = autorización de ejecución, APPROVED de Codex intacto como gate de calidad, estado intermedio «APPROVED — pendiente OK de lote», OK consolidado = cierre); las **condiciones de corte** (las del loop actual + timeout de señal terminal, cambio de scope, divergencia contra el resumen autorizado, fuentes insuficientes); y la cobertura de los commits no revisados (pre-OK en dos partes: los commits de estado intermedios los barre la r1 del feature siguiente del lote — regla vigente — y solo los remanentes del último feature, el ledger y el STATUS «esperando OK» viajan listados en el RECAP consolidado y los cubre su OK; post-OK, la excepción del commit de registro del OK del contrato).

Superficie estimada — la bajada fina decide el detalle: skill `feature` (los dos modos nuevos, el contrato padre↔hijo, el gate de lote), `scripts/review.sh` (la señal terminal), `AGENTS.md` + `templates/AGENTS.md` (la redefinición de la regla dura "nunca continuar sin OK humano" — regla de sincronía), skill `recap` (el RECAP consolidado abarca desde el último OK humano, no desde `last-approved-sha` — tras N APPROVED del lote esa base ya avanzó; el mecanismo exacto lo decide la bajada), `docs/design/review-contract.md` (la señal terminal pasa a ser comportamiento contractual versionado de `review.sh`, y ese doc es el contrato canónico que además viaja como payload de cada review), plantilla del ledger, y los tests del loop (la señal terminal entra a la suite de regresión del 03). Las decisiones que el diseño dejó explícitamente para la bajada están listadas al final de [design/batch-features.md](design/batch-features.md) (forma exacta del terminal y del ledger, protocolo del empujón, timeout del padre, bajada de mensajes humanos a mitad de feature, validación del rango).

### 08 — Reentradas de fase y feature: inventario completo fail-closed

Origen: diseño de entrada implícita ([design/implicit-entry.md](design/implicit-entry.md), §«Reentradas — inventario real»), cerrado con OK humano el 2026-07-29. El ruteo por contexto (09) entrega todo pedido que llega con estado pendiente a la **reentrada de la skill dueña** — pero el inventario real tiene huecos: `/feature` cubre «esperando OK», «esperando confirmación de arranque» y el lote (autorización pendiente, reentrada por ledger), pero **no** el feature individual **activo** (en bajada, implementación o con review en vuelo); `/design` y `/plan` no tienen ramas para «review en curso» ni «esperando OK». Este feature cierra el inventario **antes** de que el ruteo dependa de él — y tiene valor propio sin ruteo: hoy reabrir una sesión caída a mitad de un loop de `/design` o `/plan` no tiene camino definido ni con comandos explícitos.

Lo fijado por el diseño: semántica **fail-closed idéntica a la reentrada del lote** — reconstrucción desde STATUS, el doc de la fase o feature (Review log incluido) y el estado de review en `.claude/state/` (ronda, señal terminal); **sin relanzar ni duplicar una review en vuelo**; ante inconsistencia, RECAP sin adivinar.

Superficie estimada — la bajada decide el detalle: skills `design`, `plan` y `feature` (ramas de reentrada), y lo que STATUS deba registrar para que la reconstrucción alcance (¿estados nuevos en «Esperando»/«Ronda»?). Preguntas para la bajada: cómo distingue la reentrada una review **en vuelo** de una **muerta** (la señal terminal del 07 se escribe al salir — su ausencia es ambigua: ¿corriendo o crasheada?; candidatos: proceso vivo + timestamp del pedido, o entrega fail-closed al humano en la duda), y cuánta de la reconstrucción es compartible entre las tres skills (reconstruyen lo mismo con fuentes distintas).

### 09 — Ruteo implícito: triggers y despacho por contexto

Origen: mismo diseño, §Nivel 1. Un pedido sin comando se despacha a la fase correcta **sin maquinaria nueva**: (1) las `description` de las skills pasan a ser **triggers** por contexto; (2) sección de ruteo en `AGENTS.md` (espejo obligatorio en `templates/AGENTS.md`) con el **orden de despacho contractual fail-closed** — consulta → solo `/status`; **estado pendiente manda** (entrega a las reentradas del 08 — de ahí la dependencia); clasificación del pedido solo desde estado estable, por lo que el pedido necesita; ambigüedad → pregunta de una línea. Además: la **regla de confirmación** (el ruteo nunca arranca trabajo sin punto de confirmación; `/plan` despachado gana confirmación liviana — la invocación explícita directa queda como hoy), la corrección de la línea de `AGENTS.md` que presenta `/recap` como consulta que «no cambia el trabajo» (su skill fija «esperando OK» y commitea — alcance fijado por el diseño), y el ruteo en lenguaje natural inequívoco al modo lote (reinterpretación del opt-in ya registrada en [design/batch-features.md](design/batch-features.md): lo que protege es la autorización del gate, no el tipeo).

**Interinato multifase** (consecuencia del orden 09 → 10): mientras `/build` no exista, el caso «pedido que necesita dos o más fases» del orden de despacho no tiene destino. La bajada define el comportamiento interino fail-closed — candidatos: anunciar que el camino disponible es fase por fase y despachar a la primera fase necesaria con su gate, o directamente la pregunta de una línea — y el 10 lo reemplaza por el despacho a `/build`. Lo que el interinato no puede hacer: encadenar fases sin OK humano (la regla dura sigue intacta hasta el 10).

Superficie estimada — la bajada decide el detalle: `description` de las skills (`design`, `plan`, `feature`, `status`, `recap`, `adopt`), `AGENTS.md` + `templates/AGENTS.md` (sección de ruteo + línea de `/recap` — regla de sincronía), skill `plan` (la confirmación liviana cuando llega despachado, y su persistencia si la espera puede quedar abierta — ¿análoga al gate del 05?). Preguntas para la bajada: redacción exacta de triggers y tabla (el diseño la dejó abierta) y cómo se verifica el ruteo sin harness de despacho (¿matriz de casos pedido→destino en el doc del feature, verificada por el reviewer?).

### 10 — Pipeline `/build`: unidades tipadas con gate único

Origen: mismo diseño, §Nivel 2. Skill nueva `build`: un pedido que cruza dos o más fases encadena los **deltas** necesarios (design-delta → plan-delta → feature(s)) en una sola corrida — **gate único de pipeline** (la ruta propuesta con el resumen de cada delta y qué habrá **visible** al final → una autorización global), **hijo fresco por unidad tipada** con su loop de review completo, cada unidad aprobada queda «APPROVED — pendiente OK de pipeline», y el **OK del RECAP consolidado cierra todo**. Pedido monofase → despacho directo a la skill de la fase: `/build` no cuenta como autorización (la autorización es siempre de un gate) ni puentea estado pendiente. **POC-first como caso particular** (proyecto virgen): DESIGN mínimo marcado «borrador (modo POC)», plan que nace con feature 01 = esqueleto que camina, POC como resultado visible, endurecimiento post-OK recomendado y no forzado.

Lo fijado por el diseño (la bajada no lo rediscute): reuso de los **contratos del lote** generalizando la unidad de «feature» a unidad tipada — padre orquestador liviano que supervisa la señal terminal y empuja sin transportar feedback; ledger versionado por corrida con reentrada fail-closed idéntica; condiciones de corte del lote más la **divergencia** entre el resumen autorizado y el re-derivado al arrancar cada unidad; cobertura pre/post-OK de commits idéntica a la del lote; sin lote anidado (los features del pipeline son unidades hermanas — un padre, un ledger); reviews por unidad con loop completo y **criterio acotado al delta declarado** (modo borrador incluido); la **regla dura extendida** («nunca continuar a otro feature *ni cruzar a otra fase* sin OK humano — salvo dentro de un lote o pipeline autorizado») reflejada en `AGENTS.md` + `templates/AGENTS.md` (regla de sincronía); y el riesgo aceptado con su registro honesto (propagación hasta el sufijo desde la unidad errada; en virgen, potencialmente el pipeline completo con costo hundido acotado).

Decisiones para la bajada (lista del diseño): ledger propio vs. generalización del ledger del lote (los contratos ya están unificados; el código a decidir); relación de implementación entre `/feature all` y la etapa multi-feature del pipeline; forma del marker «borrador (modo POC)» y de la pasada que lo cierra; overrides de modelo por tipo de unidad en el subagente; validación del pedido de `/build` (vacío, que no toca ninguna fase, contradictorio con STATUS). Superficie estimada (completada en r1 de este ciclo): skill `build` (nueva), skill `feature` (reuso del contrato padre↔hijo), skills `design` y `plan` (**semántica de hijo de pipeline**: hoy ambas cierran con RECAP/OK individual y `/design` exige ping-pong — como unidad de pipeline terminan con la línea de estado del contrato padre↔hijo, y en POC el ping-pong se comprime en el gate), skill `recap` (RECAP consolidado de pipeline — mismo mecanismo del lote, base = gate del pipeline en el ledger), `AGENTS.md` + `templates/AGENTS.md` (regla dura, `/build` en fases y ruteo — reemplaza el interinato multifase del 09), plantilla del ledger de pipeline, `scripts/install.sh` + `tests/install.sh` (la allowlist explícita del payload debe incorporar la skill `build`, con su cobertura — sin ella la entrada implícita completa no viaja a los destinos), `docs/design/review-contract.md` (generalizar el contrato de la identidad de invocación `AXEL_REVIEW_ID`, hoy declarada exclusiva del lote — el pipeline también la usa; sin cambio de lógica en `review.sh`), y DESIGN.md ya al día (componentes y flujo se tocaron en el ciclo de diseño).

### 11 — Modelo fijo de los subagentes por tipo de unidad

Pedido humano (2026-07-29, gate del pipeline `/build` — literal en la extensión de arriba): la maquinaria fija el modelo con el que se lanza cada subagente según el tipo de unidad — **Fable 5** para los hijos que corren las skills `design` y `plan` (unidades `design-delta` y `plan-delta` de un pipeline), **Opus 5** para los hijos que corren `feature` (tanto los del modo lote de `/feature all` / `NN..MM` como las unidades `feature` de un pipeline). Hoy todos los hijos heredan el modelo de la sesión padre. Posición: único ítem del backlog nuevo (vacío tras el cierre del lote 08–10), fijada por pedido humano (criterio 1).

**Qué revierte y por qué no toca diseño**: el diseño ya habilita el esquema por tipo de unidad ([design/implicit-entry.md](design/implicit-entry.md) §«Modelos por unidad»: el padre *puede* aplicar el override — detalle en la bajada); lo que se revierte es una decisión **de bajada** — §10 de [implementation/10-build-pipeline.md](implementation/10-build-pipeline.md), «no se cablean» (hoy: el padre lanza con el modelo de la sesión, y otro modelo se pide por gate como ajuste de alcance). La reversión queda **registrada, no borrada**: la §10 conserva su texto con una nota que apunta a este feature, porque su razón de fondo —cablear modelos en una skill que es payload exporta las preferencias de axel a los destinos y se desactualiza en silencio— no desaparece: se convierte en pregunta que la bajada debe responder. La fila de decisión en `DESIGN.md` la agrega el feature, como hicieron el 05 y el 06.

**Distinción que el feature instala**: el modelo de la **sesión** sigue siendo elección del humano (`AGENTS.md` §Roles); el modelo de los **hijos** pasa a estar fijado por la maquinaria según el tipo de unidad. `AGENTS.md` y `templates/AGENTS.md` deben distinguir ambos planos.

Superficie estimada — la bajada decide el detalle: skill `build` (§«Modelos por unidad» + el bullet de spawn «Lanzá el hijo»), skill `feature` (el spawn del hijo de lote y las sustituciones del modo hijo de pipeline), `AGENTS.md` §Roles + `templates/AGENTS.md` (regla de sincronía), [design/implicit-entry.md](design/implicit-entry.md) (§«Modelos por unidad» y la lista de decisiones para la bajada, que esta decisión desactualiza), §10 de [implementation/10-build-pipeline.md](implementation/10-build-pipeline.md) (la nota de reversión) y la fila de decisión en `DESIGN.md`.

Preguntas para la bajada: **cómo se nombra el modelo al spawnear** — el identificador del harness (p. ej. `opus`), no el ID de API, y qué pasa si el harness lo renombra o no lo reconoce; **qué hace la skill en un proyecto destino que no tiene esos modelos disponibles** — default de axel documentado y editable vs. degradación silenciosa: es exactamente la razón por la que la §10 había decidido no cablearlos, y la bajada debe responderla explícitamente; y **si el humano conserva el override puntual por gate** (hoy: ajuste de alcance registrado en el ledger que el padre aplica al spawnear).

Verificación prevista (fijada por la ruta autorizada): como en los features 08–10 — no hay harness de despacho; la evidencia es una matriz de casos resuelta **contra el texto final instalado**.

### 12 — Reporte de cierre de `/adopt`: inventario de archivos tocados

Pedido humano (2026-07-29, gate del segundo pipeline `/build` del día — literal en la extensión de arriba): que la skill `/adopt` cierre reportándole al humano el **inventario completo de archivos que tocó**, no solo la narrativa de las decisiones que le parecieron importantes. Posición: único ítem del backlog nuevo (vacío tras el cierre del feature 11), fijada por pedido humano (criterio 1).

**El hueco**: `.claude/skills/adopt/SKILL.md` tiene seis pasos y **ninguno** pide un reporte al humano. El paso 6 dice «Cerrar: borrá `docs/ADOPTION.md`, actualizá `docs/STATUS.md` y commiteá», y la regla de cierre remite los movimientos de archivos al diff del commit — pero el humano no mira el diff: lee el mensaje con que la sesión termina el turno. Sin un paso que lo exija, cada corrida improvisa qué contar.

**La evidencia** — primera adopción real de axel fuera de sí mismo (`/Users/alexweil/src/inquirylab`, commit `4908bfb`): la **ejecución** fue correcta y quedó auditada de punta a punta (cero pérdida de contenido en la mudanza de docs, 55 links relativos y 0 rotos, `ADOPTION.md` consumido, cuatro docs canónicos y symlink OK, `settings.json` cumpliendo `axel-policy.json`, árbol limpio, STATUS derivado del estado real y no plantilla); **el problema fue el reporte**. El commit tocó **8 archivos** —`AGENTS.md`, `DESIGN.md` (borrado de la raíz), `README.md`, `docs/ADOPTION.md` (consumido), `docs/DESIGN.md`, `docs/IMPLEMENTATION.md`, `docs/STATUS.md` y `docs/implementation/bitacora.md` (rename)— y el reporte habló de **tres**, cerrando con «Dos cosas que decidí yo y conviene que mires». Dos omisiones concretas: (1) **`README.md` no se mencionó en absoluto** — el agente reescribió los links a los docs movidos, agregó punteros a STATUS y AGENTS y editó una fila de la tabla de mapa del proyecto; el cambio era correcto y necesario (sin él quedaban links rotos), pero el humano aprobó a ciegas una edición a su README; (2) **`AGENTS.md` se subdeclaró** — el reporte dijo «agregué un bloque *Disciplina propia del proyecto*» cuando en realidad había redactado la sección «Sobre este proyecto» entera (qué es el proyecto, qué se está construyendo, objetivo, alcance y fronteras): interpretación sustantiva de un proyecto ajeno, presentada como un bloque secundario. Con `git show --stat` a la vista se ven 8 archivos; el humano escuchó 3.

**Qué se busca**: que el cierre de `/adopt` sea auditable **de un vistazo**, sin que el humano tenga que ir a correr `git show --stat` para descubrir qué se editó de su repo. Como mínimo, el inventario completo de archivos tocados con **qué se le hizo a cada uno** (movido / fusionado / creado / borrado / editado), separando de forma **visible** lo **mecánico** (lo que el handoff mandaba) de lo **interpretativo** (texto que el agente escribió sobre el proyecto del humano y que conviene que ratifique). La forma exacta la decide la bajada.

**Dos criterios duros** que el pedido fija y la bajada no rediscute: (a) el inventario **no depende de que el agente se acuerde de contar** — sale de lo que el **commit tocó**, no de su memoria de lo que hizo; (b) **no infla el cierre hasta volverlo ilegible** — en la adopción de inquirylab habrían sido 8 líneas.

**Alcance: acotado a `/adopt`** (decisión abierta resuelta en el gate, 2026-07-29). El mismo hueco podría existir en las otras skills que cierran con un mensaje al humano (`recap`, `feature`, `design`, `plan`, `build`), pero `/adopt` es la única que edita docs **preexistentes de un proyecto ajeno** en una sesión donde el humano todavía no conoce la convención — el humano confirmó acotar. Con la **condición registrada**: el mecanismo debe quedar escrito de forma **reusable**, de modo que generalizarlo más adelante sea adoptar un contrato ya escrito y no reinventarlo. Extenderlo ahora a las otras skills es divergencia.

Superficie estimada — la bajada decide el detalle: `.claude/skills/adopt/SKILL.md` (el paso de reporte; es **maquinaria/payload**, así que el instalador la sobreescribe en los destinos y el cambio se propaga al re-correrlo, y **no** tiene copia en `templates/` — ahí solo viven `AGENTS.md`, `DESIGN.md`, `IMPLEMENTATION.md`, `STATUS.md` y `settings.json`), más la decisión **explícita** —el pedido pide decidirla, no asumirla— de si `AGENTS.md` + `templates/AGENTS.md` acompañan por la regla de sincronía: si esto es comportamiento de una sola skill probablemente no aplique, pero la bajada lo argumenta en vez de darlo por hecho.

Preguntas para la bajada: **forma exacta del inventario** (qué se lista por archivo, dónde va la separación mecánico/interpretativo, cómo se mantiene en el orden de las 8 líneas que el criterio (b) exige); **cómo se deriva del commit** — el reporte se produce después de commitear, con qué comando, y qué pasa si el cierre necesitó más de un commit o si quedó algo sin commitear; y **cómo se clasifica «interpretativo» de forma reproducible** — el criterio no puede depender del juicio libre de cada corrida, porque es exactamente lo que el criterio (a) descarta: hace falta una regla que se pueda aplicar mirando el diff (candidato obvio: prosa que el agente redactó sobre el proyecto ajeno — la sección «Sobre este proyecto» de `AGENTS.md`, el STATUS derivado — frente a lo que el handoff mandaba mecánicamente).

Verificación prevista (fijada por la ruta autorizada): como en los features 08–11 — no hay harness de despacho; la evidencia es una **matriz de casos resuelta contra el texto final instalado**, incluido el caso de inquirylab como **caso de prueba retrospectivo**: ¿el texto nuevo habría producido las 8 líneas, con `README.md` y la sección de `AGENTS.md` visibles? Es retrospectivo **e hipotético**, y la bajada no debe confundirse: re-correr el instalador sobre inquirylab **no** reabre su adopción ya cerrada —el modo update propaga la skill nueva pero `docs/ADOPTION.md` ya fue consumido y `/adopt` informaría que no hay adopción pendiente, comportamiento fijado por `tests/install.sh` T5—, así que el inventario recién se ve en la **próxima adopción real**. (Promesa corregida en la r1 de esta unidad; la versión original y su corrección quedan registradas en el bloque Gate del ledger.)

### 13 — Vidriera: LICENSE MIT + README en inglés + `docs/install.md`

Pedido humano (2026-07-29, gate del **tercer** pipeline `/build` del día — literal en la extensión de arriba): dejar el repo público presentable para quien lo descubre. Posición: **primero** del bloque nuevo 13–14 (backlog vacío tras el cierre del feature 12), fijada por pedido humano (criterio 1). El orden interno lo fija el mismo pedido, y lo acompañan dos razones de los agentes: (a) **el embudo** — el README es lo primero que un colega lee y lo que decide si sigue; `CONTRIBUTING.md` y `.github/` le sirven recién a quien ya decidió engancharse, así que invertirlos entregaría los formularios de feedback antes que la razón para dar feedback; (b) **valor desbloqueado antes que pulido** (criterio 2) — la vidriera es lo único que hoy bloquea compartir el repo con un colega, que es la urgencia declarada del pedido.

**Estado verificado hoy** (base `31471be`, derivado del repo y no de memoria): **no existen** `LICENSE`, `CONTRIBUTING.md`, `docs/install.md` ni `.github/`. `README.md` tiene **69 líneas** en cuatro tramos — título y tres links (1–7), «Uso» (9–18), «Llevar axel a otro proyecto» (20–50) y «Para agentes (Claude Code)» (52–69): los dos últimos suman **49 líneas de casuística** —**50** si se cuenta la línea en blanco 51 que los separa, o sea el intervalo 20–69 completo—, y es exactamente lo que se muda al manual.

**Lo que el diseño fija y la bajada no rediscute** ([design/public-surface.md](design/public-surface.md)): el **corte vidriera/manual** como test aplicable línea a línea (al README solo entra lo que vale para todos; toda frase que empieza con «si…» o «salvo que…» describe un caso y los casos van al manual, que es el completo **en el dominio operativo**; el README lleva punteros, nunca casos; los requisitos van **antes** del comando de instalación); **inglés** para el README y para `docs/install.md`, que es la última milla del mismo embudo; el **contrato editorial** —toda afirmación publicada es hecho derivable con su comando, limitación declarada u opinión marcada, y lo que no cae en las tres no se publica— con **fuente única** para todo número; el **transcript en dos bloques** con procedencia propia y **ninguna línea inventada**; **MIT**, con titular y año a confirmar en la bajada; la **limitación del aviso MIT que no viaja con el payload**, que se declara en el manual como **incumplimiento pendiente** y no como cumplimiento parcial; y el mapa de los tres problemas conocidos — el de `build/` con su workaround `!.claude/skills/build/` y la trampa de `!.claude/` explicada, en «Known issues» de `docs/install.md` **más un puntero de una línea desde el README** por ser el único que bloquea la promesa; los otros dos, solo en el manual.

**Tres hallazgos de este delta que la bajada tiene que resolver**, y que no estaban anotados en ningún doc:

1. **El README de hoy omite dos de los comandos.** La sección «Uso» lista cinco (`/status`, `/feature`, `/design`, `/plan`, `/recap`) y **no menciona `/build` ni `/adopt`**: la vidriera actual no nombra el pipeline —que desde el feature 10 es la entrada principal del método— ni la adopción. La tabla de comandos del README nuevo los cubre; queda por decidir si `/adopt` entra en la tabla o como línea aparte, porque `DESIGN.md` lo trata como componente separado de los seis.
2. **La mudanza no rompe punteros de la maquinaria, pero sí deja sin destino un camino de uso.** Verificado: ni las skills, ni `scripts/install.sh`, ni `AGENTS.md` referencian el README o sus secciones (las apariciones en `tests/` lo usan como archivo dummy). Pero el feature 02 diseñó el camino «instalá axel siguiendo \<url\>», y esa URL cae en el README: si «Para agentes (Claude Code)» se muda entera sin dejar rastro, ese camino aterriza en una página que ya no tiene el procedimiento. El README debe conservar un **puntero de una línea** para agentes — que es justo lo que el corte del diseño permite (puntero sí, caso no).
3. **Las dos dependencias con el 14**, que este delta cierra abajo para que la bajada no las re-decida ni las descubra tarde.

**Contrato entre el 13 y el 14 — las dos referencias hacia adelante.** El README que crea el 13 apunta a **dos artefactos que nacen recién en el 14**, y son dos dependencias distintas, no una: (a) el diseño manda **fuente única** para los números —toda cifra de la vidriera vive en el doc de métricas y el README la **cita**—, y (b) la estructura aprobada del README cierra con links, entre ellos **cómo dar feedback**, que es `CONTRIBUTING.md`. Se cierran con un **estado intermedio verificable**, sin re-cortar los features:

- **El 13 no deja links rotos: deja referencias no activas y explícitamente pendientes.** El README nombra en prosa el doc de métricas y la vía de feedback y **no los linkea**; cada cifra publicada va con su commit de corte y con la marca visible de que su artefacto auditable llega en el 14. La bajada del 13 fija la **forma** de esa marca —tiene que verse, no ser una nota al pie—, no los paths.
- **La elección de los paths se queda en el 14**, donde el diseño la puso: este delta ya no la mueve. El 14 elige el path del informe y el del snapshot manteniendo la distinción que el diseño le encarga (informe = prosa de vidriera en inglés; snapshot = dato neutro de idioma), crea `CONTRIBUTING.md`, el informe y el snapshot, y **activa las dos referencias del README**, convirtiéndolas en links.
- **Que el 14 edite `README.md` está dentro de la ruta autorizada**, y conviene dejarlo dicho porque es el punto donde una lectura estrecha vería divergencia: no parte ni fusiona los features, no agrega ninguno de los que el pedido excluyó, y el resultado visible que el gate prometió trata a los dos como **una sola entrega**. Es la activación de dos referencias que el 13 dejó pendientes **a propósito**, no una reapertura del 13. Si en la bajada esa edición resultara excluida, la salida correcta es **registrar divergencia y cortar**, no mover en silencio una decisión del diseño.
- **Un solo corte, declarado por el 13 y reconstruido por el 14.** Sin esto la coincidencia entre el README y el doc de métricas se rompe sola: el 13 publica cifras con un corte, y después sus propias rondas de review y las del 14 agregan rondas nuevas al `rounds-log`. De las dos salidas posibles, este delta fija la **más chica**: el **13 declara el commit de corte** —uno que ya exista cuando escribe, no el suyo, que todavía no está— y el **14 reconstruye exactamente ese corte** filtrando el log por él, en vez de sacar una foto nueva. **Consecuencia declarada**, propia de cualquier foto y no un defecto: el snapshot publicado **no incluye las rondas de los features 13 y 14**, y lo dice. La otra salida —que el 14 re-corte y actualice las cifras— se descarta porque ampliaría su edición del README de «activar dos links» a «rehacer toda cifra publicada», que es exactamente la superficie que este corte entre features busca no mover.
- **Los dos estados se verifican por separado**, que es lo que vuelve honesto el corte. Al cerrar el **13**: todo link del README **resuelve**, y toda referencia a un artefacto del 14 está **sin linkear y marcada como pendiente** — cero links rotos, literalmente, sin excepción que negociar. Al cerrar el **14**: las dos referencias son links que resuelven, el corte del snapshot es **el mismo** que declaró el 13, ninguna cifra de la vidriera vive fuera del doc de métricas, y el chequeo de links corre sobre el conjunto completo.

Con esto no queda ventana con el repo inconsistente: el estado intermedio es un README **completo y coherente** que declara dos pendientes, no un README roto esperando el feature siguiente.

Superficie estimada — la bajada decide el detalle: `LICENSE` (nuevo), `README.md` (reescrito de punta a punta, en inglés) y `docs/install.md` (nuevo, en inglés, con la casuística completa de las líneas 20–69 del README actual más las secciones de problemas conocidos y de adopción). **Fuera de la superficie por alcance**: el método, las skills, el instalador, los scripts y toda acción sobre GitHub.

Preguntas para la bajada: **titular y año** del copyright de MIT — no se inventan: los confirma el humano o se derivan del repo; **cómo se verifica «cero pérdida» cuando la mudanza es a la vez traducción** — el diseño ya advierte que un diff de texto no sirve y pide chequeo por contenido caso por caso, así que falta la forma concreta (candidato: inventario de los casos del README actual, cada uno con su destino en el manual); **qué corrida concreta se renderiza en el transcript** — el diseño fija los cinco hitos y sus fuentes durables y sugiere un pipeline como fuente natural, pero cuál y con qué extensión lo decide la bajada, con la salida ya declarada de **cambiar la promesa del título** si ninguna tiene el arco completo; y **cómo se prueban el one-liner y el workaround** sin ensuciar el repo, ya que el criterio (c) de abajo pide probarlos y no transcribirlos.

Verificación prevista (fijada por la ruta autorizada): no hay harness que compruebe prosa. La evidencia es (a) **cero afirmación no verificable** — cada cifra, cada nombre de archivo y cada comando del README chequeado contra el repo, con las métricas re-derivadas al commit de corte, y cada oración clasificable en una de las tres clases del contrato editorial; (b) **cero pérdida y cero link roto** en la mudanza a `docs/install.md` — la casuística actual entra completa, **todo link del README resuelve**, y las dos referencias a artefactos que nacen en el 14 quedan **sin linkear y marcadas como pendientes**, según el contrato de arriba; (c) el one-liner y el workaround de `build/` **probados**, no transcritos de memoria.

### 14 — Onboarding y feedback: CONTRIBUTING + `.github/` + métricas versionadas

Pedido humano (2026-07-29, mismo gate — literal en la extensión de arriba): los docs de onboarding y feedback que faltan, más la evidencia del loop en un artefacto que un tercero pueda auditar. Posición: **segundo** del bloque 13–14, por el embudo del criterio de arriba.

Sustancia:

- **`CONTRIBUTING.md`** en inglés, orientado a **qué feedback busca esta ronda**: colegas probándolo, no PRs de código — cómo reportar que el instalador rebotó, qué información sirve para diagnosticarlo, y qué está explícitamente fuera de alcance hoy (los tres puntos de fricción ya documentados como problemas conocidos, y el aviso MIT que todavía no viaja con el payload).
- **`.github/ISSUE_TEMPLATE/`** con las plantillas que hagan que ese feedback llegue accionable — candidatos del pedido: fallo de instalación, fricción de uso, pregunta.
- **El doc de métricas del reviewer**, que el diseño parte en **dos piezas de idioma distinto**: el **informe** (prosa, vidriera ⇒ inglés) y el **snapshot crudo** del `rounds-log` (dato —marcas de tiempo, veredictos y SHA— ⇒ neutro de idioma; vivir bajo `docs/` no lo vuelve un doc del método). Con **commit de corte** declarado, **comando de derivación** que corra con el toolchain de stock de macOS —verificado en el diseño: el `awk` del sistema **no** tiene `asort`, así que el orden se resuelve con `sort`—, el **contra-chequeo independiente** contra los SHA de una historia verificablemente lineal, y el ejemplo citable de una review que atrapó algo real.
- **La activación de las dos referencias pendientes del README** (contrato del 13, arriba): los links al informe de métricas y a `CONTRIBUTING.md`, más el chequeo final de links y de fuente única sobre el conjunto completo.
- **Los comandos exactos de GitHub** —push, topics y homepage— **dejados listos para que los corra el humano**, y nunca ejecutados desde acá: el ajuste (b) del gate saca la acción del alcance del pipeline y correrla es divergencia ⇒ corte. **«Exactos» se conserva tal como lo autorizó el gate** («la unidad `14` deja los comandos exactos listos y los corre el humano al leer el resultado»), y fija una vara concreta: los tres tienen que poder **pegarse en una terminal sin editarlos**, con herramienta, sintaxis y precondiciones declaradas, y **cero huecos**. El push no depende de nada pendiente. Para **topics y homepage**, cuyo contenido el diseño dejó sin decidir, lo que cierra la vara es que la **bajada del 14 proponga valores concretos y argumentados**, derivados de lo que el repo ya es, de modo que el comando quede **completo**; el humano conserva la decisión sin bloquear a la unidad, porque es **él quien lo corre** y puede cambiar cualquier valor antes de hacerlo — proponer no es decidir. Quedan **descartadas dos salidas**: entregar un template con huecos y llamarlo comando exacto, y **degradar la promesa** del gate a «mecanismo documentado» porque conseguir los valores resulte incómodo — la promesa está autorizada y este delta no tiene autoridad para reescribirla. Si la bajada no pudiera proponer valores defendibles, eso es **fuentes insuficientes** y sigue el camino de corte del pipeline.

**Lo que el diseño fija y la bajada no rediscute**: las **tres unidades que no se mezclan** —ronda (**80** registradas al corte `e1e1282`), hito (**27**), ciclo (**16** completos observables más uno parcial)—; que los `APPROVED` son **hitos, no features**, por lo que hay más aprobados que features y enunciarlo mal es lo que destruye la credibilidad; el **criterio de conteo** —la ronda es la del contrato y no la fila del log: se deduplica por (ciclo, número de ronda), porque un reintento deja `PROC_FAIL` antes del veredicto y un veredicto inválido deja `NO_VERDICT`, y los eventos pre-invocación no son rondas—; y los **tres hallazgos** que el doc tiene que incorporar porque fijan el criterio y no solo el número:

1. «Cero aprobados en ronda 1» es **cierto por ciclo y falso por hito** (hay exactamente uno, `2dbbdfc`); la mediana es **4,5 por ciclo** y **3 por hito**, y el peor caso **11 por ciclo** y **5 por hito**. La formulación defendible **nombra la unidad**.
2. El log **arranca el día 2** y el faltante es recuperable de **dos fuentes que se citan por separado** — **30** rondas en los review logs versionados de features y **5** del ciclo de plan inicial, cuya memoria son los commits y el STATUS histórico ⇒ **115** históricas contra **80** registradas, publicadas como dos cifras rotuladas y nunca como una sola sin decir a cuál corresponde. El «cero en ronda 1» se demuestra con el veredicto de **cada primera ronda**, no con la ronda de cierre del ciclo.
3. Las cifras **se movieron durante la review del propio delta de diseño**: la demostración en vivo de por qué la evidencia se publica como foto fechada con su commit de corte.

**Contrato con el 13** (detallado en su sección): el 13 deja el README **completo y sin links rotos**, con dos referencias sin activar y marcadas como pendientes; el 14 **elige los paths** —la decisión que el diseño le asignó—, crea los artefactos **reconstruyendo el corte que el 13 declaró** (no saca foto nueva) y **activa las dos referencias**. Es el 14, y no el 13, el que cierra el estado.

Superficie estimada — la bajada decide el detalle: `CONTRIBUTING.md` (nuevo, inglés), `.github/ISSUE_TEMPLATE/` (nuevo), el informe de métricas y el snapshot (nuevos, en los paths que elige este feature), y **`README.md`** (edición acotada: activar las dos referencias pendientes; no se reabre el resto de su prosa). Lo que **no** se toca: `.claude/state/` no se desversiona, porque el loop escribe ahí en cada ronda y versionarlo metería su propio ruido en todos los commits (descartado explícitamente por el diseño).

Preguntas para la bajada: **cuántas plantillas y cuáles** —hay tres candidatos, pero de más plantillas se llenan menos, así que se decide contra el objetivo declarado: que el reporte de un colega llegue accionable—; **cómo se reconstruye mecánicamente el corte que declaró el 13** — el *cuándo* ya no es pregunta: el contrato de arriba lo fija en el commit que el 13 declaró, así que lo que queda es el **cómo** (filtrar el `rounds-log` por ese commit) y qué se hace con las filas que no llevan SHA, que son los eventos pre-invocación y no son rondas; **si el snapshot es copia literal del `rounds-log` o una proyección** — la copia literal es lo más auditable pero arrastra un formato interno que no es contrato público; **cómo se citan las 35 rondas previas a la instrumentación**, que no tienen esquema tabular y por lo tanto no se derivan con el mismo comando que las 80; y **qué valores concretos se proponen para topics y homepage, y con qué argumento** — el diseño los dejó sin decidir y la bajada los propone **completos**, para que el comando sea pegable sin editar (arriba); más la herramienta y las precondiciones con que se corren los tres: repo ya pusheado, herramienta instalada y autenticada.

Verificación prevista (fijada por la ruta autorizada): la misma vara de prosa auditable del 13 —contrato editorial aplicado por oración—, más tres chequeos propios: que las cifras del doc de métricas **se re-deriven** en la bajada con el comando declarado, sobre el snapshot versionado, **al mismo corte que declaró el 13**, y **coincidan** con lo que el README afirma —si no coinciden, el que está mal es el snapshot y no el README, porque el corte es uno solo—; que al cerrar el feature **las dos referencias del 13 sean links que resuelven** y ninguna cifra de la vidriera viva fuera del doc de métricas (chequeo de links y de fuente única sobre el conjunto completo); y que **los tres comandos de GitHub** —push, topics y homepage— estén escritos de forma **pegable sin editar**, con herramienta, sintaxis y precondiciones declaradas y **cero huecos**, cumpliendo el «exactos» que autorizó el gate y verificados **sin correrlos** contra el remoto.
