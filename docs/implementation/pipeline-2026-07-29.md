# Pipeline 2026-07-29 — `/build`

## Gate
- tipo: `pipeline`
- protocolo: `spawn-token v1`
- Pedido del humano: «fijar el modelo de los subagentes por tipo de unidad: los hijos que corren las skills design y plan (unidades design-delta y plan-delta de un pipeline) con Fable 5, y los hijos que corren feature (tanto los del modo lote de /feature all como las unidades feature de un pipeline) con Opus 5. Hoy todos heredan el modelo de la sesión padre, y la skill build tiene un párrafo «Modelos por unidad» que documenta esa decisión y habría que reescribir. El backlog del plan está vacío, así que el pedido necesita entrada de plan + implementación.»
- Modo: `normal`
- Autorizado: 2026-07-29 — «dale, autorizado» · Ajustes de alcance: ninguno al autorizar. **Ajuste posterior (2026-07-29, «dale, autorizado» tras cambiar el modelo de la sesión)**: los hijos que resten de la corrida se lanzan con **Opus 5**. Motivo operativo, no de método: Fable 5 se quedó sin créditos a mitad de la unidad `plan` y mató al hijo; el humano pasó la sesión a Opus 5 y autorizó continuar.
- Resultado visible al final: las skills `build` y `feature` lanzando cada subagente con el modelo fijado por tipo de unidad (Fable 5 para hijos de `design`/`plan`, Opus 5 para hijos de `feature` — lote y pipeline), con `AGENTS.md` + `templates/AGENTS.md` distinguiendo el modelo de la sesión (elección del humano) del modelo de los hijos (fijado por la maquinaria), y los docs de diseño y de bajada al día, incluida la reversión registrada de la §10 del feature 10.
- gate_base: `51ec00b`

## Ruta derivada (propuesta)

Dos unidades. **No** se propone `design-delta`: el diseño ya habilita el override por tipo de unidad ([design/implicit-entry.md](../design/implicit-entry.md) §«Modelos por unidad»: «el padre **puede** aplicar el esquema por tipo de unidad … Detalle en la bajada») y lo que se revierte es una decisión **de bajada** — §10 de [implementation/10-build-pipeline.md](10-build-pipeline.md), «no se cablean». La fila de decisión en `DESIGN.md` la agrega el feature, como hicieron el 05 y el 06.

### plan — plan-delta — Backlog: modelo fijo de subagentes por tipo de unidad

Agregar al backlog un **único** feature (previsiblemente el 11) con la posición fijada por pedido humano (criterio 1 del orden), su superficie estimada y las preguntas que quedan para la bajada. Superficie estimada a registrar: skill `build` (§«Modelos por unidad» + el bullet de spawn), skill `feature` (spawn del hijo de lote y las sustituciones del modo hijo de pipeline), `AGENTS.md` §Roles + `templates/AGENTS.md` (regla de sincronía), `docs/design/implicit-entry.md` (§«Modelos por unidad» y la lista de decisiones para la bajada, hoy desactualizadas), §10 de `docs/implementation/10-build-pipeline.md` (reversión registrada, no borrada) y una fila de decisión en `DESIGN.md`. Preguntas para la bajada: cómo se nombra el modelo al spawnear (el identificador del harness, no el ID de API, y qué pasa si cambia); qué hace la skill cuando viaja a un proyecto destino que no tiene esos modelos disponibles (default de axel documentado y editable vs. degradación silenciosa) — es exactamente la razón por la que la §10 había decidido no cablearlos, y la bajada debe responderla; y si el humano conserva el override puntual por gate.

**Restricción de alcance**: el plan-delta debe entrar como **un** feature. Si concluyera que hace falta partirlo en más de uno, eso es divergencia contra la ruta autorizada ⇒ corte y RECAP.

### 11 — feature — Modelo fijo de los subagentes por tipo de unidad

Bajada fina + implementación de lo anterior: hijos de las unidades `design` y `plan` de un pipeline con **Fable 5**; hijos que corren `feature` con **Opus 5**, tanto los del modo lote de `/feature all` / `NN..MM` como las unidades `feature` de un pipeline. Reescritura del párrafo «Modelos por unidad» de la skill `build`, de los bullets de spawn de ambas skills, y de `AGENTS.md` + plantilla; actualización de los docs de diseño y de la §10 del 10. Verificación como en los features 08–10: no hay harness de despacho, así que la evidencia es una matriz de casos resuelta **contra el texto final instalado**.

## Estado
| Unidad | Tipo | Estado | SHA inicio | SHA APPROVED |
|---|---|---|---|---|
| plan | plan-delta | **Cerrada** | `2aaa9dc` | `e5f2f87` |
| 11 | feature | **Cerrada** | `8c11092` | `cdece9c` |

## Eventos
- 2026-07-29 — ledger creado con la ruta derivada; gate presentado al humano (`gate_base` `51ec00b`), a la espera de la autorización global.
- 2026-07-29 — **gate autorizado** («dale, autorizado»), sin ajustes de alcance. La ruta autorizada es la derivada: plan-delta + feature 11.
- 2026-07-29 — unidad `plan`: pre-arranque OK (los docs no cambiaron desde el gate — el resumen re-derivado coincide con el autorizado; fuentes suficientes), ventana de awake renovada, token acuñado y **hijo lanzado desde el commit de arranque** (este) con `token=E232C306-1FD5-47D9-B987-4A7AC157C7FE`. El SHA del commit de arranque se asienta en la fila al cierre de la unidad (no es conocible antes de commitearlo).
- 2026-07-29 — unidad `plan`: **hijo caído y relanzado**. Codex dio `APPROVED` en la r3 (base `2c64c8a`, head `e6d26e2`) y el hijo alcanzó a consumir el desenlace (`pipeline-expected` en `phase=consumed`, `last-verdict` con el APPROVED), pero murió por falta de créditos de Fable 5 **antes** del commit de cierre de la unidad. Reentrada resuelta por la máquina, sin adivinar: ledger sin corte pendiente; `phase=consumed` ⇒ no hay review en vuelo; ancla activa única `claimed-E232C306…` + evento vigente con ese token + `pipeline-expected` legible y de esta unidad ⇒ **retirar y relanzar con el orden durable completo**. Token `E232C306…` retirado (`retired-*`), token nuevo `E60A66FB-B863-40F2-A72D-1BACE68C7A1F` acuñado. **El hijo nuevo va con Opus 5** (ajuste de alcance registrado arriba). Árbol limpio al momento de la reentrada: el hijo no dejó nada sin commitear. El token de ronda de STATUS no lo mueve el padre — lo resuelve el hijo al cerrar.
- 2026-07-29 — unidad `plan`: **APPROVED de cierre en r3** (base `2c64c8a`, head revisado `e6d26e2`, cierre en `e5f2f87`). Tres rondas, dos con cambios pedidos, ambos de **forma** y aceptados: la §11 quedaba insertada dentro del cuerpo del feature 10 (movida completa al final del doc, texto idéntico, r2) y la línea «Esperando» de STATUS no usaba el vocabulario fijo del contrato (r3). La **sustancia** la acordó Codex desde r1: un único feature (el 11), posición por criterio 1, reversión registrada —no borrada— de la §10 del feature 10, superficie estimada y las tres preguntas para la bajada. Verificación del padre: árbol limpio, `pipeline-expected` y anclas de token activas ausentes (queda solo el `retired-E232C306…` como historia), autorización del gate registrada en `IMPLEMENTATION.md` con fecha, pedido literal y path de este ledger, STATUS al día y los cinco commits de la unidad presentes.

- 2026-07-29 — unidad `11` (feature): **pre-arranque OK** — el resumen re-derivado de la §11 recién escrita por el plan-delta coincide **en sustancia** con el autorizado en la ruta (mismo alcance: Fable 5 para hijos de `design`/`plan`, Opus 5 para hijos de `feature` en lote y pipeline; un único feature; mismas superficies; la reversión de la §10 del 10 registrada y no borrada), y las fuentes alcanzan. Ventana de awake renovada. Token `87E71BFA-99AB-4100-85BB-9E19E37A5310` acuñado; **hijo lanzado con Opus 5** (ajuste de alcance del gate), desde el commit de arranque (este).

- 2026-07-29 — unidad `11`: **APPROVED de cierre en r6** (base `2c64c8a`, head revisado `74d3f5c`, cierre en `cdece9c`), con el APPROVED de la **bajada en r4**. Seis rondas en dos ciclos, la sustancia acordada temprano en ambos y todos los pedidos aceptados sin argumentar (r1 tres puntos, r2 dos, r3 uno, r5 cuatro). El más valioso fue el encaje de la degradación con el orden durable de spawn: el argumento del hijo de «sin commit intermedio» era correcto sobre el estado de control pero perdía la **proveniencia** —un padre que cae tras el fallback dejaría un hijo degradado sin registro y el RECAP lo omitiría—, así que el rechazo y el modelo elegido se commitean **antes** del segundo spawn, con el mismo token y sin desplazar el evento vigente. Verificación del padre: árbol limpio, `pipeline-expected` y anclas de token activas ausentes, `IMPLEMENTATION.md` con la fila 11 en «APPROVED — pendiente OK de pipeline», autorización del gate registrada en el doc de la unidad (fecha, pedido literal, path de este ledger, más el ajuste de alcance a Opus 5), STATUS al día y los siete commits de la unidad presentes.
- 2026-07-29 — **fin del pipeline**: las dos unidades en «APPROVED — pendiente OK de pipeline». STATUS a «esperando OK humano» y RECAP consolidado sobre `gate_base` `51ec00b`.
- 2026-07-29 — nota operativa de la corrida (no es un defecto del trabajo): la diferencia entre los conteos de `tests/loop.sh` del reviewer (287) y del repo canónico (293) quedó explicada — el sandbox de Codex saltea la clase **L5**, que son exactamente esos seis casos. Útil para futuras comparaciones entre entornos.

## Cierre
- **OK humano del RECAP consolidado: 2026-07-29 — «dale, OK el recap».** Cierra el pipeline completo: las dos unidades pasan a **Cerrada** y el feature 11 a **Cerrado** en [IMPLEMENTATION.md](../IMPLEMENTATION.md). El OK cubre además los dos commits que el RECAP listó como no revisados por Codex (`cdece9c`, cierre de la unidad 11, y `1d2afc2`, fin de corrida), y este commit de registro se ampara en la excepción del contrato (transcripción mecánica cubierta por el OK que registra).
- **Resultado visible entregado** (lo que el gate prometió): la maquinaria fija el modelo de los subagentes por tipo de unidad — `fable` para los hijos que corren `design`/`plan`, `opus` para los que corren `feature` (modo lote y pipeline) —, con tabla canónica en la skill `build` §«Modelos por unidad», fila referenciada en el spawn del lote de `feature`, y `AGENTS.md` + plantilla distinguiendo el modelo de la sesión (elección del humano) del de los hijos (fijado por la maquinaria). Sin cambios ejecutables.
- **Pregunta del humano respondida antes del OK** (2026-07-29, queda registrada porque nombra un borde real): cómo overridear el default si se agota una familia de modelos. Tres caminos vigentes — degradación automática ante rechazo del harness al spawnear, override por gate (al autorizar o a mitad de corrida, registrado en el ledger y por eso resistente a la reentrada), y edición de la tabla canónica en axel (en un destino la pisa el re-run del instalador). **El borde**: el agotamiento con el hijo **ya vivo** —lo que pasó en esta corrida— no es un rechazo de spawn, así que no lo cubre la degradación automática; lo resuelve la reentrada más un override manual, porque el relanzamiento vuelve a leer la tabla. Es el riesgo **R6** de la bajada; automatizarlo sería trabajo nuevo, no un retoque.
- Backlog vacío tras el cierre: el paso siguiente es `/plan` para extenderlo, o `/build` para un pedido que cruce fases.

