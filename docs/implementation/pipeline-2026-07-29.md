# Pipeline 2026-07-29 — `/build`

## Gate
- tipo: `pipeline`
- protocolo: `spawn-token v1`
- Pedido del humano: «fijar el modelo de los subagentes por tipo de unidad: los hijos que corren las skills design y plan (unidades design-delta y plan-delta de un pipeline) con Fable 5, y los hijos que corren feature (tanto los del modo lote de /feature all como las unidades feature de un pipeline) con Opus 5. Hoy todos heredan el modelo de la sesión padre, y la skill build tiene un párrafo «Modelos por unidad» que documenta esa decisión y habría que reescribir. El backlog del plan está vacío, así que el pedido necesita entrada de plan + implementación.»
- Modo: `normal`
- Autorizado: 2026-07-29 — «dale, autorizado» · Ajustes de alcance: ninguno
- Resultado visible al final: las skills `build` y `feature` lanzando cada subagente con el modelo fijado por tipo de unidad (Fable 5 para hijos de `design`/`plan`, Opus 5 para hijos de `feature` — lote y pipeline), con `AGENTS.md` + `templates/AGENTS.md` distinguiendo el modelo de la sesión (elección del humano) del modelo de los hijos (fijado por la maquinaria), y los docs de diseño y de bajada al día, incluida la reversión registrada de la §10 del feature 10.
- gate_base: `51ec00b`

## Ruta derivada (propuesta)

Dos unidades. **No** se propone `design-delta`: el diseño ya habilita el override por tipo de unidad ([design/implicit-entry.md](../design/implicit-entry.md) §«Modelos por unidad»: «el padre **puede** aplicar el esquema por tipo de unidad … Detalle en la bajada») y lo que se revierte es una decisión **de bajada** — §10 de [implementation/10-build-pipeline.md](10-build-pipeline.md), «no se cablean». La fila de decisión en `DESIGN.md` la agrega el feature, como hicieron el 05 y el 06.

### plan — plan-delta — Backlog: modelo fijo de subagentes por tipo de unidad

Agregar al backlog un **único** feature (previsiblemente el 11) con la posición fijada por pedido humano (criterio 1 del orden), su superficie estimada y las preguntas que quedan para la bajada. Superficie estimada a registrar: skill `build` (§«Modelos por unidad» + el bullet de spawn), skill `feature` (spawn del hijo de lote y las sustituciones del modo hijo de pipeline), `AGENTS.md` §Roles + `templates/AGENTS.md` (regla de sincronía), `docs/design/implicit-entry.md` (§«Modelos por unidad» y la lista de decisiones para la bajada, hoy desactualizadas), §10 de `docs/implementation/10-build-pipeline.md` (reversión registrada, no borrada) y una fila de decisión en `DESIGN.md`. Preguntas para la bajada: cómo se nombra el modelo al spawnear (el identificador del harness, no el ID de API, y qué pasa si cambia); qué hace la skill cuando viaja a un proyecto destino que no tiene esos modelos disponibles (default de axel documentado y editable vs. degradación silenciosa) — es exactamente la razón por la que la §10 había decidido no cablearlos, y la bajada debe responderla; y si el humano conserva el override puntual por gate.

**Restricción de alcance**: el plan-delta debe entrar como **un** feature. Si concluyera que hace falta partirlo en más de uno, eso es divergencia contra la ruta autorizada ⇒ corte y RECAP.

### 11 — feature — Modelo fijo de los subagentes por tipo de unidad

Bajada fina + implementación de lo anterior: hijos de las unidades `design` y `plan` de un pipeline con **Fable 5**; hijos que corren `feature` con **Opus 5**, tanto los del modo lote de `/feature all` / `NN..MM` como las unidades `feature` de un pipeline. Reescritura del párrafo «Modelos por unidad» de la skill `build`, de los bullets de spawn de ambas skills, y de `AGENTS.md` + plantilla; actualización de los docs de diseño y de la §10 del 10. Verificación como en los features 08–10: no hay harness de despacho, así que la evidencia es una matriz de casos resuelta **contra el texto final instalado**.

## Estado
| Unidad | Tipo | Estado | SHA inicio | SHA APPROVED |
|---|---|---|---|---|
| plan | plan-delta | en curso | (commit de arranque; se asienta al cierre) | — |
| 11 | feature | pendiente | — | — |

## Eventos
- 2026-07-29 — ledger creado con la ruta derivada; gate presentado al humano (`gate_base` `51ec00b`), a la espera de la autorización global.
- 2026-07-29 — **gate autorizado** («dale, autorizado»), sin ajustes de alcance. La ruta autorizada es la derivada: plan-delta + feature 11.
- 2026-07-29 — unidad `plan`: pre-arranque OK (los docs no cambiaron desde el gate — el resumen re-derivado coincide con el autorizado; fuentes suficientes), ventana de awake renovada, token acuñado y **hijo lanzado desde el commit de arranque** (este) con `token=E232C306-1FD5-47D9-B987-4A7AC157C7FE`. El SHA del commit de arranque se asienta en la fila al cierre de la unidad (no es conocible antes de commitearlo).

## Cierre
- —
