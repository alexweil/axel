# 03 — Hardening del loop de review

## Alcance

Consolidar el código safety-critical de uso diario del loop (criterio 3 del plan: `review.sh` y `awake.sh` ejecutan `reset --hard`, `clean -fdx` y señales de procesos en cada ciclo). Cinco entregables, de IMPLEMENTATION §03:

1. **Suite de regresión reproducible** (`tests/loop.sh`): las siete clases de falla que el ciclo 00 encontró a mano y quedaron sin prueba automatizada, ejecutable **sin invocar a Codex** (doble del binario vía PATH) y sin tocar jamás el repo axel real.
2. **Captura robusta del session id**: anclada al evento `thread.started` del JSONL de Codex, en vez del primer UUID que aparezca en un archivo que mezcla stderr.
3. **Reintento automático ante fallas transitorias de Codex**: una vez, solo fallas de proceso, dentro de la misma ronda.
4. **Métricas de rondas**: registro local por corrida + resumen en `review.sh status`.
5. **shellcheck** instalado y corriendo como puerta (`tests/lint.sh`) sobre `scripts/` y `tests/` (el reviewer lo buscó en las 4 rondas del ciclo 00 sin encontrarlo).

Fuera de alcance: cambios al contrato de review más allá de documentar el retry (la semántica de veredicto, base y deadlock no se toca — la suite la **congela**); hardening del instalador (cubierto por su propia suite en 01/02); portabilidad de la suite fuera de macOS (mismo supuesto que `tests/install.sh`); pinning/verificación del binario codex.

## Enfoque técnico

### Orden: primero la red, después los cambios

- **Paso A** — `tests/loop.sh` congela el comportamiento **actual** de `review.sh` y `awake.sh` (las 7 clases). Recién con la suite en verde se toca código.
- **Paso B** — mejoras a `review.sh` (session id, retry, métricas), cada una con sus casos nuevos en la suite y los docs al día.
- **Paso C** — shellcheck: instalación, `tests/lint.sh` como puerta, y corrección/triage de hallazgos en `scripts/` y `tests/`.

La aceptación real contra Codex no necesita corrida aparte: **las rondas de review de este mismo ciclo corren con el `review.sh` modificado** — cada ronda del loop es el smoke real.

### La suite: `tests/loop.sh`

Mismo arnés y patrón que `tests/install.sh`: helpers `t/ok/ko/assert_*`, fixtures en `mktemp -d` con `trap` de limpieza, repos git temporales. `review.sh` resuelve el repo por `git rev-parse --show-toplevel` desde el cwd: la suite lo invoca **siempre con cwd dentro del fixture**, así todo su estado (`.claude/state/`) y sus operaciones destructivas (worktree, reset, clean) quedan confinados al fixture. El repo axel real nunca participa.

**Dobles por PATH** (un directorio de stubs antepuesto a `PATH`):

- `codex` — el doble central, con guion **secuencial por invocación**: un directorio de plan donde la invocación N toma su libreto (`N.rc`, `N.msg`, `N.events`, `N.hook` — exit code, contenido para el archivo del flag `-o`, JSONL a emitir por stdout, script a ejecutar "durante la review" para simular commits en el canónico u observar el worktree desde adentro); las variables escalares (`FAKE_CODEX_RC`/`MSG`/`EVENTS`/`HOOK`) quedan como caso degenerado de un solo intento. La secuencia es imprescindible para representar fallo→éxito en las regresiones del retry. Default de eventos: fixture tomado de una corrida real, con `thread.started`/`thread_id` y la línea de stderr `ERROR` que el archivo real mezcla. Registro de cada invocación (argv + stdin) en un log para los asserts (resume con el SID correcto, modelo/esfuerzo, contenido del prompt, **no**-invocación en deadlock).
- `caffeinate` — passthrough que saltea sus flags y ejecuta el comando restante (o duerme, si no hay comando): la suite no crea assertions de sueño reales y no depende del entorno.
- `pmset` — **dinámico, derivado de la vida real del pid trackeado**: en modo `track` emite **siempre una salida base no vacía con RC 0** (como el pmset real, que lista el encabezado de assertions aunque no haya ninguna) y agrega la línea `pid N(caffeinate)` **solo mientras el pid del pidfile vive**. Muerto el proceso, la salida sigue siendo no vacía y sin match — `check()` retorna 1, muerte confirmada; un stub que enmudeciera al morir el pid haría caer a `check()` en el fallback sin pmset y devolver 2 (indeterminado), y `kill_confirmed()` no confirmaría jamás. Así una misma corrida de `awake.sh stop` observa la assertion viva y, tras el TERM, su muerte confirmada (transición 0→1 de `check()`, congelada explícitamente), y el caso "proceso que ignora TERM" la sigue mostrando viva sin guion aparte. Modo `absent` (sin salida, rc≠0) para forzar el fallback sin pmset; salida fija solo donde un caso puntual la pida.

**Matriz por clase** (numeración de IMPLEMENTATION §03; cada caso referencia la ronda del ciclo 00 que lo encontró):

1. **Parser de veredicto + gate de RC** (r1.1): última línea `VERDICT: APPROVED` → exit 0; `VERDICT: CHANGES_REQUESTED` → exit 1; tolerancia de espacios/CRLF/líneas vacías finales; veredicto en el medio con otra última línea → exit 2; sin veredicto, con sufijo (`APPROVED.`), minúsculas → exit 2 (comparación literal); mensaje vacío o ausente → exit 2; **RC≠0 de codex con mensaje válido ya escrito → exit 2 sin tomar veredicto**.
2. **Consistencia del estado** (r2.2), separando **estado de resultado** (base, `last-verdict`, racha) de **estado de ciclo** (ronda, session id), con asserts por modo y por etapa del error — esta es la semántica actual que se congela:
   - *Resultado*: base y `last-verdict` se escriben **solo** con un veredicto validado — intactos en toda salida 2, en ambos modos. La racha: +1 con CHANGES_REQUESTED, 0 con APPROVED, intacta en todo exit 2 de `round`; `new` la rearma **al abrir el ciclo**, antes de invocar e incluso si la corrida después falla — el rearme pertenece a la apertura, no al resultado.
   - *Ciclo*: `round` fallido deja el SID intacto y la ronda ya incrementada (se anota antes de invocar: un intento fallido consume número de ronda informativo, nunca racha); `new` borra el SID al lanzar y, si el intento fallido alcanzó a emitir `thread.started`, recaptura el nuevo — comportamiento congelado adrede: es la base del resume exacto del retry (paso B).
   - *Etapas*: pre-invocación (stdin vacío; deadlock) — `round` no toca nada, ni siquiera la ronda; `new` con stdin vacío ya rearmó la racha (parte de la apertura, documentado). Post-invocación (RC≠0; mensaje vacío; veredicto inválido) — según lo anterior, verificado con huella del estado antes/después.
   - `status` muestra solo el último resultado validado (tras una corrida rechazada con "APPROVED" en el cuerpo del mensaje, no cambia); `round` resume con el SID guardado (assert sobre el argv del doble); sin session file → `resume --last`.
3. **Deadlock** (r1.3, r2.3): racha 4 → aviso; racha 5 → `round` sale 2 con `DEADLOCK` en stderr **sin invocar a codex** (el log del doble queda vacío); `reset-deadlock` rearma; `new` no hereda la racha.
4. **`wt_valid` contra impostores** (r3.1) — las tres invariantes, cada una con su caso determinista: (a) **subdirectorio común** del repo en `WT_DIR` (pasa `rev-parse` heredando el repo padre; falla `toplevel == WT_DIR`); (b) **repo git independiente** anidado en `WT_DIR` (toplevel coincide; falla git-dir bajo `.git/worktrees/` del repo); (c) **worktree movido** (`mv` de un worktree válido del fixture a `WT_DIR`: toplevel y git-dir pasan; `worktree list` registra el path viejo — falla la tercera). En los tres: el canónico del fixture con un tracked modificado sin commit + un untracked de centinela **sobrevive intacto** (ni `reset --hard` ni `clean -fdx` lo tocan), el impostor se reemplaza y la review corre. Más: `WT_DIR` borrado se recrea; `WT_DIR` válido se reusa (el residuo desaparece, el worktree es el mismo).
5. **Tri-estado y `kill_confirmed` de awake.sh** (r1.6, r2.4, r3.2): `status` en los tres estados (viva por pmset; muerta confirmada → limpia pidfile huérfano; **indeterminado** — pmset ausente + pid vivo que no es caffeinate — no toca nada); `stop` viva → dentro de la misma corrida observa la assertion viva y, tras el TERM, su muerte confirmada por el pmset dinámico (salida base sin match ⇒ retorno 1, no indeterminado); pidfile borrado; `stop` indeterminado → exit 2 con pidfile y proceso intactos; `stop` con proceso que ignora TERM → exit 2 conservando el pidfile (la señal no surtió efecto); `start` con horas inválidas (texto, 0) → exit 2 **antes** de tocar la assertion vigente; `start` sobre viva → renovación positiva completa: el pid viejo muere de verdad (`kill -0` falla), el nuevo queda vivo y el pidfile cambia; `start` indeterminado → exit 2 sin arrancar ni pisar el pidfile. El caso "fallback sin pmset con caffeinate vivo" usa una copia de `sleep` renombrada `caffeinate` para que el `comm` real coincida.
6. **Movimiento de base a `REVIEW_HEAD`** (r1.2): el hook del doble commitea en el canónico durante la "review" y el mensaje aprueba → la base queda en el `REVIEW_HEAD` del pedido (no el HEAD nuevo), sale el aviso de commits posteriores no aprobados, y el próximo `round` los incluye en su rango (assert sobre el prompt).
7. **Congelamiento de la observación** (r2.1): con el mismo hook, mientras el canónico avanza, el worktree sigue clavado al `REVIEW_HEAD` del pedido (el hook registra desde adentro `rev-parse HEAD` del worktree y el contenido viejo del archivo cambiado); regresión de residuo: lo que una ronda deja en el worktree (untracked + tracked modificado) no existe al arrancar la siguiente.

### Captura robusta del session id (paso B)

Hoy: primer UUID del archivo de eventos — que mezcla stderr de codex (`2>&1`): una línea de error con un UUID anterior al evento real capturaría un id falso. Cambio: extraer `thread_id` **del evento `thread.started`** (formato verificado contra una corrida real de este repo). Sin `thread.started` → aviso y fallback `resume --last`, como hoy; el grep laxo de "cualquier UUID" desaparece (era exactamente la fragilidad). Regresión en la suite: eventos con UUID espurio en una línea de error previa al `thread.started` → se captura el `thread_id` correcto (el comportamiento actual fallaría este caso).

### Retry transitorio (paso B)

`review.sh` reintenta **una vez, dentro de la misma ronda**, solo ante **falla de proceso**: RC≠0 de codex, o mensaje final ausente/vacío con RC 0. Un mensaje bien entregado con veredicto inválido **no** se reintenta (no es transitorio: es incumplimiento de contrato → exit 2 directo). Máquina de estados del reintento, cuidando la **frontera de contexto** entre features:

- `new` fallido **con** `thread_id` capturado del intento → retry por `resume <SID>` exacto: la sesión ya existe; relanzar `exec` abriría una segunda.
- `new` fallido **sin** `thread_id` → retry con un `exec` nuevo. **Jamás `resume --last`** en este camino: sin SID propio, `--last` podría retomar la sesión del feature anterior — violación de la frontera de contexto por feature.
- `round` fallido → retry con `resume` del mismo SID vigente, argv idéntico (si no había SID guardado, repite el fallback `resume --last` que ya venía usando esa ronda).
- Antes del retry se preservan los eventos del intento fallido (`last-review-events.failed.jsonl`) y el reintento se anota en stderr; el contador de ronda no se duplica (se escribió antes del primer intento). `AXEL_REVIEW_RETRIES` (default 1, `0` desactiva — lo usan los tests).

Regresiones (con el guion secuencial del doble): fallo→éxito en `new` **con** SID (el retry va por `resume <SID>` — assert de argv) y **sin** SID (va por `exec`, nunca `--last`); fallo→éxito en `round` (mismo SID, mismo número de ronda); dos fallas consecutivas → exit 2; mensaje vacío con RC 0 → reintenta; veredicto inválido → **no** reintenta; `AXEL_REVIEW_RETRIES=0` → no reintenta; eventos del intento fallido preservados; el veredicto validado del segundo intento escribe el estado normal (base/racha/`last-verdict`).

Efecto lateral aceptado: si el primer intento murió a mitad de sesión, el resume del retry le repite el prompt al reviewer — el mismo efecto que ya tiene el reintento manual documentado hoy.

**Mapa completo de docs del retry**: `review-contract.md` (el reintento de proceso pasa del generador al script; si el exit 2 persiste, el generador diagnostica con los eventos y corta a RECAP); skill `feature` (su regla de exit 2); skills `design` y `plan` (dejan explícita la taxonomía completa: `DEADLOCK` no se reintenta jamás; falla de proceso la reintenta el script y si persiste → RECAP; veredicto inválido → exit 2 directo al manejo acordado); y las enumeraciones de estado local de `AGENTS.md`, `templates/AGENTS.md` (duplicación obligatoria raíz↔plantilla) y `docs/DESIGN.md` incorporan el registro de métricas (`rounds-log`).

### Métricas de rondas (paso B)

`.claude/state/rounds-log` (no versionado), con esquema fijo que **distingue evento, intento y ronda**: una línea por evento del loop — `fecha · modo · ronda · intento · resultado · SHA corto · racha`. Resultados con invocación de Codex (intento numérico): `APPROVED`, `CHANGES_REQUESTED`, `NO_VERDICT` (mensaje entregado con última línea inválida), `PROC_FAIL` (RC≠0 o mensaje vacío — cada intento fallido deja su propia línea). Eventos **pre-invocación** (ronda, intento y SHA en `-`; Codex no corre): `DEADLOCK` (bloqueo por racha) e `INPUT_ERROR` (stdin vacío) — **todo rechazo pre-invocación deja su línea**: en particular, un `new` con stdin vacío rearma la racha (abre el ciclo, clase 2) y sin línea propia `status` seguiría resumiendo el ciclo anterior con la racha ya perteneciente al nuevo.

Denominadores explícitos: **eventos** = todas las líneas; **intentos** = líneas con intento numérico (`DEADLOCK`/`INPUT_ERROR` quedan fuera); **rondas** = números de ronda distintos. Una ronda con `PROC_FAIL` seguido de `APPROVED` son dos líneas con el mismo número de ronda — una ronda, dos intentos, dos eventos. La frontera de ciclo es la **primera línea de modo `new`, falle o no — incluso pre-invocación**: la apertura del ciclo es del modo, no del resultado, coherente con la semántica de racha de la clase 2.

`review.sh status` resume el ciclo actual desde esa frontera — rondas, intentos, eventos, distribución por resultado — y muestra las últimas líneas del log. Regresiones: contenido del log tras secuencias guionadas (fallo→éxito, veredicto inválido, deadlock, `new` con stdin vacío que abre ciclo, ciclo nuevo) y conteos de `status` congelados por assert — incluido que `DEADLOCK` e `INPUT_ERROR` **no** inflan el denominador de intentos. Es **observabilidad local**: la memoria oficial por feature sigue siendo el Review log de los docs; se documenta en el contrato como estado local, junto a los demás archivos de `.claude/state/`.

### shellcheck (paso C)

- Instalación por brew, con evidencia (versión) en este doc.
- `tests/lint.sh`: puerta que corre `shellcheck --severity=warning` sobre `scripts/*.sh` y `tests/*.sh`. **Fail-closed**: shellcheck ausente → exit ≠ 0 con la instrucción de instalación (una puerta que se salta en silencio no es puerta).
- Hallazgos: los de severidad error/warning se corrigen (o se silencian con `# shellcheck disable=SCnnnn` + comentario justificando, caso por caso); los info/style se triagean — se aplican los que son señal real y el resto queda inventariado en este doc con el criterio. Gate en `warning`: el volumen de style sobre `install.sh` (40 KB con heredocs) es ruido que no justifica arriesgar refactors sobre contrato aprobado en 01/02.

## Criterios de cierre

1. **Suite del loop en verde y aislada**: `tests/loop.sh` cubre las 7 clases con la matriz de arriba — incluida la semántica de estado por modo y etapa (resultado vs ciclo) —, corre sin Codex real (doble por PATH, con su log de invocaciones asertado), jamás toca el repo axel real, y pasa **3 corridas consecutivas completas**.
2. **Las 3 invariantes de `wt_valid` ejercitadas** cada una por su caso (subdir impostor, repo anidado, worktree movido), con el canónico del fixture demostrado intacto por centinelas en los tres.
3. **Session id por `thread.started`**: capturado del evento (no "primer UUID"), con la regresión del UUID espurio en verde y el fallback `resume --last` intacto.
4. **Retry transitorio con frontera de contexto**: un reintento automático solo ante falla de proceso, misma ronda; `new` fallido con SID reanuda esa sesión exacta y sin SID relanza `exec` (jamás `resume --last`); eventos del intento fallido preservados; veredicto inválido no se reintenta; las regresiones de la máquina de estados en verde; contrato y las tres skills actualizados sin contradicciones (mapa completo del enfoque).
5. **Métricas con esquema evento/intento/ronda**: `rounds-log` registra cada evento con el esquema definido (dos intentos de una ronda comparten número; `DEADLOCK` e `INPUT_ERROR` como eventos pre-invocación con intento `-`, fuera del denominador de intentos; el ciclo lo delimita la primera línea `new`, falle o no — incluso pre-invocación), `status` resume el ciclo actual con esos denominadores, y las enumeraciones de estado local (AGENTS raíz + plantilla + DESIGN + contrato) quedan al día.
6. **Lint como puerta**: shellcheck instalado (evidencia), `tests/lint.sh` en verde sobre `scripts/` y `tests/`, hallazgos corregidos o justificados uno a uno.
7. **Aceptación real**: las rondas de review de este ciclo corridas con el `review.sh` modificado (registradas en el Review log); DESIGN/IMPLEMENTATION/STATUS y el contrato al día.

## Decisiones

- 2026-07-27 (bajada): **suite separada** (`tests/loop.sh`) en vez de anexar a `tests/install.sh` — objeto distinto (loop vs instalador), dobles propios. **Orden A→B→C**: la red de regresión congela el comportamiento actual antes de tocar `review.sh`. **Retry solo de fallas de proceso** (RC≠0 / mensaje ausente), nunca de veredictos inválidos: transitorio ≠ incumplimiento. **Session id anclado a `thread.started`** con fallback `resume --last`, eliminando el grep laxo de UUID. **Métricas como estado local** no versionado (la memoria oficial es el Review log). **Gate de lint en `--severity=warning`** con triage documentado de info/style. **Invariante c de `wt_valid` testeada vía worktree movido** (toplevel y git-dir pasan; el registro delata el path viejo).
- 2026-07-27 (ronda 1): la matriz de estado separa **resultado** (base, `last-verdict`, racha) de **ciclo** (ronda, SID) y congela la semántica real por modo y etapa — el rearme de racha y la recaptura de SID pertenecen a la **apertura** del `new`, falle o no (la matriz original los declaraba intactos en todo exit 2 y era incumplible contra el comportamiento actual). **Máquina de estados del retry con frontera de contexto**: `new` fallido con SID → resume exacto de esa sesión; sin SID → `exec` nuevo, jamás `resume --last` (podría retomar la sesión del feature anterior). **Doble de codex con guion secuencial por invocación** (fallo→éxito representable); **doble de pmset derivado de la vida real del pid** (la transición viva→muerta dentro de una misma corrida de `stop`, y la renovación positiva de `start`, no salen de un guion estático). **Esquema ronda/intento/invocación** en el log de métricas, con el ciclo delimitado por el `new` falle o no. **Mapa completo de docs del retry**: contrato + skills feature/design/plan + enumeraciones de estado local en AGENTS raíz, plantilla y DESIGN.
- 2026-07-27 (ronda 2): el stub `track` de pmset emite **siempre salida base no vacía** y solo agrega la línea de assertion mientras el pid vive — si enmudeciera al morir, `check()` caería al fallback y daría indeterminado en vez de muerte confirmada (la transición 0→1 queda congelada explícitamente). Denominadores de métricas saneados: **intentos = líneas con intento numérico**; `DEADLOCK` e `INPUT_ERROR` son eventos pre-invocación (ronda/intento/SHA `-`) que no inflan intentos. **Todo rechazo pre-invocación deja línea en el log**: el `new` con stdin vacío rearma la racha y sin línea propia `status` resumiría el ciclo anterior con la racha del nuevo — la frontera de ciclo (primera línea `new`, falle o no) ahora es realizable porque todo `new` deja línea.

## Riesgos

- **El doble de codex diverge del real** → eventos de fixture tomados de una corrida real de este repo (formato `thread.started` verificado hoy), y la aceptación real es el propio loop del ciclo corriendo con el script modificado.
- **Semántica de procesos macOS en los tests de awake** (comm de `ps`, señales, timing) → dobles de `pmset`/`caffeinate`, procesos efímeros propios de la suite, y la copia renombrada de `sleep` para el caso de comm real; si un caso no se puede hacer determinista, se documenta el hueco antes que aceptar un flake (lección de T15t6 en el 02).
- **Tests que ejecutan `reset --hard`/`clean -fdx`** → confinados a fixtures `mktemp` con el patrón ya probado de la suite del instalador, y con centinelas que demuestran que el canónico del fixture no sufre efectos; cwd siempre dentro del fixture.
- **El retry repite el prompt en la sesión del reviewer** si el intento murió a mitad → aceptado, único, mismo efecto que el reintento manual actual.
- **shellcheck sobre `install.sh` pide cambios masivos** → gate en warning y triage: nada de refactors grandes sobre el contrato aprobado del 01/02; el residuo queda inventariado.

## Review log

- **Ronda 1: CHANGES_REQUESTED** (5 puntos, todos aceptados; el reviewer verificó además el bookkeeping del cierre del 02, que el worktree movido efectivamente pasa las dos primeras invariantes y falla la tercera, y que la suite vigente sigue en 321 ok). Resolución:
  1. La matriz de estado exigía racha y SID intactos en todo exit 2, pero `new` rearma la racha antes de invocar y recaptura el SID aunque codex falle — incumplible contra el comportamiento actual → corregido: la clase 2 separa estado de **resultado** (base, `last-verdict`, racha) de estado de **ciclo** (ronda, SID), con la semántica real por modo y etapa (pre/post invocación) y el rearme/recaptura documentados como parte de la apertura del `new`.
  2. Retry sin máquina de estados (un `new` fallido sin SID que cayera a `resume --last` violaría la frontera de contexto entre features) → corregido: especificados los tres caminos (`new`+SID → resume exacto; `new` sin SID → `exec` nuevo, jamás `--last`; `round` → resume del SID vigente), las regresiones completas (fallo→éxito ×3, dos fallas, mensaje vacío, veredicto inválido sin retry, retries=0, eventos preservados, misma ronda, argv/SID asertado) y el doble pasa a guion secuencial por invocación.
  3. Métricas mezclaban rondas, intentos e invocaciones → corregido: esquema fijo con campo de intento, `PROC_FAIL`+`APPROVED` = una ronda/dos intentos con el mismo número, `DEADLOCK` con intento `-` (sin invocación), ciclo delimitado por el `new` falle o no, y regresiones de log y conteos de `status`.
  4. El doble estático de pmset no podía representar la transición viva→muerta dentro de una corrida de `stop` ni la renovación positiva de `start` → corregido: doble dinámico derivado de la vida real del pid trackeado (modo `track`), `absent` para el fallback, y los dos casos de transición explícitos en la matriz.
  5. Mapa de docs incompleto → corregido: se suman las skills `design` y `plan` (taxonomía completa de exit 2: deadlock / falla de proceso reintentada por el script / veredicto inválido) y las enumeraciones de estado local de `AGENTS.md`, `templates/AGENTS.md` y `docs/DESIGN.md` (respetando la duplicación raíz↔plantilla).
- **Ronda 2: CHANGES_REQUESTED** (3 puntos, todos aceptados; convergiendo 5→3; el reviewer dio por bien resueltas la separación de estado y la máquina del retry). Resolución:
  1. El stub `track` de pmset, si imprimía la assertion "solo mientras vive", al morir el pid dejaba stdout vacío → `check()` lo interpreta como pmset ausente y retorna 2 (indeterminado), no 1 — `kill_confirmed()` jamás confirmaría → corregido: salida base siempre no vacía con RC 0 (como el pmset real) + línea de assertion condicional; la transición 0→1 de `check()` queda congelada explícitamente en la matriz.
  2. `DEADLOCK` contaminaba el denominador de intentos ("intentos = líneas" lo contaba pese al intento `-`) → corregido: eventos = todas las líneas; intentos = líneas con intento numérico; assert específico de que deadlock e `INPUT_ERROR` no inflan intentos.
  3. El `new` con stdin vacío abría el ciclo (racha rearmada) pero no dejaba línea en el log — `status` resumiría el ciclo anterior con la racha del nuevo → corregido: evento pre-invocación `INPUT_ERROR` (ronda/intento/SHA `-`) para todo rechazo previo a invocar; la frontera "primera línea `new`, falle o no" pasa a ser realizable; regresión específica en la matriz de métricas.
- **Ronda 3: APPROVED (bajada)** — los tres puntos de r2 cerrados a juicio del reviewer (pmset `track` reproduce la transición 0→1 que `kill_confirmed` exige; eventos/intentos/rondas con denominadores no ambiguos; `INPUT_ERROR` hace auditable la apertura de ciclo de todo `new`); verificó que la semántica congelada coincide con el orden real de mutaciones de `review.sh` y los retornos de `awake.sh`, más `git diff --check` y `bash -n` limpios. Base movida a `853f9d5`. Convergencia de la bajada: 5→3→0 (r1–r3). El APPROVED habilita el paso A; no cierra el feature.
