# Pipeline 2026-08-05 — `/build`

## Gate

- tipo: `pipeline`
- protocolo: `spawn-token v1`
- Pedido del humano (2026-08-05, literal; los saltos de línea del original se marcan con «/»): «Revisa el README y hacelo FACIL. El usuario debe entender en un parrafo para que sirve, y por qué. No se por que en el README de hoy se habla de commits, y despues habla de rounds y no se que más. POR QUE ESTA DICIENDO ESTO? deberia ser simple, deberia ser: / Para que sirve? Por Que lo usaria? / Como lo instalo? / Como lo uso? / Lo esencial»
- Modo: `normal`
- Autorizado: 2026-08-05 — «dale, autorizado» · Ajustes de alcance: ninguno
- Resultado visible al final: `README.md` reescrito donde el **primer párrafo** le dice a quien llega para qué sirve axel y por qué lo usaría, seguido de los bloques ¿por qué lo usaría? · ¿cómo lo instalo? · ¿cómo lo uso? · lo esencial — sin la apertura por métricas (rounds, commits) que hoy exige un contexto que el lector todavía no tiene
- gate_base: `88e197178e67ba8f5957d8cbad711a7660d88168`

## Contexto que el padre verificó al abrir el gate

- La queja es verificable en el `README.md` vigente (entregado por el feature 13, cerrado 2026-08-02): la línea 5 abre con «At commit `b0bdf4d`: 88 logged review rounds · 59 rejections · zero first-round approvals» y el párrafo siguiente explica qué es una «round» — métricas y su glosario antes de que el lector sepa qué es axel.
- La estructura vigente del README la fija el diseño: [../design/public-surface.md](../design/public-surface.md) §«Estructura del README» (nueve secciones, cifras en el título con su unidad). El pedido la contradice ⇒ hace falta un **design-delta**; editar el README sin tocar el diseño lo dejaría contradiciendo su propio doc.
- El backlog de [../IMPLEMENTATION.md](../IMPLEMENTATION.md) está vacío (features 00–14 cerrados) ⇒ implementar exige un **plan-delta** previo.
- Multifase (diseño + plan + implementación) desde estado estable ⇒ pipeline.
- Pendiente ajeno que no bloquea pero conviene tener a la vista: los tres comandos de GitHub (push, topics, homepage) siguen sin correr — `main` está adelante de `origin`, así que el README que este pipeline reescriba reemplaza a uno que todavía no se publicó.

## Ruta propuesta

### design — design-delta — README de cinco preguntas

Reescribir §«Estructura del README» de [../design/public-surface.md](../design/public-surface.md): el README pasa a contestar, en este orden y sin jerga, **¿Para qué sirve? · ¿Por qué lo usaría? · ¿Cómo lo instalo? · ¿Cómo lo uso? · Lo esencial**, con un primer párrafo que un recién llegado entiende sin contexto previo. Las métricas dejan la apertura: si aparecen, es una vez, más abajo y con su contexto, citando [../metrics.md](../metrics.md) como fuente única. Lo que **no** se toca del diseño vigente: política de idioma (vidriera en inglés), contrato editorial (hecho derivable / limitación declarada / opinión marcada), corte README-extracto vs. manual-completo, requisitos antes del comando de instalación. El destino del contenido desplazado (transcript de sesión, «What this is not», etc.) lo decide esta unidad: se comprime, se muda o se referencia — sin pérdida operativa.

### plan — plan-delta — feature 15 en el backlog

Agregar el feature 15 («README simple: reescritura según la estructura de cinco preguntas») a [../IMPLEMENTATION.md](../IMPLEMENTATION.md) — único ítem nuevo, posición por criterio 1 (pedido humano).

### 15 — feature — Reescritura del README

Reescribir `README.md` (en inglés, por la política de idioma vigente) según la estructura nueva, con su loop de review: verificación de cero pérdida operativa (todo lo operativo sigue completo en `docs/install.md`) y del contrato editorial sobre la prosa nueva.

## Modelos por unidad

Tabla canónica de la skill `build`: `design` y `plan` con `fable`; feature `15` con `opus`. Sin override del gate salvo ajuste del humano.

## Estado

| Unidad | Tipo | Estado | SHA inicio | SHA APPROVED |
|---|---|---|---|---|
| design | design-delta | **Cerrada** | `70d99d7` | `4a60531` (cierre `11d280a`) |
| plan | plan-delta | **Cerrada** | `5ae5fc6` | `75e192a` (cierre `f0d9e95`) |
| 15 | feature | **Cerrada** | `87ee282` | `12bd6ff` (cierre `7477425`; bajada APPROVED en r5, `a7164f3`) |

## Eventos

- 2026-08-05 — ledger creado y gate presentado; STATUS → «esperando autorización de pipeline»
- 2026-08-05 — **autorización global recibida** («dale, autorizado»), sin ajustes de alcance: README sigue en inglés y «Lo esencial» queda como lo interpretó el gate. Pipeline en curso.
- 2026-08-05 — **arranque de la unidad `design`** (design-delta, modelo `fable` por §«Modelos por unidad»), `token=B3DA49AF-8BDF-4350-AA80-C9F3CD9C2E86`, SHA inicio `70d99d7`. Pre-arranque verificado: el resumen re-derivado coincide en sustancia con el autorizado — [../design/public-surface.md](../design/public-surface.md) §«Estructura del README» sigue fijando las nueve secciones con las cifras en el título, y el `README.md` vigente sigue abriendo con la línea de métricas al corte `b0bdf4d` más el párrafo que explica la «round»; fuentes suficientes (diseño, README, `docs/metrics.md` presentes). Sin divergencia. Anclas limpias antes de acuñar: sin `pipeline-child-token` activo (solo el `retired-E232C306…` del pipeline 2026-07-29, que es historia), sin `pipeline-expected`, árbol limpio.

- 2026-08-05 — review r1 de la unidad `design`: `CHANGES_REQUESTED` con cuatro puntos, corregidos sin argumentar en `560790a` (mención de métricas fijada en exactamente una; el doc del render incorporado al inventario de la superficie pública; los agregados del cierre del pipeline 2026-07-29 (3) re-derivados del review log — 30 rondas con veredicto y 99 pedidos; «tres plantillas» normalizado a dos formularios más `config.yml`).
- 2026-08-05 — review r2: `CHANGES_REQUESTED` con un punto único, corregido en `4a60531` (balances intermedios del ciclo de bajada del feature 14 a 22 rondas con veredicto, aritmética consistente con el total 30/99).
- 2026-08-05 — **`APPROVED` de la unidad `design` en la ronda 3** (head `4a60531`, sin observaciones accionables); cierre del hijo en `11d280a`. Tres rondas, cinco pedidos de Codex, los cinco aceptados sin argumentar. Ninguna decisión del delta se movió por la review: lo que cambió fueron cifras heredadas y trazabilidad. Entregado: §«Estructura del README» de `docs/design/public-surface.md` reescrita (un párrafo de apertura y cuatro bloques; métricas fuera de la apertura con mención única citando `docs/metrics.md`; render de la sesión mudado a doc propio de la vidriera, path en la bajada del 15; «What this is not» comprimido en «lo esencial» sin despublicar limitaciones; reversión registrada de «cifras en el título») + decisión con procedencia completa en `docs/DESIGN.md`.

- 2026-08-05 — **arranque de la unidad `plan`** (plan-delta, modelo `fable` por §«Modelos por unidad»), `token=2E8D829D-2B44-479F-8A40-1C9AAF42142E`, SHA inicio `5ae5fc6`. Pre-arranque verificado: el resumen re-derivado coincide con el autorizado — el diseño nuevo está cerrado (§«Estructura del README» reescrita, APPROVED r3) y el backlog de `IMPLEMENTATION.md` sigue sin feature 15 (00–14 cerrados); el delta sigue siendo agregar el 15 como único ítem nuevo, posición por criterio 1. Fuentes suficientes. Anclas limpias antes de acuñar (el `claimed-B3DA49AF…` de la unidad design fue retirado por su hijo al cerrar; solo queda el `retired` del pipeline de julio).

- 2026-08-05 — review r1 de la unidad `plan`: `CHANGES_REQUESTED` con un punto único (las fechas de dos eventos de este ledger estaban en UTC — 2026-08-06 — cuando los commits son del 05 en -03:00), corregido sin argumentar en `75e192a`.
- 2026-08-05 — **`APPROVED` de la unidad `plan` en la ronda 2** (base `4a60531`, head `75e192a`, sin observaciones accionables); cierre del hijo en `f0d9e95`. Dos rondas, un pedido, de precisión factual. Entregado: feature 15 («README simple: reescritura según la estructura de cinco preguntas») como único ítem del backlog nuevo en `docs/IMPLEMENTATION.md`, posición por criterio 1, con su extensión de procedencia (pedido literal del gate, ledger, restricción de un solo feature) y su sección (lo fijado por el diseño, lo que decide la bajada, superficie y verificación prevista).

- 2026-08-05 — **arranque de la unidad `15`** (feature, modelo `opus` por §«Modelos por unidad»), `token=0F7F8469-AD07-4339-96A9-D64380577251`, SHA inicio `87ee282`. Pre-arranque verificado: el resumen re-derivado coincide con el autorizado — el feature 15 está en el backlog de `IMPLEMENTATION.md` como Pendiente (extensión 2026-08-05, APPROVED r2) y el diseño cerrado fija la estructura (§«Estructura del README» de `public-surface.md`); la unidad implementa la reescritura de `README.md` con su loop de review. Fuentes suficientes. Anclas limpias antes de acuñar (el `claimed-2E8D829D…` retirado por el hijo del plan al cerrar; solo queda el `retired` de julio).

- 2026-08-05/06 — **ciclo de bajada fina de la unidad `15`** (r1–r5): bajada escrita en `cbe9e20` (`docs/implementation/15-readme-simple.md`) y **APPROVED en la r5** (head `a7164f3`, «no quedan bloqueantes: podés empezar a escribir», cierre del tramo en `8d38954`). Cinco rondas, 21 pedidos, los 21 aceptados sin argumentar; las tres decisiones que el hijo puso a discusión en la r1 (SHA inline en la mención de métricas, frontera de qué cuenta como cifra del loop, cierre de placeholders de path desde una unidad `feature`) quedaron dadas por buenas ahí mismo. Lo que iteró cinco veces fue la capacidad de los criterios de atrapar defectos — cuatro versiones del criterio de inventario cayeron por satisfacer la forma sin demostrar nada.
- 2026-08-06 — **implementación de la unidad `15`** (r6–r10): mudanzas primero (`docs/session.md` generado mecánicamente por script; diagrama a `docs/install.md` byte a byte), README reescrito en la r7 (179 → 156 líneas al cierre). Rondas 6–9 `CHANGES_REQUESTED` (racha 4): la r7 tumbó cuatro clasificaciones del inventario al volver mecánica la comprobación de literalidad y obligó a escribir §«What a run does, and where it stops» en `docs/install.md` (el README publicaba comportamiento operativo que el manual no desarrollaba); la r8 hizo derivar los locators mecánicamente; la r9 dejó un bloqueante único, y al corregirlo el hijo encontró y sinceró una sincronización que su propio review log daba por hecha sin haberse hecho.
- 2026-08-06 — **incidente sin pérdida**: la sesión del hijo se cortó por límite de uso del harness (~01:20, reseteo 03:20) justo al empezar a consumir el desenlace de la r9; el estado en disco quedó consistente (terminal sin consumir, `phase=launched`, árbol limpio) y el padre lo reanudó a las 12:50 con su contexto intacto, sin relanzar review ni re-acuñar token.
- 2026-08-06 — **`APPROVED` de la unidad `15` en la ronda 10** (head `12bd6ff`, sin bloqueantes); cierre del hijo en `7477425`. Diez rondas, **33 pedidos de Codex, los 33 aceptados sin argumentar**, una sola desviación declarada (la cola de una formulación que duplicaba otra oración), ratificada por el reviewer. Entregado: `README.md` reescrito — párrafo de apertura sin cifras, sin SHAs y sin vocabulario del método, más los cuatro bloques en el orden de las preguntas del lector, métricas una sola vez con unidad y corte —, `docs/session.md` nuevo con el render de la sesión mudado entero, y `docs/install.md` con el diagrama y la sección operativa nueva. Ninguna de las ocho decisiones de la bajada se movió en el ciclo.

## Cierre

- 2026-08-06 — **OK humano del pipeline** («Ok»), en respuesta al RECAP consolidado. Cierra las tres unidades y cubre los dos commits no revisados que listó el RECAP (`7477425`, `1fe9af7`, solo bookkeeping de ledger/STATUS). Resultado entregado, tal como lo prometió el gate: `README.md` con el párrafo de apertura y los cuatro bloques (156 líneas), `docs/session.md` nuevo y `docs/install.md` ampliado.
