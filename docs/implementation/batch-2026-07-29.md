# Lote 2026-07-29 — `/feature all`

## Gate
- Autorizado: 2026-07-29 — «autorizo, dale»
- Features (en orden): 08, 09, 10  · Exclusiones/correcciones: ninguna
- gate_base: `60d4e608d01e3c1618926d8abaef5fc9c38b1673`
- protocolo: `legacy` — sin token de spawn. Esta corrida la arrancó un padre con instrucciones previas al feature 09, así que sus eventos de arranque no declaran `token=` y sus hijos se validan con la coincidencia STATUS+ledger (la protección vigente hasta el 09). Anotado por el hijo del feature 09 a pedido de la review r3 (p3): la compatibilidad se **declara**, nunca se infiere de una línea ausente — un ledger que declare `protocolo: spawn-token v1` y omita el token es una corrida malformada y corta, y uno **sin** la línea también corta (r4 p2). Los lotes arrancados con la skill actualizada nacen con `protocolo: spawn-token v1`.

## Resúmenes autorizados

### 08 — Reentradas de fase y feature: inventario completo fail-closed

Cierra los huecos del inventario de reentradas para que reabrir cualquier sesión caída tenga camino definido. Hoy `/feature` cubre «esperando OK», «esperando confirmación de arranque» y el lote, pero **no** el feature individual **activo** (en bajada, implementación o con review en vuelo); `/design` y `/plan` no tienen ramas para «review en curso» ni «esperando OK». Se agregan esas ramas con la semántica fail-closed de la reentrada del lote: reconstrucción desde STATUS + doc de fase/feature (Review log incluido) + estado de review en `.claude/state/`; **sin relanzar ni duplicar una review en vuelo**; ante inconsistencia, RECAP sin adivinar. La bajada resuelve: cómo distinguir review en vuelo de review muerta (la ausencia de señal terminal es ambigua), y cuánta reconstrucción se comparte entre las tres skills. Superficie: skills `design`, `plan`, `feature`, y lo que STATUS deba registrar de más.

### 09 — Ruteo implícito: triggers y despacho por contexto

Un pedido sin comando se despacha a la fase correcta, sin maquinaria nueva: (1) las `description` de las skills pasan a ser triggers por contexto; (2) sección de ruteo en `AGENTS.md` (+ espejo en `templates/AGENTS.md`) con el orden de despacho contractual fail-closed — consulta → solo `/status`; **estado pendiente manda** (entrega a las reentradas del 08 — de ahí la dependencia); clasificación solo desde estado estable, por lo que el pedido necesita; ambigüedad → pregunta de una línea. Incluye: la regla de confirmación (el ruteo nunca arranca trabajo sin punto de confirmación; `/plan` despachado gana confirmación liviana), la corrección de la línea de `AGENTS.md` sobre `/recap` (no es consulta: fija «esperando OK» y commitea), y el ruteo en lenguaje natural inequívoco al modo lote. **Interinato multifase**: mientras `/build` no exista, un pedido que cruza fases recibe un comportamiento interino fail-closed que la bajada define (sin encadenar fases sin OK); el 10 lo reemplaza. La bajada resuelve además la redacción exacta de triggers/tabla y cómo verificar el ruteo (candidato: matriz pedido→destino en el doc, verificada por el reviewer).

### 10 — Pipeline `/build`: unidades tipadas con gate único

Skill nueva `build`: un pedido que cruza dos o más fases encadena los deltas necesarios (design-delta → plan-delta → feature(s)) en una sola corrida — gate único de pipeline (ruta + resumen por delta + qué habrá visible al final → una autorización global), hijo fresco por unidad tipada con loop de review completo, unidades en «APPROVED — pendiente OK de pipeline», OK del RECAP consolidado cierra todo. Reusa los contratos del lote (padre liviano, señal terminal, ledger con reentrada fail-closed, condiciones de corte + divergencia entre unidades); sin lote anidado. POC-first como caso particular (proyecto virgen): DESIGN «borrador (modo POC)», feature 01 = esqueleto que camina, endurecimiento post-OK recomendado y no forzado. Superficie: skill `build` nueva, `feature`/`design`/`plan` (semántica de hijo de pipeline), `recap`, `AGENTS.md` + plantilla (regla dura extendida: «ni cruzar a otra fase sin OK — salvo lote o pipeline autorizado»), plantilla de ledger, **instalador + tests** (allowlist: `build` debe viajar), y `review-contract.md` (generalizar `AXEL_REVIEW_ID`, sin cambio de lógica en `review.sh`). La bajada decide: ledger propio vs. generalizado, relación con `/feature all`, forma del marker POC, overrides de modelo por unidad, validación del pedido.

## Estado

| # | Feature | Estado | SHA inicio | SHA APPROVED |
|---|---|---|---|---|
| 08 | Reentradas de fase y feature: inventario completo fail-closed | APPROVED — pendiente OK de lote | `d01094f` | `394965c` |
| 09 | Ruteo implícito: triggers y despacho por contexto | APPROVED — pendiente OK de lote | `764f6a2` | `82d13c8` |
| 10 | Pipeline `/build`: unidades tipadas con gate único | pendiente | — | — |

## Eventos
- 2026-07-29 — gate de lote autorizado («autorizo, dale»), sin exclusiones; ledger creado sobre `gate_base` `60d4e60`.
- 2026-07-29 — feature 08: pre-arranque OK (resumen re-derivado coincide en sustancia con el autorizado; fuentes suficientes), ventana de awake renovada, hijo lanzado desde `d01094f` (el commit de arranque; la fila decía `fa4db69` porque se escribió antes de commitearlo — corregido acá).
- 2026-07-29 — feature 08: **APPROVED de cierre en r5** (base `a2f673d`, cierre en `394965c`). Cinco rondas, cuatro hallazgos de Codex aceptados: sede canónica a `review-contract.md` (payload) en vez de `AGENTS.md` (semilla), `codex-session-id` como precondición de lanzamiento y no de reentrada, token de ronda derivado de `.claude/state/round` con la excepción del primer `new`, y la rama del APPROVED de paso intermedio. Verificación del padre: árbol limpio, `batch-expected` borrada, IMPLEMENTATION y STATUS al día, seis commits presentes.
- 2026-07-29 — feature 09: pre-arranque OK — resumen re-derivado coincide en sustancia con el autorizado (la entrada del plan no cambió; el 08 solo agregó la línea de reentradas a `AGENTS.md`/plantilla, que es justamente la dependencia declarada). Ventana de awake renovada, hijo lanzado desde el commit de arranque.
- 2026-07-29 — feature 09: **APPROVED de cierre en r7** (base `ee20a57`, cierre en `82d13c8`), con el APPROVED de la bajada en r5. Siete rondas, cinco con cambios pedidos, todos aceptados: `recap` dentro de la guarda (muta STATUS y commitea), guarda acotada al ruteo implícito, confirmación de `/plan` persistida, y el **token de spawn** del hijo de lote (adquisición exclusiva por rename, triple coincidencia evento/prompt/ancla, máquina de reentrada de 8 filas, discriminante de protocolo de tres valores). Verificación del padre: árbol limpio, `batch-expected` y anclas de token ausentes, IMPLEMENTATION y STATUS al día, diez commits presentes. Dos apuntes del hijo: `tests/loop.sh` heredaba `AXEL_REVIEW_ID` del entorno de las reviews de lote y fallaba L10 (corregido con `unset`; 292/1 → 293/0), y el commit `b6b2563` tocó solo `AGENTS.md` sin STATUS — única infracción de la regla dura en el rango, confirmada por Codex, registrada en el doc del feature y corregida en el commit siguiente sin amend.

## Cierre
- pendiente

