# 02 — Instalación remota: one-liner desde GitHub

## Alcance

Instalar axel en un destino **sin clon local previo** (pedido humano 2026-07-27, al dar el OK del 01): un solo comando que baja axel desde GitHub y corre la instalación de una. Tres entregables:

- **Modo bootstrap del instalador**: `scripts/install.sh --from <url> <target-dir>` clona (o actualiza) un **cache local de axel** en `AXEL_HOME` (default `~/.axel`) y **delega** la instalación en el `install.sh` de ese clon. Toda la lógica de instalación (payload, semillas, preflight, settings) es la del feature 01 sin cambio de contrato: el modo local `install.sh <target-dir>` se comporta exactamente igual que hoy.
- **Camino piped**: `curl -fsSL <raw>/scripts/install.sh | bash -s -- --from <url> <target-dir>` funciona — el script corre sin `$0` en disco, desde cualquier cwd (incluso adentro de otro repo git), hace el clone/update del cache y ejecuta el instalador clonado.
- **README para agentes**: sección pensada para que un agente de Claude Code la lea y la siga ante "instalá axel siguiendo <url>": precondiciones, el comando, qué hacer según exit code, y el camino desde adentro de una sesión (las skills se cargan en caliente; el settings rige pleno recién en la sesión siguiente). Incluye ajuste menor de `templates/AGENTS.md`: la nota de actualización de la maquinaria menciona también el camino `--from` para destinos sin clon de axel.

Fuera de alcance (v1): verificación de integridad/firma de lo bajado (se confía en HTTPS + GitHub); pinning de versión (`--ref` o similar — siempre el branch default del remoto); soporte Windows; merge de settings (sigue siendo de `/adopt`); shellcheck (queda en el feature 03, hardening — la referencia "feature 02" que quedó en el doc del 01 era anterior a la inserción de este feature y se corrigió con esta bajada).

## Enfoque técnico

### Un solo entry point: `install.sh --from`

Se elige extender `install.sh` (como esboza IMPLEMENTATION) en vez de un script bootstrap separado: una sola URL y un solo comando que enseñar, y la delegación garantiza que **el código que instala es siempre el del clon** — versionado, auditable, idéntico al camino local. El costo de tocar el script aprobado del 01 se acota con dos reglas:

- El parseo de argumentos y el branch `--from` viven **antes de toda resolución de `AXEL_ROOT`**: en modo piped no hay `$0` en disco y el `git rev-parse` actual resolvería el repo del cwd (p. ej. el propio destino). Nada del modo bootstrap depende del entorno del script que corre.
- El modo local queda cubierto por la suite existente **sin tocar sus asserts**: mismo contrato, misma salida, mismos exit codes.

Flujo del modo `--from`:

1. **Argumentos**: `--from <url>` exige URL y `<target-dir>`, con **flag de modo separado del contenido** — `--from ""` rechaza, jamás cae al modo local (repro del reviewer, r5). Una URL que parece opción (empieza con `-`) se **rechaza**, y en todo comando git la URL viaja después de `--` — un valor option-like jamás llega a interpretarse como flag de git. El resto de la validación del destino (toplevel, self-install, árbol limpio) sigue siendo del delegado: nada se duplica.
2. **Disjunción cache/lock↔destino, antes de cualquier clone/pull**: el destino (debe existir como directorio, igual que exige el delegado), `AXEL_HOME` **y el path del lock** se canonicalizan — para paths aún inexistentes se canonicaliza el ancestro existente más profundo y se anexa el resto **con normalización léxica de `.`/`..` en el sufijo** (un `../` en la parte inexistente esquivaría la comparación y el clone aterrizaría adentro del destino — repro del reviewer, r5), así un cache o lock planeado adentro del destino con padres sin crear también se detecta. Igualdad o relación ancestro/descendiente de cualquiera de los dos con el destino ⇒ **rechazo**. Sin esto, un `AXEL_HOME` (o un `.lock`) adentro del destino ensuciaría el árbol que el delegado va a exigir limpio, y un destino adentro del cache podría ser modificado por el update.
3. **Lock del cache** (validada la disjunción, se asegura antes el padre de `AXEL_HOME` con `mkdir -p` — el lock es su hermano y `ln -s` lo necesita): todo el acceso (validación, update y delegación — el delegado lee el payload del cache mientras instala) se serializa con un lock en `$AXEL_HOME.lock`: un **symlink creado atómicamente** (`ln -s` falla si el path existe) cuyo target codifica la propiedad (`pid` + host) — no hay ventana entre tomar el lock y anotar el dueño, ni formato a medias. Un preexistente que no sea un symlink con ese formato (archivo, directorio, symlink ajeno) ⇒ **rechazo sin borrarlo** — propiedad no demostrable, misma regla que marker y handoff. Dueño con pid vivo ⇒ espera acotada (default 60s; override por env para los tests) y a timeout ⇒ rechazo con el pid en el mensaje; pid muerto ⇒ **rechazo inmediato sin auto-limpieza**, con la instrucción de verificar y borrar a mano — fail-closed: una recuperación automática sin carrera entre dos reclaimers exigiría un compare-and-swap que bash no tiene (cambia la decisión de r1, que limpiaba huérfanos con aviso). Liberación por trap (EXIT/INT/TERM) que verifica que el lock siga siendo propio antes de borrar — y **jamás con el delegado vivo**: ante una señal dirigida solo al wrapper, el trap reenvía `TERM` al delegado y **espera su muerte** (`wait`) antes de quitar el lock; si el wrapper muere sin poder demostrarla (`KILL`), el lock queda — fail-closed, la corrida siguiente rechaza con instrucciones. Liberar con el hijo vivo dejaría a otra instalación actualizar la fuente que ese hijo está leyendo.
4. **Cache** (`AXEL_HOME`, default `~/.axel`, override por env — lo usan también los tests):
   - No existe → `git clone -- <url> "$AXEL_HOME"` (el padre ya está asegurado desde el paso 3).
   - Existe pero no es un directorio, o no es el toplevel de un repo git → **rechazo**.
   - `origin` no coincide con la URL pedida (normalizando solo `.git` y `/` finales) → **rechazo** mostrando ambas.
   - Árbol sucio → **rechazo** (podría haber trabajo humano; jamás se pisa).
   - La verdad del branch default y de su tip viene **del remoto, no de la metadata local del cache** — un `origin/HEAD`, branch y upstream adulterados hacia otro branch pasan cualquier chequeo local (repro del reviewer, ronda 2): `git ls-remote --symref -- <url> HEAD` da el default real (server que no informa el symref ⇒ rechazo, fail-closed) y el fetch va **por URL con refspec explícito**, con el tip en `FETCH_HEAD` — ni `remote.origin.fetch` ni `origin/HEAD` locales participan. El fetch corre con hooks deshabilitados y **`--no-tags`** (un `reference-transaction` hook plantado en el cache se ejecutaba durante el fetch, disparado por el auto-follow de un tag alcanzable — repro del reviewer, r6; la verificación posterior no deshace código ya ejecutado).
   - HEAD debe estar en el branch local homónimo del default real: detached u otro branch ⇒ **rechazo**.
   - **Ancestría explícita**: HEAD debe ser ancestro (o igual) del tip fetcheado; **ahead o divergido ⇒ rechazo** — `pull --ff-only` solo no alcanza: con un commit local ahead devuelve 0 "Already up to date" y se instalaría código no publicado (repro del reviewer, ronda 1). Detrás → `git merge --ff-only FETCH_HEAD`.
   - **Árbol demostrado contra el commit, sin pasar por el index** (post-merge): `skip-worktree`/`assume-unchanged` ocultan reemplazos a `git status` (repro del reviewer, r5: un delegado sustituido con skip-worktree se ejecutaba) — cualquier flag distinto de `H` en `git ls-files -v` ⇒ **rechazo**; además cada entrada del commit se compara contra el archivo real por **hash y modo** (symlinks incluidos), con `hash-object` corriendo **en el contexto del cache** (`git -C`) — desde un cwd dentro de un repo SHA-256 el object-format del caller daba falso rechazo (repro r6) — y el listado consumido entero por herestring, sin pipes ni salidas tempranas: un corte temprano le metía SIGPIPE a `ls-tree` y `pipefail` lo convertía en un RC 141 fuera de la taxonomía (repro r6). Los hooks del cache quedan **deshabilitados** (`core.hooksPath` a un path nulo, clone con `--template=`) en clone, fetch y merge — y como la verificación corre después del merge, ataja también lo que un hook hubiera logrado modificar.
5. **Sanity del clon**: `"$AXEL_HOME/scripts/install.sh"` archivo **regular — no symlink** (`-f` sigue symlinks: un delegado symlink hacia un ejecutable fuera del clon se ejecutaba, repro del reviewer r5), legible y ejecutable → si no, "el remoto no parece axel", rechazo.
6. **Delegación**: se invoca `"$AXEL_HOME/scripts/install.sh" <target-dir>` — sin `--from` (recursión imposible) y **sin `exec`** (el lock debe liberarse al terminar) — y se captura su RC.

**Taxonomía de salida del bootstrap**: toda falla propia, anterior al lanzamiento del delegado (argumentos, disjunción, lock, cache, red, sanity), es **exit 2 sin mutaciones del destino** — el cache sí puede haberse clonado o actualizado: es infraestructura de la máquina, no del destino, y es el punto del comando. Del delegado lanzado correctamente se propagan **solo sus RC contractuales (0/1/2)**; cualquier otro RC (126/127 de lanzamiento fallido, muerte por señal) se **normaliza a 2 con aviso**: en el caso de señal el destino puede haber quedado con el diff parcial de un instalador interrumpido, y el aviso manda a mirar `git status` en el destino (la misma exposición que ya tiene el modo local del 01 ante un kill a mitad de escritura).

### Seguridad del cache: fail-closed, nada destructivo, un solo usuario a la vez

El cache es un clon normal de axel que el bootstrap solo avanza por fast-forward y solo cuando está **exactamente** en el estado esperado: limpio, en el branch default real del remoto, sin commits locales (HEAD ancestro del tip consultado al remoto — `FETCH_HEAD`, nunca la metadata local). Todo lo demás — sucio, ahead, divergido, detached, otro branch, otro origin, irreconocible — se rechaza con instrucción de resolverlo a mano. Nunca `reset --hard`, nunca clobber. Es la misma postura del instalador del 01 (propiedad demostrable o nada) aplicada al único directorio nuevo que este feature introduce, más el lock que serializa sesiones concurrentes sobre el cache global: dos instalaciones simultáneas no pueden pisarse la fuente mientras una de ellas la lee.

### README: one-liner + guía para agentes

La sección "Llevar axel a otro proyecto" gana el camino remoto como primera opción:

```bash
curl -fsSL https://raw.githubusercontent.com/alexweil/axel/main/scripts/install.sh | bash -s -- --from https://github.com/alexweil/axel <repo-destino>
```

y conserva el modo local (con clon) como está. Subsección nueva **"Para agentes (Claude Code)"**, autocontenida para que una sesión a la que le dicen "instalá axel siguiendo <url>" pueda ejecutar de punta a punta:

- Precondiciones: destino = repo git con árbol limpio; `git`, `curl`, `python3` disponibles.
- El comando, con `<repo-destino>` = toplevel del repo (p. ej. `$(git rev-parse --show-toplevel)`).
- Reacción por exit code: `0` → revisar el diff, commitearlo con el proceso del destino, `/status`; `1` → diff + commit + cerrar la adopción con `/adopt` (handoff en `docs/ADOPTION.md`); `2` → leer el reporte y distinguir: si es un **rechazo** (del bootstrap o del preflight del delegado), nada se escribió en el destino (el cache `~/.axel` sí pudo crearse o actualizarse); si el aviso dice que el instalador fue **interrumpido** (RC anómalo o señal), revisar `git status` del destino antes de seguir — puede haber diff parcial.
- Camino en-sesión: instalado desde adentro de una sesión de Claude Code, las skills quedan disponibles en caliente; los permisos del settings sembrado rigen plenos recién en la **sesión siguiente** (hasta entonces puede haber prompts de confirmación). El instalador no commitea: el commit es del proceso del destino.
- Quien prefiera auditar antes de ejecutar: el camino en dos pasos (clonar y correr `scripts/install.sh` del clon) hace exactamente lo mismo.

### Verificación

Tests nuevos en `tests/install.sh` (misma suite y helpers; sección propia). **Sin red**: los "remotos" son repos git locales (`git clone` acepta paths) y `AXEL_HOME` se apunta a un directorio temporal por env en todos los casos. Matriz:

- **Bootstrap fresco**: sin cache → clona → delega → destino con la estructura completa del 01, exit 0; `axel-sha` del marker == HEAD del remoto local (prueba de que instaló el clon, no otra fuente).
- **Re-run con remoto avanzado**: cache existente limpio → ff-update → el update del destino registra el SHA nuevo.
- **Cache fail-closed** (exit 2; destino y cache intactos por huella de contenido): sucio; **ahead** (commit local con árbol limpio — el caso que `pull --ff-only` no ve); divergido; **detached**; **otro branch**; origin distinto; `AXEL_HOME` archivo regular; directorio no-repo.
- **Metadata local adulterada**: `origin/HEAD` + branch + upstream apuntando a otro branch (repro r2) → exit 2 (HEAD no está en el default real del remoto); `remote.origin.fetch` adulterado → inocuo por diseño (fetch por URL con refspec explícito): la corrida instala el default real; remoto que no informa el symref de HEAD (bare con HEAD detached) → exit 2.
- **Árbol del cache no demostrable** (repros r5): delegado reemplazado bajo `skip-worktree` (con precondición verificada: `git status --porcelain` vacío) → exit 2 sin ejecutar el reemplazo; ídem `assume-unchanged`; `post-merge` hook plantado en el cache → **no corre** durante el ff-update (exit 0, sin el side-effect del hook, marker al tip nuevo); remoto cuyo `scripts/install.sh` es un **symlink** a un ejecutable externo → exit 2 sin ejecutarlo; `--from ""` → exit 2 sin caer al modo local.
- **`..` en el sufijo inexistente de `AXEL_HOME`** (repro r5: `<tmp>/missing/../destino/cache` esquivaba la disjunción y el clone aterrizaba adentro del destino) → exit 2 pre-mkdir con huella del destino.
- **Repros r6**: remoto con gitlink como primera entrada + árbol grande → exit **2** con diagnóstico ("tipo 160000 inesperado"), destino intacto y lock liberado — nada de RC 141 por SIGPIPE; caller con cwd dentro de un repo **SHA-256** → exit 0 con marker correcto (los hashes se computan en el cache); `reference-transaction` hook plantado en el cache + remoto avanzado con tag alcanzable → exit 0, el hook **no corre** y el tag **no entra** (`--no-tags`).
- **Disjunción cache/lock↔destino** (exit 2; huella del destino antes/después): `AXEL_HOME == target`; `AXEL_HOME` dentro del target; target dentro de `AXEL_HOME`; `$AXEL_HOME.lock == target` (cache hermano del destino); `AXEL_HOME` con padres aún inexistentes adentro del target → rechazo pre-mkdir; padre inexistente **fuera** del destino → se crea y la corrida sigue.
- **URL option-like** (p. ej. `--upload-pack=…`) → exit 2 sin invocar git.
- **Remoto malformado**: clon sin `scripts/install.sh` → exit 2 (sanity); `install.sh` presente pero sin bit de ejecución → exit 2.
- **Red rota**: URL de clone inexistente (cache fresco) y fetch que falla sobre cache existente (origin apuntando a un path borrado) → exit 2; destino y cache intactos.
- **Lock**: en poder de un pid vivo → espera y timeout (acortado por env) → exit 2 con mensaje; pid muerto → exit 2 **inmediato, sin borrar el lock** (con instrucciones; huella verifica que sigue ahí); preexistente ajeno (directorio, archivo regular, symlink con formato inválido) → exit 2 sin borrarlo; **señal al wrapper con delegado vivo**: `TERM` dirigido solo al wrapper mientras un delegado stub (que ignora `TERM` un rato) sigue corriendo → el lock **persiste** mientras el hijo viva y se libera recién tras su muerte; **smoke concurrente**: dos bootstraps simultáneos a destinos distintos comparten el cache y terminan ambos exit 0 con el mismo `axel-sha` en los dos markers (la espera del lock los serializa).
- **Normalización de RCs** (delegados stub servidos por un remoto de fixture): delegado que retorna un RC no contractual (p. ej. 7) → bootstrap exit 2 + aviso + lock liberado; delegado que no lanza (shebang roto → 126) → exit 2 + aviso; delegado que **muta el destino y muere por señal** → exit 2 + aviso de interrupción (el diff parcial queda visible para `git status`) + lock liberado.
- **Passthrough**: destino con contenido propio vía `--from` → exit 1 con handoff (adopción); destino sucio → exit 2 del delegado, atraviesa tal cual.
- **Piped**: script por stdin (`bash -s -- --from …`) con cwd adentro de **otro** repo git → instala igual (no misresuelve la fuente); piped **sin** `--from` → exit 2 con el mensaje que apunta a `--from`.
- **Modo local intacto**: la suite del 01 pasa completa sin modificar sus asserts.

Cierre con **aceptación real contra GitHub**: el **one-liner piped exacto del README** (`curl -fsSL … | bash -s -- --from https://github.com/alexweil/axel …`) sobre un repo de prueba, con `AXEL_HOME` fresco y aislado — es la única evidencia que cubre `curl`, raw.githubusercontent y el camino por stdin reales. Se registran en el Review log: comando, RC, salida, `origin` y branch del cache resultante, y la correspondencia HEAD del cache == `axel-sha` del marker del destino.

## Criterios de cierre

1. **Bootstrap de punta a punta**: `install.sh --from <url> <target>` sin clon previo deja el destino idéntico a una corrida local del 01 (estructura completa, marker con el SHA del remoto), exit 0; re-corrida con el remoto avanzado hace ff-update del cache y actualiza el destino al SHA nuevo.
2. **Piped robusto**: el script por stdin instala igual desde cwd arbitrario (incluso dentro de otro repo git); piped sin `--from` rechaza con mensaje que apunta a `--from`.
3. **Cache fail-closed y disjunto**: no-repo, origin distinto, sucio, **ahead, divergido, detached u otro branch** ⇒ exit 2 sin tocar destino ni cache (verificado por huella de contenido); la disjunción cache/lock↔destino se verifica **antes de cualquier clone/pull** (igualdad y ancestro/descendiente, paths canónicos incluso con padres inexistentes); la verdad del branch default y su tip se consulta **al remoto** (`ls-remote --symref` + fetch por URL con refspec explícito) — metadata local adulterada (origin/HEAD, upstream, `remote.origin.fetch`) no puede desviar la fuente, con los tres casos en la matriz; solo ff-update de un cache limpio en el branch default real; fallo de clone/red ⇒ exit 2 con destino intacto.
4. **Contrato intacto, passthrough y taxonomía de RCs**: el modo local no cambia (suite del 01 verde, asserts existentes sin tocar); del delegado lanzado correctamente atraviesan **solo** sus RC contractuales 0/1/2 (incluido el 1 de adopción con su handoff); toda falla propia del bootstrap y todo RC no contractual (126/127, señales) ⇒ exit 2, este último con aviso; regresiones de normalización con delegados stub (RC no contractual, fallo de lanzamiento, señal post-mutación) verificando RC, aviso, estado del destino y lock liberado.
5. **Concurrencia y lock fail-closed**: el acceso al cache (validación + update + delegación) está serializado por un lock atómico con propiedad demostrable que participa de la disjunción con el destino; sin auto-reclaim (pid muerto ⇒ exit 2 inmediato con instrucciones; ajeno o malformado ⇒ exit 2 sin borrar nada); el lock **jamás se libera con el delegado vivo** (ante señal al wrapper: reenvío de `TERM` + `wait` antes de soltar; sin demostración de muerte, queda stale); las regresiones de lock (vivo ⇒ timeout; muerto ⇒ rechazo sin borrar; ajeno ⇒ rechazo sin borrar; colisión con el destino; padres inexistentes; señal al wrapper con delegado vivo ⇒ lock persistente hasta la muerte del hijo) y el smoke concurrente pasan.
6. **Docs y evidencia**: matriz nueva en verde en `tests/install.sh`; `bash -n` limpio sobre los scripts tocados; README con el one-liner y la sección para agentes (camino en-sesión y la distinción rechazo —destino intacto— vs interrupción anómala —revisar `git status`— incluidos); `templates/AGENTS.md` con la nota de actualización remota; DESIGN/IMPLEMENTATION/STATUS al día; la aceptación real = one-liner piped exacto con cache fresco, registrada en el Review log.

## Decisiones

- 2026-07-27 (bajada): **un solo entry point** (`install.sh --from`) en vez de bootstrap separado — una URL para enseñar y la garantía de que lo que instala es siempre el clon versionado; el riesgo de tocar el script aprobado se acota moviendo args/`--from` antes de toda resolución de fuente y dejando el contrato local cubierto por la suite existente. **Clone completo** (no shallow): repo chico, cero sorpresas en pulls posteriores. **ff-only + rechazo de cache sucio/divergido/ajeno**: nada destructivo sobre un directorio que podría tener trabajo humano. **Sin `--ref`** en v1: branch default del remoto; pinning es hardening futuro si duele. **Los agentes usan el mismo `--from`** que los humanos: un solo flujo con todas las validaciones (nada de `clone || pull` a mano en el README que las esquive).
- 2026-07-27 (ronda 1): **ancestría explícita** en vez de confiar en `pull --ff-only` (no detecta commits locales ahead — repro del reviewer) y estado exacto requerido del cache (branch default, no detached); **disjunción canónica cache↔destino antes de cualquier clone/pull**; **taxonomía de RCs** — solo 0/1/2 del delegado lanzado atraviesan, todo lo propio y los RC no contractuales se normalizan a 2 (con aviso si el delegado murió a mitad); **lock atómico** con espera acotada — y por eso la delegación deja de usar `exec` (el lock se libera por trap al terminar); URL siempre tras `--` y rechazo de URLs option-like; la aceptación real pasa a ser el **one-liner piped exacto** del README con cache fresco aislado.
- 2026-07-27 (ronda 6, implementación): `hash-object` corre **en el contexto del cache** (`git -C`) — el object-format del repo que rodee al cwd del caller no participa (falso rechazo SHA-256); hooks deshabilitados **también en fetch**, más `--no-tags` (el auto-follow de tags disparaba `reference-transaction`); `verify_tree` consume el listado entero por herestring — sin pipes no hay SIGPIPE y el RC queda dentro de la taxonomía; `ls-tree` con rc verificado y árbol vacío ⇒ rechazo (fail-closed también ante un listado ausente).
- 2026-07-27 (ronda 5, implementación): la coincidencia cache↔commit se **demuestra sin el index** — flags de `ls-files -v` distintos de `H` rechazan y el árbol se verifica entrada por entrada con `hash-object` + modo, después del merge; hooks del cache **deshabilitados** en clone/merge (`core.hooksPath` nulo + `--template=`); sanity del delegado exige **regular no-symlink**; el modo `--from` usa un **flag separado del contenido de la URL** (vacía ⇒ rechazo); `canon_path` normaliza léxicamente `.`/`..` del sufijo inexistente. El comentario de exit 2 del script distingue rechazo de delegado interrumpido.
- 2026-07-27 (ronda 3): el trap del lock **espera la muerte del delegado antes de liberar** (reenvío de `TERM` + `wait`; sin demostración — p. ej. `KILL` al wrapper — el lock queda stale): liberar con el hijo vivo dejaría a otra instalación actualizar la fuente que ese hijo lee; el `mkdir -p` del padre de `AXEL_HOME` se mueve a después de la disjunción y antes del lock (el `ln -s` lo necesita); la ancestría queda expresada en todo el doc contra el tip consultado al remoto (`FETCH_HEAD`), nunca contra `origin/<default>` local.
- 2026-07-27 (ronda 2): la verdad del **default branch y su tip se consulta al remoto** (`ls-remote --symref` contra la URL pedida + fetch por URL con refspec explícito a `FETCH_HEAD`) — la metadata local del cache es mutable y no participa (repro del reviewer: origin/HEAD, branch y upstream adulterados pasaban todo chequeo local); el lock pasa a ser un **symlink atómico con la propiedad en el target** (sin ventana entre crearlo y anotar el dueño) y su path **participa de la disjunción** con el destino; **sin auto-reclaim de locks** — pid muerto o lock ajeno ⇒ exit 2 sin borrar nada (supersede r1: la limpieza automática de huérfanos tenía carrera entre dos reclaimers y bash no tiene compare-and-swap); la guía de agentes separa rechazo (destino intacto) de interrupción anómala (revisar `git status`); regresiones de normalización de RCs con delegados stub.

## Riesgos

- **`curl | bash` ejecuta código sin review previa**: mitigado porque en `--from` el script piped solo parsea args, clona y delega — mínimo auditable; lo que instala es el clon versionado, y el README ofrece el camino en dos pasos para auditar antes de correr.
- **Cache ≠ checkout de desarrollo**: `~/.axel` es un clon más de axel y alguien podría trabajar ahí. Mitigado: fail-closed (sucio/ahead/divergido jamás se pisa, solo ff), y el README lo llama cache y apunta el desarrollo a un clon propio.
- **El one-liner cablea `main` y la URL del repo**: cambio de branch default o de dueño rompe el string del README. Aceptado: es un string en un solo lugar, visible.
- **Deriva piped ↔ clonado**: el script bajado (p. ej. cacheado por el CDN de raw.githubusercontent) puede ser más viejo que el clon que instala. Benigno por diseño: la delegación ejecuta siempre el del clon; el piped solo necesita estabilidad en el parseo de `--from` y el manejo del cache.
- **Delegado interrumpido (señal/kill)**: el destino puede quedar con un diff parcial; el bootstrap sale 2 con aviso de mirar `git status` en el destino. Misma exposición que el modo local del 01; el diff visible de git sigue siendo la red.
- **Lock sin auto-reclaim**: un crash duro (SIGKILL, corte de energía) deja el lock y la próxima corrida exige verificación y borrado manual (el mensaje trae el pid y la instrucción exacta). Aceptado: molesto pero jamás engañoso; la alternativa —reclaim automático— tiene carrera entre dos reclaimers.

## Verificación — resultados

- **Paso A** (2026-07-27): `install.sh --from` implementado (bootstrap completo: args/URL option-like, disjunción canónica con `canon_path` para paths inexistentes, lock symlink atómico con espera acotada y trap que espera al hijo, verdad remota vía `ls-remote --symref` + fetch por URL a `FETCH_HEAD`, ancestría explícita, sanity y delegación con normalización de RCs; modo local endurecido con `BASH_SOURCE` + estructura esperada). Suite tras las correcciones de r5 y r6: **321 asserts, 0 fallas** — los 158 del feature 01 intactos (contrato local sin cambios) + 163 nuevos de la matriz T15 (bootstrap fresco y update, los ocho estados fail-closed del cache, metadata adulterada ×3, árbol no demostrable ×5 — skip-worktree, assume-unchanged, post-merge hook, delegado symlink, `--from ""` —, repros r6 ×3 — gitlink/SIGPIPE, caller SHA-256, hook de fetch con tag —, disjunción ×5 incluido `..` en sufijo inexistente, URL option-like, remoto malformado ×2, red rota ×2, lock ×5 incluida la señal al wrapper con delegado vivo y el smoke concurrente, passthrough 1/2, normalización de RCs ×3, piped ×2). `bash -n` limpio sobre `install.sh` y `tests/install.sh`. Estabilidad verificada tras r7: **tres corridas consecutivas completas en verde** (el único flake — gc en background del fixture grande — quedó eliminado por config).
- Bug encontrado y corregido por la propia suite (T15p3): `ln -s` sobre un **directorio** existente no falla — crea el link adentro —, con lo que el lock se "tomaba" dentro de un directorio ajeno y la instalación seguía; corregido con pre-chequeo del caso directorio antes del `ln -s` más verificación de propiedad post-adquisición (y retiro del residuo propio si un directorio ajeno aparece en la ventana).

## Aceptación real contra GitHub (paso B, 2026-07-27)

Corrida en esta máquina, con red real (curl → raw.githubusercontent.com; git → github.com), cache **fresco y aislado** vía el override documentado `AXEL_HOME` (el resto del comando es byte a byte el one-liner del README). Fixture: `$ACC` = `mktemp -d`; destino `$ACC/demo` = repo git recién inicializado (`git init -b main` + commit vacío).

**Corrida 1 — instalación desde cero:**

```bash
curl -fsSL https://raw.githubusercontent.com/alexweil/axel/main/scripts/install.sh | AXEL_HOME="$ACC/axel-cache" bash -s -- --from https://github.com/alexweil/axel "$ACC/demo"
```

- RC: **0**. Salida (íntegra en estructura; paths abreviados a `$ACC`): línea de bootstrap `── axel bootstrap · remoto: https://github.com/alexweil/axel · cache: $ACC/axel-cache (main @ 1fa8566) ──`, seguida del reporte del instalador delegado `modo: initial` con los 10 payloads, las 5 semillas, `CLAUDE.md → AGENTS.md (symlink)`, `.gitignore (entrada .claude/state/)` y `.claude/axel-install` bajo "instalado".
- Cache resultante: `origin` = `https://github.com/alexweil/axel`, branch `main`, HEAD `1fa85667901ef4524a3543fa2944c1c068919317`.
- Marker del destino: `axel-install-format: 1` / `axel-sha: 1fa85667901ef4524a3543fa2944c1c068919317` — **idéntico al HEAD del cache** (criterio: lo instalado es el clon).
- `$ACC/axel-cache.lock`: no existe tras la corrida (lock liberado).

**Corrida 2 — mismo one-liner, cache existente, destino commiteado:**

- RC: **0**, `modo: update`, bootstrap reusó el cache (`main @ 1fa8566`, ff no-op); los 10 payloads + marker bajo "sin cambios", las 5 semillas "preexistente, intacto".
- `git status --porcelain` del destino tras la corrida: **vacío** — update no-op sin diff alguno, marker incluido, a través del camino remoto completo.

Reproducibilidad: los SHAs corresponden a `origin/main` = `1fa8566` al momento de la corrida; una re-corrida futura instalará el tip vigente y el marker acompañará.

## Review log

- **Ronda 1: CHANGES_REQUESTED** (6 puntos, todos aceptados; bookkeeping del cierre del 01 verificado sin observaciones). Resolución:
  1. `pull --ff-only` no detecta un cache con commits locales ahead (repro del reviewer: devuelve 0 "Already up to date" y se instalaría código no publicado) → corregido: validación explícita de branch default, upstream y **ancestría** (HEAD ancestro de `origin/<default>`); ahead, divergido, detached y otro branch rechazan; los cuatro casos en la matriz.
  2. Validar el destino solo en el delegado permitía mutarlo antes del rechazo (`AXEL_HOME` adentro del target, o target == cache) → corregido: **disjunción canónica cache↔destino antes de cualquier clone/pull** (igualdad y ancestro/descendiente); tres topologías en la matriz con huella del destino.
  3. Taxonomía de errores abierta (git devuelve 1/128; un `exec` fallido termina 126/127) → corregido: toda falla propia del bootstrap ⇒ exit 2; del delegado lanzado atraviesan solo 0/1/2 y los RC no contractuales se normalizan a 2 con aviso; sanity exige regular+legible+ejecutable; URL tras `--` y option-like rechazada; remoto malformado, no-ejecutable y red rota sobre cache existente en la matriz.
  4. Cache global sin contrato de concurrencia → corregido: lock atómico (`mkdir` + pid) sobre validación+update+delegación, espera acotada con override por env, recuperación de lock huérfano documentada; regresiones deterministas (lock vivo ⇒ timeout; huérfano ⇒ recuperado) + smoke concurrente; la delegación deja de usar `exec` para poder liberar el lock por trap.
  5. La aceptación con red no cubría `curl`/raw/stdin → corregido: la evidencia de cierre es el **one-liner piped exacto del README** con `AXEL_HOME` fresco y aislado, registrando comando, RC, salida, origin/branch del cache y HEAD == `axel-sha` del marker.
  6. Inconsistencias documentales → corregidas: la guía de agentes dice "nada se escribió **en el destino**"; IMPLEMENTATION §02 ya no presenta el enfoque como "a definir" y el párrafo del orden acordado distingue la numeración original del plan del override humano posterior.
- **Ronda 2: CHANGES_REQUESTED** (3 puntos, todos aceptados; convergiendo 6→3). Resolución:
  1. `origin/HEAD` local tomado como verdad del branch default es metadata mutable (repro del reviewer: cache limpio con origin/HEAD, branch y upstream cambiados a `feature` pasaba todas las validaciones propuestas, con `ls-remote` informando `main`) → corregido: `ls-remote --symref` contra la URL pedida da el default real (server sin symref ⇒ rechazo) y el fetch va por URL con refspec explícito a `FETCH_HEAD` — nada local participa; adulteraciones de origin/HEAD/upstream y de `remote.origin.fetch` en la matriz.
  2. Lock no fail-closed (fuera de la disjunción — `$AXEL_HOME.lock` podía coincidir con el destino —; sin propiedad demostrable frente a objetos ajenos; carreras mkdir→pid y entre dos reclaimers) → corregido: symlink atómico con la propiedad en el target, path del lock dentro de la canonicalización/disjunción (incluidos padres inexistentes), objetos ajenos rechazados **sin borrarlos**, y sin auto-reclaim: pid muerto ⇒ exit 2 inmediato con instrucciones (fail-closed en lugar de una recuperación con carrera); colisión con destino, ajeno/malformado y padres inexistentes en la matriz.
  3. La guía contradecía la taxonomía (prometía destino intacto para todo exit 2) y la normalización de RCs no tenía tests → corregido: la guía separa rechazo (intacto) de interrupción anómala (revisar `git status`); regresiones con delegados stub — RC no contractual, fallo de lanzamiento (126), señal post-mutación — verificando RC 2, aviso, estado del destino y lock liberado.
- **Ronda 3: CHANGES_REQUESTED** (2 puntos, ambos aceptados; convergiendo 6→3→2; el reviewer verificó por su cuenta la consulta remota, el fetch explícito y que un remoto con HEAD detached no entrega symref). Resolución:
  1. Ventana de señales en el lock: `INT/TERM` al wrapper liberaba por trap con el delegado posiblemente vivo leyendo el cache — otra instalación podría actualizar la fuente bajo sus pies → corregido: el trap reenvía `TERM` al delegado y espera su muerte (`wait`) antes de quitar el lock; sin demostración de muerte (`KILL` al wrapper), el lock queda stale (fail-closed); regresión nueva: `TERM` solo al wrapper con un delegado stub que ignora `TERM` → el lock persiste mientras el hijo viva.
  2. Dos remanentes de sincronización del doc → corregidos: el `mkdir -p` del padre de `AXEL_HOME` se movió a después de la disjunción y antes del lock (el paso del lock lo necesita para `ln -s`); la sección de seguridad del cache expresaba la ancestría contra `origin/<default>` local — ahora dice `FETCH_HEAD`/tip consultado al remoto, coherente con lo eliminado en r2.
- **Ronda 4: APPROVED (bajada)** — los dos puntos de r3 resueltos; secuencia disjunción→padre→lock→clone coherente y ancestría expresada solo contra el tip remoto. Base movida a `6010ed2`; el APPROVED habilita la implementación, no cierra el feature. Convergencia de la bajada: 6→3→2→0 (r1–r4).
- **Ronda 5 (paso A): CHANGES_REQUESTED** (4 puntos, todos aceptados; los cuatro reproducidos por el reviewer ejecutando; verificó además la suite completa y que los 158 asserts del 01 no se modificaron). Resolución:
  1. `canon_path` no normalizaba `..` dentro del sufijo inexistente (repro: `AXEL_HOME=<tmp>/missing/../destino/cache` esquivaba la disjunción y el clone quedaba adentro del destino) → corregido: normalización léxica de `.`/`..` sobre el ancestro canónico; T15l5 con huella del destino.
  2. El checkout real no estaba demostrado contra `FETCH_HEAD` (repro: delegado reemplazado bajo `skip-worktree` con `status --porcelain` vacío se ejecutaba; y un `post-merge` local podía modificar la fuente durante el ff) → corregido: flags de `ls-files -v` distintos de `H` rechazan; verificación entrada por entrada contra el commit por `hash-object` + modo, corriendo después del merge; hooks deshabilitados en clone (`--template=` + `core.hooksPath` nulo) y merge; T15t1–t3.
  3. El sanity seguía symlinks (repro: `scripts/install.sh` symlink a un ejecutable externo al clon se ejecutaba) → corregido: rechazo explícito de `-L`; T15t4 verifica que el externo no corre.
  4. `--from ""` caía al modo local e instalaba (repro con exit 0) → corregido: flag de modo separado del contenido de la URL, vacía ⇒ exit 2; T15t5. El comentario de exit del script ya no promete destino intacto para el caso de delegado interrumpido (aviso explícito + `git status`).
- **Ronda 6 (paso A): CHANGES_REQUESTED** (3 puntos, todos aceptados y reproducidos por el reviewer; verificó además suite en verde, `bash -n` y `git diff --check`). Resolución:
  1. `hash-object` dependía del cwd (repro: caller dentro de un repo SHA-256 ⇒ falso rechazo de un cache SHA-1 válido, contradiciendo el camino piped desde cualquier cwd) → corregido: ambos hashes (archivos y symlinks) corren con `git -C "$BOOT_CACHE"`; T15t7 instala con cwd dentro de un repo `--object-format=sha256`.
  2. El fetch ejecutaba hooks locales (repro: `reference-transaction` plantado en el cache disparado por el auto-follow de un tag alcanzable) → corregido: fetch con `core.hooksPath` nulo y `--no-tags`; T15t8 verifica que el hook no corre y que el tag no entra.
  3. `verify_tree` podía escapar con RC 141 (gitlink como primera entrada + árbol grande: el consumidor cortaba temprano, `ls-tree` recibía SIGPIPE y `pipefail` abortaba antes del `die`) → corregido: el listado se captura con rc verificado (vacío ⇒ rechazo) y se consume entero por herestring, sin pipes ni salidas tempranas; T15t6 exige RC 2 con diagnóstico, destino intacto y lock liberado.
- **Ronda 7 (paso A): CHANGES_REQUESTED** (1 punto, aceptado; las tres correcciones de producción de r6 verificadas como documentadas). Resolución:
  1. T15t6 era intermitente (el commit de 2000 archivos disparaba gc/maintenance de git en background y el clone inmediato competía con el repack — el reviewer lo vio fallar 2 de 2 con "git clone falló") → corregido: `gc.auto 0` + `maintenance.auto false` en el fixture antes del commit; evidencia renovada con tres corridas limpias consecutivas de la suite completa (321 ok × 3).
- **Ronda 8: APPROVED (paso A)** — T15t6 estable (el reviewer corrió dos suites completas simultáneas: 321 ok ambas); `install.sh` idéntico a 901c507. Base movida a `1fa8566`. Convergencia del paso A: 4→3→1→0 (r5–r8).
- **Ronda 9 (paso B): CHANGES_REQUESTED** (3 puntos de docs, todos aceptados; el reviewer **reprodujo por su cuenta el camino real contra GitHub** en el tip 93cdf11: initial/update RC 0, marker == HEAD del cache, plantilla nueva instalada, lock liberado, update sin diff). Resolución:
  1. El README afirmaba sin excepción "exit 2 = rechazo sin tocar el destino" → corregido con la misma salvedad de la guía de agentes (instalador interrumpido con aviso ⇒ revisar `git status`).
  2. El camino auditable en dos pasos, seguido desde el toplevel del destino, clonaba axel adentro y ensuciaba el árbol → corregido: el comando clona a un temporal fuera del destino y pasa el toplevel como argumento.
  3. La invocación ilustrativa de `templates/AGENTS.md` omitía el `<repo-destino>` obligatorio → firma completa y puntero al comando del README.
- **Ronda 10: APPROVED (cierre)** — las tres correcciones de r9 verificadas; el reviewer ejecutó literalmente el comando auditable desde un destino (RC 0, clon afuera, plantilla corregida instalada) y dio los criterios 1–6 por cumplidos. Base: `7c1fda3`. Convergencia del ciclo: bajada 6→3→2→0 (r1–r4), paso A 4→3→1→0 (r5–r8), paso B 3→0 (r9–r10).

## Revisiones posteriores

- **Feature 06 (2026-07-28)** — el criterio 2 de este doc ("piped sin `--from` rechaza con mensaje que apunta a `--from`") quedó **superado**: ese camino ahora defaultea la fuente a la URL canónica y el destino al toplevel del cwd, y instala. Lo demás del contrato sigue vigente tal cual, incluida toda la validación del cache, el lock, la disjunción y la taxonomía 0/1/2. El test T15s2 se retiró con su puntero a la cobertura nueva (T16). Detalle: [06-oneliner-defaults.md](06-oneliner-defaults.md).

## Cierre

Feature cerrado el 2026-07-27 con el APPROVED de Codex en la ronda 10. Este commit de cierre es bookkeeping (solo docs): por contrato no mueve la base y lo verifica la ronda 1 del ciclo siguiente (feature 03).
