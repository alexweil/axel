# Pipeline 2026-07-29 (3) — `/build`

## Gate

- tipo: `pipeline`
- protocolo: `spawn-token v1`
- Pedido del humano: «Dejar el repo público de axel presentable para compartirlo: README en inglés escrito para quien lo descubre, licencia MIT, y los docs de onboarding y feedback que faltan.

  Todo lo de abajo son decisiones ya tomadas con el humano y hallazgos ya verificados en una sesión previa. No están registrados en ningún doc del repo — este pipeline es quien los baja a diseño, plan y features.

  **## Alcance: qué NO entra**

  El humano decidió explícitamente que este pipeline se ocupa **solo del repo público**. Fuera de alcance, ya decidido, no re-abrir en el gate:

  - **El fix del instalador para la colisión `build/`** (ver «Problema conocido» abajo). Queda para después; por ahora los primeros colegas agregan la regla a mano y **el README/`docs/install.md` tienen que documentarlo**.
  - Los dos mensajes de rechazo del instalador que dan fricción (también abajo): documentarlos si ayuda, no arreglarlos acá.

  **## Contexto**

  El repo es público (github.com/alexweil/axel, creado 2026-07-27, 0 stars) y `main` está pusheado y sincronizado en `88020af`. Estado del loop: estable, backlog vacío, los once features cerrados.

  Difusión en **dos etapas**: primero entre colegas para feedback, después publicación amplia (HN, Twitter). La vara final es HN; la urgencia inmediata es que un colega entienda qué es esto y pueda instalarlo.

  **## axel ya se instaló en otro repo real — usar esto**

  Se instaló axel en `/Users/alexweil/src/inquirylab` (repo activo, 181 commits, con `docs/` propios ⇒ modo adopción), commit `846308f`, 20 archivos. La adopción quedó pendiente de cerrar con `/adopt` ahí (handoff en `inquirylab/docs/ADOPTION.md`, 21 docs preexistentes listados como candidatos a mapear).

  Esto importa para el README por dos razones:
  1. **Responde la objeción más letal** — "¿funciona en algo que no sea axel mismo?". Hasta ahora axel se había validado solo sobre axel, y `tests/install.sh` es sintético.
  2. **Es la fuente del transcript** de la sección "What a session actually looks like". Un ejemplo sobre un proyecto cualquiera se lee solo; uno de axel diseñándose a sí mismo es recursivo y confuso para quien recién llega. Si `/adopt` ya se cerró en inquirylab cuando corras, hay material real; si no, usar lo que haya y no inventar.

  **Lo que funcionó bien en la instalación real** (no romperlo, y sirve como evidencia): el cache `~/.axel` se auto-actualizó por fast-forward, los anuncios de fuente/destino salieron antes de tocar nada, el rechazo fue fail-closed sin escribir un byte, apareció la línea final `── axel · fin: rc=N · … ──`, los 18 archivos quedaron dentro del diff, el symlink `CLAUDE.md → AGENTS.md` correcto y el handoff `ADOPTION.md` claro.

  **## Problema conocido que el README debe documentar (no arreglar)**

  La skill `/build` vive en `.claude/skills/build/`, y **`build/` es uno de los patrones de `.gitignore` más comunes que existen** — está en el template de Python, Node, Java, Gradle, Maven y C/C++. El preflight del instalador lo detecta y rechaza fail-closed:

  ```
  rechazo: .claude/skills/build/SKILL.md: nacería ignorado por las reglas del destino; nada del instalador puede quedar fuera del diff
  ── axel · fin: rc=2 · rechazo (ver el detalle arriba) ──
  ```

  Ninguna otra skill colisiona (adopt, design, feature, plan, recap, status). El preflight se comporta **correctamente**: el diseño acertó, lo que falla es el nombre de un directorio.

  **Workaround verificado empíricamente**, que es lo que hay que documentar: agregar al `.gitignore` del destino, **después** del patrón `build/`:

  ```
  !.claude/skills/build/
  ```

  Trampa que hay que decir explícitamente porque es contraintuitiva: **`!.claude/` NO funciona** — git no re-incluye un archivo si un directorio padre intermedio quedó excluido. La negación tiene que nombrar el directorio. `!.claude/**` también funciona pero es más amplio de lo necesario.

  Documentarlo como "known issue" con el workaround y con la nota de que se va a arreglar. Es lo primero que le va a pasar a un colega con un repo Python, y sin esto el mensaje de rechazo es críptico.

  **## Otros dos puntos de fricción observados (documentar si ayuda, no arreglar)**

  1. El rechazo por árbol sucio dice `el árbol del destino no está limpio; commiteá o stasheá` pero **no lista qué archivos** están sucios, y **`git stash` sin `-u` no toca untracked** — que es el caso más común en un repo activo. El usuario stashea, sigue rechazado y no entiende por qué.
  2. El instalador anunció `modo: initial` cuando lo que ejecutó fue una **adopción** (detectó 21 docs propios, escribió `ADOPTION.md`, salió con RC=1). `AGENTS.md` documenta tres modos —instalación / adopción / actualización— así que el modo anunciado contradice el ejecutado. Consecuencia visible: inquirylab quedó con `DESIGN.md` e `IMPLEMENTATION.md` en la raíz (suyos) **y** `docs/DESIGN.md` + `docs/IMPLEMENTATION.md` (semillas) — cuatro archivos, dos nombres, sin ningún aviso de cuál manda. Es lo diseñado (las semillas no pisan nada y `/adopt` resuelve), pero nadie lo explica.

  **## Decisiones ya tomadas por el humano**

  - **Idioma**: README y CONTRIBUTING en **inglés** (es la vidriera de GitHub). `AGENTS.md`, `docs/DESIGN.md`, `docs/IMPLEMENTATION.md`, `docs/STATUS.md`, los subdirectorios y los commits **siguen en español**, como manda la convención del repo. No traducir los docs del método.
  - **Licencia**: **MIT**. Hoy falta por completo — el repo es público sin licencia, o sea legalmente "todos los derechos reservados", lo que contradice que su propuesta central sea "instalalo en tu proyecto".
  - **Difusión en dos etapas**: colegas primero, público amplio después.

  **## El activo que hoy no se usa: las métricas del reviewer**

  `.claude/state/rounds-log` (no versionado) tiene **70 rondas de review**: 14 sesiones nuevas de Codex + 56 rondas de resume.

  - `CHANGES_REQUESTED`: **46** (66%)
  - `APPROVED`: **24**
  - Aprobados en ronda 1: **0**
  - Mediana hasta aprobar: **4 rondas**; peor caso: **11**

  Esto responde solo la objeción "el reviewer es un sello de goma que aprueba todo". **Precisión obligatoria**: los 24 APPROVED son *hitos* (bajada fina, implementación), no features — hay más aprobados que features, y enunciarlo mal es exactamente lo que destruye credibilidad en HN. La formulación defendible es "70 rounds, 46 rejections, zero first-round approvals". Como `rounds-log` no está versionado, **extraer las cifras a un doc versionado** para que un tercero pueda auditarlas, y decir de dónde salen.

  Ejemplo concreto y citable de una review que atrapó algo real: commit `74d3f5c` — "tres defectos invisibles en la lectura del texto nuevo".

  **## Posicionamiento acordado**

  El gancho es un hecho verificable, no una promesa: **este repo se construyó a sí mismo con su propio método** — 176 commits en 3 días, 11 features, cada uno con su loop hasta APPROVED, con ledgers de lote y pipeline versionados en `docs/implementation/`.

  Los cuatro diferenciales frente a los cincuenta "AI coding agent" que salen por semana:
  1. Review **cross-vendor** (Claude contra GPT), no un modelo revisándose a sí mismo ni dos instancias del mismo.
  2. El reviewer **ejecuta**: corre tests y builds en un worktree snapshot clavado al commit. Verificación, no confianza.
  3. La memoria es **el repo**, no la ventana de contexto. Sesión nueva = leer tres archivos.
  4. Se construyó a sí mismo y es **auditable**.

  Estructura propuesta para el README (~120 líneas, inglés), a validar y refinar en la unidad de diseño:

  1. Título + una línea + las tres cifras del reviewer.
  2. **The problem** (~6 líneas): un agente solo no se audita a sí mismo y pierde el hilo entre sesiones.
  3. **What a session actually looks like** — transcript real en markdown: pedido sin comando → gate → un `CHANGES_REQUESTED` verdadero → RECAP → OK. Va **arriba**, antes de instalar: es la sección que convierte.
  4. **Requirements, honestly** — Claude Code **y** Codex CLI (dos suscripciones, dos proveedores distintos), macOS (`caffeinate`), git, python3, y que las reviews en xhigh tardan >10 min. Hoy no está declarado en ningún lado.
  5. **Install** — el one-liner, el known issue de `build/` con su workaround, y nada más.
  6. **The six commands** — tabla de una línea cada uno.
  7. **How it works** — el diagrama de flujo de `docs/DESIGN.md` + los 5 principios comprimidos.
  8. **What this is not** — sección de honestidad (ver objeciones abajo).
  9. Links: diseño, contrato de review, cómo dar feedback.

  Toda la casuística actual del README (pipefail, exit codes, forks, corridas interrumpidas, y la sección entera "Para agentes (Claude Code)") **se conserva pero se muda a `docs/install.md`**, referenciada desde el README. Hoy el README tiene 70 líneas y ~50 son eso: está escrito para quien ya decidió usarlo, no para quien lo descubre.

  **## Objeciones de HN que el README debe contestar antes de que aparezcan**

  | Objeción | Respuesta acordada |
  |---|---|
  | "Cuesta una fortuna" | Declararlo de frente en Requirements: dos suscripciones, reviews largas. Es caro y es a propósito. |
  | "El reviewer es otro LLM, ¿por qué confiaría?" | Las métricas + que Codex ejecuta tests, no solo lee el diff. |
  | "Es un montón de markdown, no un framework" | Sí, y es la virtud: sin dependencias, sin lock-in, se lee en una tarde. Decirlo primero uno mismo. |
  | "Está todo en español" | Declararlo sin disculpas: README en inglés, docs del método en español, la maquinaria es agnóstica del idioma. |
  | "macOS only" | Declarar qué se rompe fuera de macOS (`caffeinate`) y qué no. |
  | "¿Funciona en algo que no sea axel mismo?" | Ahora sí hay respuesta: la instalación real en inquirylab. Ser honesto sobre su alcance (un repo, del mismo autor). |

  **## Archivos nuevos previstos**

  `LICENSE` (MIT) · `docs/install.md` (todo lo que sale del README + el known issue) · `CONTRIBUTING.md` en inglés (cómo dar feedback — es lo que el humano busca de esta ronda) · `.github/ISSUE_TEMPLATE/` · un doc versionado con las métricas del reviewer · topics y homepage en el repo de GitHub.

  Sobre demo visual: se descartó asciinema por ahora (las reviews tardan >10 min y habría que editarlo). Empezar con el **transcript en markdown** — cero infra, funciona en móvil, se lee en 20 segundos.

  **## Orden sugerido de features (a acordar en el plan)**

  1. **Vidriera** — LICENSE MIT + README en inglés + `docs/install.md` con el known issue.
  2. **Onboarding y feedback** — CONTRIBUTING + `.github/` + métricas del reviewer versionadas.»

- Modo: `normal`
- Autorizado: 2026-07-29 — «dale, autorizado — el push y los topics los hago yo» · **Ajustes de alcance**: (b) **el pipeline no toca GitHub ni pushea** — confirmada la recomendación del gate: ni `git push`, ni topics, ni homepage, ni ninguna otra acción sobre el repo remoto o sus settings. La unidad `14` deja los comandos exactos listos y los corre el humano al leer el resultado; ejecutarlos desde el pipeline es divergencia ⇒ corte. (a) y (c) **sin ajuste**: la corrección de las cifras del pedido queda como la registró el bloque «Hechos del pedido…», y la pregunta del **origen del transcript** —inquirylab (real pero es un `/adopt`, no el loop completo con `CHANGES_REQUESTED` → RECAP → OK que la sección promete) frente a un loop real de axel— queda **abierta y explícitamente delegada a la unidad de diseño**, tal como se presentó en el gate. Recomendación del padre, no vinculante: transcript **compuesto y declarado como tal** (un loop real de axel para el ciclo de review, la adopción de inquirylab como prueba separada de que funciona fuera de axel).
- Resultado visible al final: el repo público de axel legible para un colega que lo descubre — `README.md` en inglés (~120 líneas, con el transcript real arriba y los requisitos declarados sin maquillaje), `LICENSE` MIT, `docs/install.md` con toda la casuística de instalación más el known issue de `build/` con su workaround, `CONTRIBUTING.md` en inglés, `.github/ISSUE_TEMPLATE/` y un doc versionado con las métricas del reviewer auditables por un tercero. Medida concreta: un colega abre github.com/alexweil/axel, entiende en dos minutos qué es y por qué es distinto, y puede instalarlo sin volver a preguntar.
- gate_base: `39b377ef2560fe4c4dcf3c463c309044bd18fafc`

### Hechos del pedido que el padre verificó al abrir el gate

El pedido trae cifras y estados de una **sesión previa**, y varios se movieron. Se registran acá para que las unidades usen los valores vivos y no los transcriban del literal:

| Lo que dice el pedido | Verificado hoy (`39b377e`) |
|---|---|
| 176 commits en 3 días | **196** commits — 49 el 2026-07-27, 65 el 28, 82 el 29 |
| 11 features cerrados | **12** (el 12 cerró hoy con el pipeline anterior) |
| 70 rondas de review · 46 `CHANGES_REQUESTED` · 24 `APPROVED` · 14 sesiones nuevas + 56 resume | **80** rondas · **53** `CHANGES_REQUESTED` · **27** `APPROVED` · **16** nuevas + **64** resume |
| Aprobados en ronda 1: 0 · mediana 4 · peor caso 11 | ~~**se sostienen**: 0 en ronda 1, mediana **4**, peor caso **11**~~ — **detectado en la review r2 de la unidad `design` y corregido en la r3**: los 0 aprobados en ronda 1 y el peor caso 11 se sostienen, pero la **mediana por ciclo es 4,5**, no 4. El 4 salía de contar el ciclo **parcial** del arranque del `rounds-log` como si fuera completo; sobre los 16 ciclos completos (`2,2,2,2,3,3,3,4,5,5,6,7,7,7,8,11`) la mediana es 4,5. Se tacha en vez de borrarse para conservar la trazabilidad de lo que el padre verificó al abrir el gate. Detalle: [../design/public-surface.md](../design/public-surface.md) §«La unidad de conteo» |
| La adopción de inquirylab quedó **pendiente** de cerrar con `/adopt` | **cerrada** — commit `4908bfb`, `docs/ADOPTION.md` consumido. Hay material real para el transcript |
| El workaround de `build/` es «verificado empíricamente» | **confirmado en un repo real**: inquirylab lo aplicó en el commit `98c70c0`, con la trampa de `!.claude/` explicada en el propio `.gitignore` |

Consecuencia que la unidad de diseño debe resolver, no heredar: **las cifras del reviewer se mueven con cada corrida** — este pipeline mismo va a sumar rondas. El doc versionado de métricas no puede ser un número congelado sin procedencia: necesita declarar el **commit de corte** y el **comando exacto** que las deriva de `.claude/state/rounds-log`, de modo que un tercero pueda auditarlas y una corrida futura re-cortarlas sin reinventar el criterio.

Lo que **no** cambia: la precisión obligatoria del pedido (los `APPROVED` son **hitos** —bajada fina, implementación—, no features; la formulación defendible es «N rounds, M rejections, zero first-round approvals») y que el pedido de fondo sigue en pie tal cual.

## Ruta autorizada

Cuatro unidades, en el orden fijo `design` → `plan` → features. El backlog está vacío y nada de esto está registrado en ningún doc del repo, así que el pedido no tiene dónde entrar sin las dos unidades de delta.

### `design` — design-delta — Superficie pública de axel: posicionamiento, idioma y evidencia auditable

Delta acotado a `docs/DESIGN.md` (+ una profundización en `docs/design/*.md` si el diseño la justifica). Hoy el diseño no dice **nada** sobre la superficie pública: no hay decisión de licencia, ni política de idioma, ni criterio sobre qué va en el README frente a qué va en la guía de instalación, ni tratamiento de las métricas del loop como artefacto auditable. El delta baja a diseño lo que el humano ya decidió y fija lo que falta:

- **Política de idioma en dos planos** — vidriera en inglés (`README.md`, `CONTRIBUTING.md`, `.github/`), método en español (`AGENTS.md`, `docs/*`, commits, y la plantilla del instalador). Es una excepción explícita a la convención vigente de «docs, commits y comunicación en español», así que va como decisión registrada, no como práctica tácita.
- **Licencia MIT** — hoy el repo es público **sin licencia**, o sea legalmente «todos los derechos reservados», lo que contradice de frente la propuesta «instalalo en tu proyecto».
- **Separación vidriera / manual** — el README es para quien **descubre** (posicionamiento, evidencia, requisitos honestos, un camino de instalación); `docs/install.md` es para quien **ya decidió** (casuística completa: pipefail, exit codes, forks, corridas interrumpidas, la guía para agentes, el known issue). Criterio de corte reusable, no un reparto puntual.
- **La evidencia del loop como artefacto versionado** — las métricas viven hoy en `.claude/state/rounds-log`, que **no está versionado**: nadie de afuera puede auditarlas. La decisión de diseño es que la evidencia con la que axel se presenta sea derivable y re-derivable (commit de corte + comando), no una cifra escrita a mano que envejece en silencio.
- **Los cuatro diferenciales y las objeciones** — validar y refinar la estructura de README que el pedido propone (9 secciones, transcript arriba, «What this is not»), incluyendo el alcance honesto de la única instalación externa real (un repo, del mismo autor).
- **Los tres puntos de fricción del instalador** (colisión `build/`, el rechazo por árbol sucio que no lista archivos, el `modo: initial` anunciado en una adopción) entran como **problemas conocidos documentados**, y el diseño decide **dónde** se declaran. Arreglarlos está fuera de alcance por decisión del humano.

**Restricción de alcance**: el delta es sobre la **superficie pública** del repo. Tocar el método, las skills, el instalador o los scripts es divergencia ⇒ corte.

### `plan` — plan-delta — Los dos features de la vidriera en el backlog

Delta acotado a `docs/IMPLEMENTATION.md`: entran **dos** features nuevos —**13** y **14**— con su entrada completa (origen, qué se busca, superficie estimada, preguntas para la bajada, verificación prevista), la posición fijada por pedido humano (criterio 1 del orden) y la extensión que registra la autorización de este gate. El orden interno lo fija el propio pedido: la vidriera primero, porque es lo que desbloquea compartirlo con un colega; onboarding y feedback después.

**Restricción de alcance**: el delta entra como **dos** features, 13 y 14, con el corte de arriba. Partirlos, fusionarlos, o agregar features que el pedido dejó explícitamente fuera —el fix de la colisión `build/`, los dos mensajes de rechazo del instalador— es divergencia ⇒ corte. Si el diseño de la unidad anterior hiciera evidente otro corte, eso es cambio de scope ⇒ corte, no re-corte silencioso.

### `13` — feature — Vidriera: LICENSE MIT + README en inglés + `docs/install.md`

Bajada fina + implementación con su loop de review completo. Sustancia: `LICENSE` (MIT, titular y año a confirmar en la bajada); `README.md` reescrito en inglés para quien descubre el proyecto, con el gancho verificable (se construyó a sí mismo), las cifras del reviewer, el transcript real de una sesión, los requisitos declarados sin maquillaje (dos suscripciones de dos proveedores, macOS, reviews >10 min), un solo camino de instalación, la tabla de los seis comandos, cómo funciona y «What this is not»; y `docs/install.md` con **toda** la casuística que hoy vive en el README —conservada, no resumida— más el known issue de `build/` con su workaround y la trampa de `!.claude/`.

Verificación prevista: no hay harness que compruebe prosa. La evidencia es (a) **cero afirmación no verificable** — cada cifra, cada nombre de archivo y cada comando del README chequeado contra el repo, con las métricas re-derivadas al commit de corte; (b) **cero pérdida** en la mudanza al `docs/install.md` — la casuística actual entra completa y los links no quedan rotos; (c) el one-liner y el workaround **probados**, no transcritos de memoria.

### `14` — feature — Onboarding y feedback: CONTRIBUTING + `.github/` + métricas versionadas

Bajada fina + implementación con su loop de review completo. Sustancia: `CONTRIBUTING.md` en inglés orientado a **qué feedback busca esta ronda** (colegas probándolo, no PRs de código) — cómo reportar que el instalador rebotó, qué información sirve, qué está explícitamente fuera de alcance hoy; `.github/ISSUE_TEMPLATE/` con las plantillas que hagan que ese feedback llegue accionable (candidatos: fallo de instalación, fricción de uso, pregunta); y el **doc versionado de métricas del reviewer** con el commit de corte, el comando de derivación y la formulación defendible (hitos, no features), más el ejemplo citable de una review que atrapó algo real.

Verificación prevista: la misma vara de prosa auditable del 13, más que las cifras del doc de métricas **se re-deriven** en la bajada con el comando declarado y coincidan con lo que el README afirma.

## Estado

| Unidad | Tipo | Estado | SHA inicio | SHA APPROVED |
|---|---|---|---|---|
| `design` | design-delta | **APPROVED — pendiente OK de pipeline** | `77d2f3a` | `780f4a7` (cierre `b748141`) |
| `plan` | plan-delta | **APPROVED — pendiente OK de pipeline** | `150c4f1` | `b0bdf4d` (cierre `8609f49`) |
| `13` | feature | **en curso** — reanudada tras el desempate humano del corte por deadlock (r6–r10) | `284ace4` | — (bajada APPROVED en r5, `61a6c32`) |
| `14` | feature | pendiente | — | — |

## Eventos

- 2026-07-29 — ledger creado; gate presentado al humano (ruta de cuatro unidades, `gate_base` `39b377e`); STATUS a «esperando autorización de pipeline». Se registran además los seis hechos del pedido re-verificados contra el repo (bloque «Hechos del pedido…»), porque tres de ellos se movieron desde la sesión previa.
- 2026-07-29 — **autorización global recibida**, con el ajuste (b): el push y los topics del repo de GitHub los hace el humano — el pipeline no toca el remoto. Las preguntas (a) y (c) del gate quedan sin ajuste; el origen del transcript lo resuelve la unidad `design`. Pipeline en curso.
- 2026-07-29 — **arranque de la unidad `design`** (design-delta, modelo `fable` por §«Modelos por unidad»), `token=CA4F8C90-7E5E-4D15-924B-6807FA16283D`, SHA inicio `77d2f3a`. Pre-arranque verificado: el resumen re-derivado de los docs actuales coincide en sustancia con el autorizado — `DESIGN.md` no tiene fila de decisión sobre licencia, idioma ni superficie pública, su tabla de Componentes no menciona `README`/`LICENSE`/`CONTRIBUTING`/`docs/install.md`, no hay profundización del tema en `docs/design/` (solo `review-contract`, `batch-features`, `implicit-entry`), la convención de `AGENTS.md` sigue diciendo «docs, commits y comunicación en español» sin la excepción de vidriera, y `.claude/state/rounds-log` sigue no versionado. Sin divergencia. Anclas del pipeline limpias antes de acuñar: sin `pipeline-child-token` activo (solo el `retired-E232C306…` del pipeline anterior, que es historia), sin `pipeline-expected`, árbol limpio.
- 2026-07-29 — **modelo `fable` no disponible: unidad `design` relanzada con el modelo de la sesión (`opus`)**. El harness rechazó el spawn con un error de API terminal — sin créditos de uso para Fable 5 en esta cuenta —; **rechazo definitivo verificado, no indeterminado**: el hijo murió sin trabajar y no dejó rastro (árbol limpio, HEAD sin moverse de `6aed00f`, cero commits nuevos, ancla `pipeline-child-token` **pendiente sin reclamar** —sin `.claimed-*`—, sin `pipeline-expected`, y `round` intacto en el `7` que dejó el feature 12). Por eso el relanzamiento **reutiliza el mismo token** sin re-acuñar ni retirar nada, respaldado por el evento de arranque de arriba, que sigue siendo el evento vigente. Causa de **cuenta** (créditos), **no** un renombre de alias: la tabla canónica de §«Modelos por unidad» no cambia. **Segunda vez consecutiva**: el pipeline 2026-07-29 (2) degradó su unidad `plan` por la misma causa — se anota como hallazgo para el RECAP, porque la política canónica pide `fable` para las dos unidades de delta y esta cuenta no puede correrlo hoy. La unidad `plan` de esta corrida, que también es `fable`, va a degradar igual salvo que los créditos cambien. Se anuncia en el RECAP consolidado.
- 2026-07-29 — review r1 de la unidad `design`: `CHANGES_REQUESTED` con siete puntos, todos aceptados y corregidos en r2 (ninguno argumentado). Los dos de fondo: la mediana por ciclo era **4,5** y no 4 —el 4 contaba el ciclo parcial del arranque del log como completo— y al `rounds-log` le faltan **35** rondas anteriores a la instrumentación que los review logs versionados sí registran (total histórico **115**). El punto 7 alcanzó a este ledger: estos eventos estaban fechados `2026-07-30` con commits del `2026-07-29` y en orden invertido — corregidos acá, sin tocar su contenido.

> Nota de fechas: los eventos usan la fecha **local** del repo (la de `git log` y la de los mensajes de commit), no UTC.

- 2026-07-29 — **`APPROVED` de la unidad `design` en la ronda 4** (head `780f4a7`, sin observaciones accionables); cierre en `b748141`. Cuatro rondas, **catorce** pedidos de Codex —7 en r1, 4 en r2, 3 en r3—, los catorce aceptados sin argumentar ninguno. Ninguna decisión del delta se movió por la review: lo que cambió fueron cifras y demostraciones. **Seis correcciones factuales**, y dos de ellas alcanzaron a hechos que **este ledger daba por verificados** al abrir el gate (bloque «Hechos del pedido…»): la mediana por ciclo es **4,5** y no 4 (el 4 contaba como completo el ciclo parcial con que arranca el log), y `rounds-log` **no** es la única evidencia — los rechazos anteriores a la instrumentación ya estaban en los review logs versionados y en el ciclo de plan inicial (30 + 5 = **35** rondas; total histórico **115** contra 80 registradas). Una tercera fue de método y no de dato: la demostración de «cero aprobados en ronda 1» miraba la ronda de **cierre** de cada ciclo, que no prueba nada sobre su r1 — la evidencia correcta es el veredicto de cada primera ronda, verificado en las cinco fuentes. El claim sobrevivió; el argumento, no.
- 2026-07-29 — **verificación del padre al cerrar la unidad**: árbol limpio; anclas del hijo liberadas (`pipeline-expected` y el `claimed-*` borrados — solo queda el `retired-E232C306…` del pipeline anterior, que es historia); los cinco commits del ciclo presentes; y **restricción de alcance cumplida, verificada por diff**: el rango `77d2f3a..b748141` toca únicamente `docs/DESIGN.md`, `docs/STATUS.md`, `docs/design/public-surface.md` y este ledger — cero cambios en el método, las skills, el instalador, los scripts o el remoto. El bloque de procedencia de `public-surface.md` registra la autorización con su fecha, el pedido literal, el path de este ledger y el ajuste de alcance (b).
- 2026-07-29 — **nota de transparencia**: el hijo editó **dos veces** este ledger, que es territorio del padre. (1) En el commit de la r2 corrigió las fechas de los eventos de arranque y degradación —el padre las había escrito en UTC (`2026-07-30`) sobre commits locales del `29`— y su orden invertido, y agregó la nota de fechas de arriba. (2) En el commit de la r3 corrigió la fila «mediana 4 · se sostienen» de la tabla «Hechos del pedido…» del bloque Gate, que seguía afirmando lo falso aunque el evento de la r1 ya registrara la corrección; quedó con el texto original **tachado y conservado**. El padre acepta las dos: ambas estaban dentro del rango bajo review de esta unidad, las dos corrigen afirmaciones del padre que eran objetivamente falsas, ninguna toca el pedido literal del humano ni el alcance autorizado, y la segunda evita que el bloque que el humano leyó en el gate siga contradiciendo al doc. Se listan en el RECAP consolidado porque tocan territorio del padre.
- 2026-07-29 — **arranque de la unidad `plan`** (plan-delta, modelo `fable` por §«Modelos por unidad»), `token=820FCBFF-E522-4313-AC77-521A801E4B8C`, SHA inicio `150c4f1`. Pre-arranque verificado: el resumen re-derivado coincide en sustancia con el autorizado — el backlog de `IMPLEMENTATION.md` sigue **vacío** (cero filas `13`/`14`), y la unidad anterior dejó el diseño de la superficie pública consolidado en `docs/design/public-surface.md`, que es exactamente el insumo que este delta baja a features. Sin divergencia. Anclas limpias antes de acuñar: sin `pipeline-child-token` activo, sin `pipeline-expected`, árbol limpio. Fecha **local** (22:40 del 29), siguiendo la nota de arriba — el padre volvió a escribirla en UTC y la corrigió antes de commitear.
- 2026-07-29 — **modelo `fable` no disponible: unidad `plan` lanzada con el modelo de la sesión (`opus`)** — **tercera degradación consecutiva por la misma causa** (la del pipeline anterior y la de la unidad `design` de esta corrida). Mismo error de API terminal: sin créditos de uso para Fable 5 en esta cuenta. **Rechazo definitivo verificado, no indeterminado**: árbol limpio, HEAD sin moverse de `987dc86`, cero commits nuevos, ancla `pipeline-child-token` **pendiente sin reclamar** (sin `.claimed-*`) y sin `pipeline-expected` — el hijo murió sin trabajar. El relanzamiento **reutiliza el mismo token** sin re-acuñar ni retirar, respaldado por el evento de arranque de arriba, que sigue siendo el vigente. Causa de **cuenta**, no renombre de alias: la tabla canónica no cambia. El padre intentó `fable` igual, teniendo ya evidencia durable de la indisponibilidad, porque el contrato pide que la degradación sea **constatada y registrada**, no presumida. **Consecuencia acumulada que el RECAP debe llevar**: las dos unidades de delta de esta corrida —y la del pipeline anterior— corrieron con `opus`, así que la política de modelos por tipo de unidad hoy **no se cumple en la práctica** para `design`/`plan`. Es decisión del humano: recargar créditos, cambiar la tabla canónica, o convivir con la degradación anunciada.
- 2026-07-29 — **`APPROVED` de la unidad `plan` en la ronda 4** (base `fa12f41`, head `b0bdf4d`, sin observaciones accionables); cierre en `8609f49`. Cuatro rondas, **nueve** pedidos de Codex —4 en r1, 3 en r2, 2 en r3—, los nueve aceptados sin argumentar ninguno. **Ninguna decisión del plan se movió**: Codex acordó desde la r1 el corte en dos features, su orden y los dos no-registros; lo que se movió fue trazabilidad, coherencia interna y precisión. Tres con sustancia propia, y **las tres nacieron de defectos del padre o del hijo, no del plan**: (a) el hijo transcribió el literal del gate **parcial**, argumentando la «fuente única» del contrato editorial — que rige la vidriera publicada, no una cita histórica dentro del plan; corregido con las 117 líneas extraídas del ledger y **verificadas idénticas por diff mecánico** (Codex las re-verificó por SHA-256 tras normalizar los prefijos de blockquote). (b) El **contrato 13→14** que el hijo inventó para cerrar la dependencia hacia adelante que **dejó abierta la ruta autorizada por el padre** era internamente incoherente: el 13 dejaba un link roto a propósito mientras su propio criterio exigía cero links rotos, faltaba una **segunda** dependencia (`CONTRIBUTING.md`, que el README cita y nace en el 14) y no estaba fijada la coincidencia temporal entre el corte de métricas del 13 y el del 14; resuelto como estado intermedio verificable —referencias no activas y marcadas como pendientes, activadas por el 14— sobre **un solo corte**, con la consecuencia declarada (el snapshot no incluye las rondas de los features 13 y 14). (c) En la r3 el hijo **degradó** los «comandos exactos» de GitHub que el gate había autorizado, y la r4 lo revirtió: una unidad `plan-delta` no reescribe una promesa autorizada porque resulte incómoda de cumplir, y diferirla al RECAP no la cumplía ni en sus propios términos (para entonces el 14 ya estaría cerrado sin haber dejado los comandos). La vía que quedó: la **bajada del 14 propone** valores concretos y argumentados y el humano decide, porque es quien los corre.
- 2026-07-29 — **verificación del padre al cerrar la unidad**: árbol limpio; anclas del hijo liberadas (`pipeline-expected` y `claimed-820FCBFF…` borrados); los cinco commits del ciclo presentes; **restricción de alcance cumplida, verificada por diff** — el rango `150c4f1..8609f49` toca únicamente `docs/IMPLEMENTATION.md`, `docs/STATUS.md` y este ledger; y **el corte autorizado se respetó exactamente**: entraron **dos** features, `13` y `14`, sin partirlos ni fusionarlos y sin ninguna entrada de backlog para lo que el gate dejó afuera (la colisión `build/` del instalador y sus dos mensajes de rechazo siguen sin registrar). El párrafo de extensión registra la autorización con su fecha, el literal del humano y el path de este ledger.
- 2026-07-29 — **arranque de la unidad `13`** (feature, modelo `opus` por §«Modelos por unidad» — coincide con el de la sesión, así que en esta unidad no hay degradación posible), `token=E3A3E0A2-9F7A-4A73-914B-B8A6DBDAC5C8`, SHA inicio `284ace4`. Pre-arranque verificado: el resumen re-derivado de la §13 que el plan-delta acaba de escribir coincide en sustancia con el autorizado en la ruta —`LICENSE` MIT, `README.md` reescrito en inglés para quien descubre, `docs/install.md` con la casuística completa más el known issue de `build/`, y la verificación en tres criterios (cero afirmación no verificable, cero pérdida y cero link roto, one-liner y workaround probados)— y lo que el ciclo de review agregó son precisiones que **acotan**, no amplían: el contrato entre features con las dos referencias no activas, el corte único de métricas y los tres hallazgos del delta. Sin divergencia. Anclas limpias antes de acuñar: sin `pipeline-child-token` activo, sin `pipeline-expected`, árbol limpio.

- 2026-07-30 — **anomalía de supervisión: el hijo reportó un `id` de review que no existe, y el watcher del padre expiró sobre una unidad sana.** El hijo cerró su turno con `REVIEW LANZADA id=13:r7:0FCE45B2-1D0F-4CD3-B01D-D6096C1D6CB4 head=7540ca1`, el padre armó el watcher con ese id, y a los 45 minutos disparó el timeout — que es la **condición de corte 1**. **No se cortó**, y esta es la justificación, con la evidencia que la sostiene: el desenlace **sí había llegado** (01:04:23 local, ~40 min antes del timeout) bajo el id `13:r7:A781CB34-882D-4FA2-BF85-C46330454A4B`. Ese id **no es residuo de otra invocación** —que es el caso que el contrato manda ignorar—, sino el **auténtico**, y lo prueba el estado de la máquina, no la prosa de nadie: `pipeline-expected`, que escribe `review.sh` al lanzar con el `AXEL_REVIEW_ID` que efectivamente recibió, dice `id=13:r7:A781CB34…` · `unit=13` · `head=7540ca1…` · `phase=launched`; `review-terminal` trae ese mismo id y ese mismo head con `CHANGES_REQUESTED`; el `rounds-log` tiene **una sola** fila `round 7`, intento **1** (no hubo `PROC_FAIL` ni reintento, así que la duplicidad de ids no se explica por un retry); y HEAD estaba en `7540ca1` con el árbol limpio. El id que el hijo reportó **no aparece en ningún archivo de estado**: fue un error de reporte suyo, no un fallo de la review. Cortar habría descartado una unidad viva con veredicto entregado por un defecto de bookkeeping del hijo, que es lo contrario de lo que la condición 1 protege (una review muerta o colgada). El padre empujó con el id **auténtico**, tomado de `pipeline-expected`.
  - **Hallazgo de contrato para el RECAP** (segundo de la corrida, junto con el de los modelos): la supervisión del padre depende de un identificador que el hijo transcribe **en prosa** en su línea de cierre de turno, mientras que la fuente autoritativa —`pipeline-expected`— ya está en disco y la escribe la maquinaria. Un hijo que reporta mal ese id hace que el padre espere el timeout completo y después **corte una unidad sana**. El arreglo obvio —que el watcher tome la identidad de `pipeline-expected` y use la línea del hijo solo como confirmación— **no se implementa acá**: tocar la skill `build` es método, y está fuera de la ruta autorizada de este pipeline. Queda registrado como deuda, sin entrada de backlog, para que el humano decida.
- 2026-07-30 — **la anomalía se repitió en la r9, y el padre la dejó pasar teniendo el dato a la vista**. El hijo declaró `id=13:r9:BA8DDAAB-1D2F-4A61-8B71-42B1F5EDCF6B` **después de haberse comprometido explícitamente a leer el id de `pipeline-expected`**; el auténtico era `9A88D372-60E4-4DAC-8E28-D2FE7137C6EC` (coincidente con `review-terminal`, head `30766b3`, `rounds-log` con una sola fila r9 en el intento 1). Segunda ocurrencia en la misma unidad. **Y el fallo del padre fue peor que la primera vez**: tras el episodio de la r7 había agregado al watcher un `cat` de `pipeline-expected` como diagnóstico — así que el id auténtico quedó **impreso en la primera línea de su propia salida** y el timeout igual corrió los 45 minutos completos, porque el padre lo **mostró en vez de compararlo**. La corrección aplicada en el acto: el watcher **deriva la identidad de `pipeline-expected`** y usa la línea del hijo solo como confirmación, avisando si difieren. Refuerza el hallazgo de contrato de arriba con dos ocurrencias reproducidas y una lección extra: un diagnóstico que solo se imprime no es una guarda — la diferencia entre observar y verificar es la misma que este ciclo lleva nueve rondas persiguiendo en los artefactos, ocurriendo ahora en la supervisión.

- 2026-07-30 — **CORTE de la unidad `13` por deadlock de 5 rondas** (condición 1). El ciclo de **implementación** salió `CHANGES_REQUESTED` en r6, r7, r8, r9 y r10 — racha **5**, el tope del contrato—, así que la corrida se detiene y el desempate es del humano. El ciclo de **bajada** de la misma unidad había cerrado `APPROVED` en la r5 (`61a6c32`); ese ciclo no se revierte.
  - **No es un deadlock de desacuerdo, y decirlo importa para el desempate**: el hijo aceptó **los 28** pedidos de Codex a lo largo de las diez rondas **sin argumentar ninguno** —~~«los veintinueve»~~, corregido el 2026-07-30 por el hallazgo de la r11: la derivación cerrada contra los encabezados de ronda del review log da bajada r1–r4 = 3+3+4+3 = **13** e implementación r6–r10 = 4+4+4+1+2 = **15**, total **28**; el padre publicó 29 sin derivarlo, en el mismo evento que reprocha cifras no derivadas—, y la r9 ya había vuelto con un único punto y **cero observaciones de preferencia**. El tope se alcanzó por agotamiento de rondas sobre un ciclo que convergía, no por posturas irreconciliables. **Postura de ambos agentes**: coinciden — Codex verificó y dio por cerrados alcance, tokens (45/45), links, anclas, las 15 secciones, la matriz de templates, los tamaños, el 35,0 %, `lint`, `loop.sh` 287/0 e `install.sh` 460/0, y declaró no tener observaciones de preferencia; el hijo no sostiene ninguna posición contra él.
  - **Los dos puntos que quedaron abiertos son de bookkeeping, no del entregable**: (1) el criterio C14 publica comandos de evidencia de alcance cuyos resultados ya no coinciden con el texto —dice «seis archivos, un commit del padre» y hoy devuelven **siete y tres**—, y (2) una mitigación quedó diciendo que C6–C9 cubren **14** secciones cuando el manual final tiene **15**. Codex verifica explícitamente que **el alcance real sigue siendo válido**: los siete paths están autorizados y los tres commits del ledger son del padre.
  - **Causa del punto (1): el propio padre.** Los dos commits con que registró la anomalía del id (`10e8f1f` y `a0e9fa8`) entraron **a mitad del ciclo** de la unidad y movieron el rango que la evidencia C14 del hijo describía. El hijo había escrito esa evidencia cuando el rango era de seis archivos y un commit del padre. El padre commiteó al ledger durante un ciclo abierto sabiendo que el rango del hijo lo incluiría —lo dio por deseable, para que la anomalía fuera visible en la review siguiente— pero **no anticipó que invalidaría una evidencia ya publicada por el hijo**, y ese efecto consumió una de las cinco rondas del tope.
  - **Estado al cortar**: árbol **limpio** (el hijo respetó el handshake, no hay suciedad que preservar ni que absorber, así que el commit del corte no necesita restricción por pathspec); HEAD `c826776`; `pipeline-expected` en `phase=launched` con el desenlace de la r10 **sin consumir**; ancla `claimed-E3A3E0A2…` vigente. Las unidades `design` y `plan` quedan **intactas** en «APPROVED — pendiente OK de pipeline». La unidad `14` **no se arrancó**.
  - **Los tres artefactos existen y están completos** (`LICENSE`, `README.md`, `docs/install.md`): lo que falta es sincronizar dos conteos de su propio doc de bajada.

- 2026-07-30 — **desempate humano: «a)»** — el humano eligió la primera de las tres salidas que le presentó el RECAP de corte: **autorizar las dos correcciones de bookkeeping y reanudar la unidad `13` con la racha reseteada**. **No es el OK consolidado**: las unidades `design` y `plan` siguen en «APPROVED — pendiente OK de pipeline», el pipeline sigue abierto y el OK que cierra llega con el RECAP consolidado del final.
  - **Alcance de la reanudación, acotado**: solo los **dos puntos** que la r10 dejó abiertos —la evidencia de alcance de C14, que debe demostrar **el estado real, sea cual sea** (al momento del desempate: siete paths autorizados y tres commits del padre sobre el ledger; las cifras de esta línea describen ese instante y no son el criterio — el criterio es que C14 no publique un número que se contradiga al ejecutarlo) en vez de una derivación que se contradice al ejecutarla, y la mitigación que dice 14 secciones donde el manual tiene 15—. **No** es una reapertura del feature: los artefactos ya verificados no se rediscuten, y ampliar más allá de esos dos puntos es divergencia ⇒ corte.
  - **Racha reseteada por el camino contractual**: `scripts/review.sh reset-deadlock`, que el propio script documenta como «rearma la racha tras un desempate humano» — no se editó el contador a mano. La numeración de rondas **no** se reinicia: la próxima es la r11.
  - **Compromiso del padre, que corrige la causa del punto (1)** —enunciado como **regla**, corregido el 2026-07-30 en la r14—: el padre **no commitea a este ledger durante el ciclo de la unidad `13`, con una sola excepción: corregir una falsedad vigente**; y cuando la excepción aplica, el commit se hace de inmediato y **su SHA se incorpora a la lista cerrada de `AUTORIZADOS` de C14 en la misma ronda**, de modo que el criterio siga siendo fail-closed y el doc deje de ser falso a la vez. Los eventos que no son correcciones se registran **al cerrar la unidad**.
    - ~~Enunciado original: «el padre **no vuelve a commitear a este ledger mientras el ciclo esté abierto**… Los eventos que falten se registran al cerrar la unidad. El hijo escribe su C14 sobre una base que a partir de acá solo mueve él.»~~ Quedó **falso** con `f6fce39` (la corrección del «veintinueve» → 28), y la falsedad no fue el commit sino el enunciado: el padre había prometido un absoluto que el propio contrato le impide cumplir, porque **dejar en pie una falsedad vigente no es una opción disponible**. Codex lo dictaminó al negarse a aprobar un doc que sabía falso, y la salida —commit ahora, SHA a la lista en la misma ronda— la propuso el hijo. El padre había razonado al revés: trataba «no romper C14» como la restricción dura y la falsedad del ledger como algo diferible, cuando **el doc falso es el defecto y C14 es la alarma que lo detecta**; diferir la corrección para no hacerla sonar es apagar la alarma.
    - Nota sobre la lista de `AUTORIZADOS`: que haya crecido es el **mecanismo funcionando**, no un costo. Sin la lista cerrada, `f6fce39` habría pasado inadvertido; con ella, obligó a una actualización explícita y verificable, con prueba negativa que falla si el SHA no se declara.

## Cierre

- **CORTE por deadlock, 2026-07-30 — resuelto por desempate humano el mismo día**: el humano autorizó la salida (a) y la corrida **se reanuda** en la unidad `13`. El corte queda registrado arriba como parte de la historia de la corrida, no como su final. Sin OK consolidado todavía.
