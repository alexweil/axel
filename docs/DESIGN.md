# axel — Diseño

> Este doc es el diseño a gran escala. Las profundizaciones por tema viven en `docs/design/*.md` y se referencian desde acá. La posición actual del trabajo está en [STATUS.md](STATUS.md); el plan, en [IMPLEMENTATION.md](IMPLEMENTATION.md).

## Objetivo

Maquinaria reusable para desarrollar proyectos con dos agentes en loop — un **generador** (Claude Code) y un **reviewer** (Codex) — iterando sobre cada pieza hasta acuerdo, con la documentación como memoria persistente y checkpoints de OK humano. Sirve para software o cualquier contenido generable por agentes. axel se desarrolla a sí mismo con su propio método.

## Principios

1. **El estado vive en el repo, no en el chat.** Toda sesión (nueva, local o remota) se reconstruye leyendo AGENTS.md → STATUS.md → DESIGN/IMPLEMENTATION. El contexto de chat se descarta entre features sin perder nada.
2. **La sesión de Claude Code es el proceso y el panel de control.** El loop corre adentro de la sesión, no en un orquestador externo: el humano la abre —incluso remota— y puede preguntar o desviar en cualquier momento, porque la sesión tiene el contexto vivo.
3. **Generador y reviewer separados, cada uno con su contexto.** Claude genera y commitea; Codex revisa con capacidad de ejecutar para verificar por su cuenta. Cada uno mantiene su contexto durante un feature (sesión de chat / resume de Codex) y lo renueva al cambiar de feature.
4. **El OK humano es la frontera de contexto.** RECAP → OK → sesiones frescas para el siguiente feature. El humano no dirige el detalle: valida dónde estamos, cómo venimos y qué sigue. En modo lote o pipeline la validación se agrupa — autorización del gate al inicio, OK consolidado que cierra al final — y la frontera de contexto la aporta la maquinaria: subagente fresco por unidad ([design/batch-features.md](design/batch-features.md), [design/implicit-entry.md](design/implicit-entry.md)).
5. **Los docs se actualizan en cada commit.** Si no quedó registrado, no pasó. El delta de docs es lo que el reviewer usa para saber qué verificar.

## Componentes

| Pieza | Qué es |
|---|---|
| `AGENTS.md` + symlink `CLAUDE.md` | Contexto raíz que ambos agentes cargan automáticamente: proceso, reglas, mapa de docs. Una sola fuente. |
| `docs/DESIGN.md` + `docs/design/*` | Diseño a gran escala y profundizaciones por tema. |
| `docs/IMPLEMENTATION.md` + `docs/implementation/*` | Plan priorizado con estado por feature; bajada fina y review log de cada uno. |
| `docs/STATUS.md` | La posición actual en ~10 líneas; se actualiza en cada commit. |
| `.claude/skills/` | Las fases del método como comandos: `/design`, `/plan`, `/feature`, `/build`, `/status`, `/recap` — con descripciones-trigger: el modelo puede despacharlas por contexto, sin comando explícito ([design/implicit-entry.md](design/implicit-entry.md)). |
| `scripts/review.sh` | Wrapper del reviewer: config de modelo, ciclo de vida de sesiones (new/resume), rango de commits, contrato de veredicto. Detalle: [design/review-contract.md](design/review-contract.md). |
| `.claude/state/` | Estado local no versionado: session id de Codex, SHA del último APPROVED, contador de ronda, racha y el registro de métricas `rounds-log`. |
| `.claude/settings.json` | Permisos preaprobados para que el loop no se frene en confirmaciones mientras el humano no está. |
| `scripts/install.sh` + `templates/` | Instalador: lleva la maquinaria a otro repo (instalación, adopción de proyectos con proceso manual previo, actualización con axel como fuente de verdad). Detalle: [implementation/01-installer.md](implementation/01-installer.md). |
| `/adopt` + `docs/ADOPTION.md` | Cierre de adopción en el destino: el instalador deja el handoff persistente con los hallazgos y la skill lo consume con el humano (mapear docs, derivar el STATUS real). |
| `README.md` · `LICENSE` · `CONTRIBUTING.md` · `.github/` · `docs/install.md` + la evidencia del loop + el doc del render de la sesión ([session.md](session.md), desde el delta 2026-08-05; path elegido por la bajada del feature 15) | **Superficie pública**: la vidriera para quien descubre el proyecto y el manual para quien ya decidió usarlo. Todo salvo el snapshot crudo va en inglés, por audiencia — el doc del render incluido; la evidencia son **dos** piezas — el **informe** de métricas (prosa, vidriera, inglés) y el **snapshot crudo** del `rounds-log` (dato, neutro de idioma). Detalle: [design/public-surface.md](design/public-surface.md). |

## El flujo

```
/design ─► DESIGN.md ─review─► RECAP ─► OK ─► /plan ─► IMPLEMENTATION.md ─review─► RECAP ─► OK
                                                              │
                 ┌────────────────────────────────────────────┘
                 ▼
        /feature (sesión limpia)
        gate de arranque: resumen ─► confirmación humana
        bajada fina ─review─► implementar ─commit─► review ─► … ─► APPROVED
                 │
                 ▼
              RECAP ─► OK humano ─► siguiente /feature (sesión limpia)
```

Dentro de un feature: cambio → commit → review → corregir o argumentar → commit → review… hasta `VERDICT: APPROVED`. Tope de 5 rondas sin convergencia → RECAP temprano para que desempate el humano.

**Modo lote** (`/feature all` o `/feature NN..MM`): varios features pendientes en una sola corrida — gate de lote (N resúmenes → una autorización global, con exclusiones puntuales) → un subagente por feature en secuencia, cada uno con su loop de review completo, supervisado por la sesión orquestadora; cada feature aprobado queda «APPROVED — pendiente OK de lote» → RECAP consolidado → OK humano que cierra todo. Estado durable en un ledger versionado por corrida. Detalle: [design/batch-features.md](design/batch-features.md).

**Entrada implícita**: la maquinaria se puede usar sin conocer los comandos. Un pedido sin comando se **rutea por contexto** (descripciones-trigger de las skills + tabla de despacho sobre STATUS.md), sin arrancar nunca trabajo sin un punto de confirmación. Un pedido que cruza dos o más fases va a **`/build`** — pipeline que encadena los deltas necesarios (design → plan → features) con gate único al inicio, un subagente fresco por unidad (el patrón del lote generalizado a unidades tipadas) y RECAP consolidado cuyo OK cierra. En proyecto virgen, `/build` es el camino POC-first: visión → diseño borrador → plan con feature 01 = esqueleto que camina → POC visible. Detalle: [design/implicit-entry.md](design/implicit-entry.md).

## Decisiones

| Fecha | Decisión | Elección | Por qué |
|---|---|---|---|
| 2026-07-27 | Alcance del repo | axel ES la maquinaria | Se desarrolla a sí misma; llevarla a otros proyectos es un feature (instalador). |
| 2026-07-27 | Orquestación | Loop adentro de Claude Code; Codex como subproceso | La sesión es lo que el humano consulta remoto; un orquestador externo la dejaría ciega. |
| 2026-07-27 | Git | `main` lineal, un commit por paso, sin amend | La historia ES el registro del loop; el reviewer ve deltas por rango de commits. |
| 2026-07-27 | Rol del reviewer | Puede ejecutar (workspace-write), no modifica | Verificación independiente: no confía en la evidencia del generador. |
| 2026-07-27 | Config de modelos | Variables al tope de `review.sh` (+ env `AXEL_REVIEW_*`) | Cambiar de modelo o esfuerzo = tocar una línea versionada, sin depender de config global. |
| 2026-07-27, refinada 2026-07-28 (feature 04) | RECAP y esperas | El turno termina y la sesión queda esperando; el aviso sigue el criterio de autonomía (espera alcanzada por trabajo autónomo → push de una línea; respuesta directa → sin push) y tras un OK que cierra fase o feature se facilita la sesión siguiente (chip de spawn / instrucción única). Protocolo: skill `recap` y skills de fase. | El OK puede llegar desde una sesión remota en cualquier momento; presencia no se adivina — se decide por cómo se llegó a la espera. |
| 2026-07-27 | Instalador | Payload sobreescribible vs. semillas intocables; modos por marker; adopción = script mecánico + `/adopt` semántico; git + preflight como red (fail-closed) | El re-run es la actualización sin pisar lo del proyecto; nada del instalador queda fuera del diff ni se decide adivinando. Detalle: [implementation/01-installer.md](implementation/01-installer.md). |
| 2026-07-28 | One-liner corto del instalador | Defaults **solo en el camino de bootstrap** (fuente ⇒ URL canónica cuando el script corre piped; destino ⇒ toplevel del cwd), anunciados antes de actuar, con guard del destino asumido que es la propia fuente; el modo local no cambia. Finalización verificable: único terminal `finish`, línea final `── axel · fin: rc=N` y RC no contractual para la incompletitud del delegado | Un `curl … \| bash` no puede llevar argumentos, y sin `pipefail` no distingue "falló" de "nunca corrió": los defaults declaran la ambigüedad en vez de exigirla y la línea final la hace detectable. Detalle: [implementation/06-oneliner-defaults.md](implementation/06-oneliner-defaults.md). |
| 2026-07-28 | Gate de arranque de feature | `/feature` presenta un resumen derivado de los docs y espera la confirmación humana antes de la bajada fina; estado persistido en STATUS («esperando confirmación de arranque») con re-presentación al reabrir | Validar el rumbo cuando el humano está presente por construcción, antes de gastar bajada y rondas de review; el OK final de integración no se reemplaza. Detalle: [implementation/05-feature-gate.md](implementation/05-feature-gate.md). |
| 2026-07-28 | Batch de features | `/feature all` y `/feature NN..MM` (el `/feature` solo no cambia): orquestador supervisor + un subagente por feature; gate de lote = autorización de ejecución (N resúmenes, exclusiones puntuales), cada feature aprobado queda «APPROVED — pendiente OK de lote», el OK del RECAP consolidado cierra; ledger versionado por corrida con reentrada fail-closed; señal terminal atómica con identidad de invocación en `review.sh` — el APPROVED de Codex por feature no cambia | La frontera de contexto la resuelve el subagente fresco; el spike mostró que el subagente no se re-invoca al terminar su background → el padre supervisa la señal terminal y lo empuja por mensaje, sin transportar feedback. Detalle: [design/batch-features.md](design/batch-features.md). |
| 2026-07-29 | Entrada implícita | Nivel 1: ruteo por contexto — descripciones-trigger en las skills + tabla de despacho sobre STATUS.md, sin arrancar trabajo sin punto de confirmación (gates existentes; `/plan` ruteado gana confirmación liviana); el opt-in del lote se reinterpreta (la autorización es el gate, no el tipeo). Nivel 2: `/build` — pipeline por pedido que encadena las fases necesarias como unidades tipadas (design-delta, plan-delta, feature) con los contratos del lote: gate único, hijo fresco por unidad, ledger, «APPROVED — pendiente OK de pipeline», OK consolidado que cierra; POC-first (proyecto virgen, docs borrador, feature 01 = esqueleto) como caso particular | Bajar el costo de entrada (usar la maquinaria sin conocer comandos) y acortar el camino de la visión del humano a un resultado visible sobre el que iterar; el control humano no se pierde — se concentra en gates y OK consolidado. Detalle: [design/implicit-entry.md](design/implicit-entry.md). |
| 2026-07-29 | Modelo de los subagentes | Dos planos: el de la **sesión** lo elige el humano; el de los **hijos** lo **fija la maquinaria** por tipo de unidad y no se hereda — `fable` para los que corren `design`/`plan`, `opus` para los que corren `feature` (lote y pipeline). Alias de familia, no IDs de API; tabla canónica en la skill `build` §«Modelos por unidad», override puntual por gate, y degradación **anunciada** (evento en el ledger + línea en el RECAP) si el harness rechaza el modelo — nunca corte, salvo spawn de resultado indeterminado. Revierte la decisión de no cablearlos del feature 10, que queda registrada | Cada tipo de unidad tiene un perfil de trabajo distinto y elegirlo a mano en cada corrida es fricción que se olvida; el alias de familia no envejece como un ID versionado, y la degradación anunciada evita que la preferencia de axel se vuelva requisito duro de un destino. Detalle: [implementation/11-subagent-models.md](implementation/11-subagent-models.md). |
| 2026-07-29 | Cierre de `/adopt` | El turno termina con un **inventario derivado del commit** (`git show --name-status -M`), no de la memoria de la sesión: una línea por entrada, en **dos bloques** — mecánico (lo que mandaba el handoff) y para ratificar (texto que la sesión escribió sobre el proyecto ajeno, archivos fuera de la memoria del loop, contenido reescrito), con la duda del lado de ratificar. El mecanismo queda escrito como **contrato genérico y reusable** dentro de la skill; `AGENTS.md` y su plantilla no acompañan (no describen los pasos internos de la fase, y la semilla no llega a destinos ya instalados) | El humano no mira el diff: lee el mensaje con que la sesión cierra el turno, y sin un paso que lo exija cada corrida improvisa qué contar — la primera adopción real tocó 8 archivos y el reporte habló de 3. Derivarlo de git saca la **completitud** del juicio del agente, que es lo único que no puede fallar. Detalle: [implementation/12-adopt-close-report.md](implementation/12-adopt-close-report.md). |
| 2026-07-29 | Superficie pública: idioma, licencia y corte vidriera/manual | Idioma **por audiencia**, en tres planos — vidriera en inglés (`README.md`, `CONTRIBUTING.md`, `.github/`, `docs/install.md`, el informe de métricas), método de este repo en español (`AGENTS.md`, `docs/*`, commits), y la **prosa instalada en el destino** —skills, contrato, los mensajes que los scripts imprimen y las cuatro plantillas documentales— en español **como limitación declarada** (el plano se define por prosa, y la distinción corre dentro de un mismo archivo: la **lógica ejecutable**, la policy, el `settings.json` y el symlink no tienen idioma; las cadenas que el usuario lee, sí)—; licencia **MIT**; y corte README/manual por un test aplicable línea a línea: al README solo entra lo que vale para todos, toda frase que empiece con «si…» describe un caso y los casos van al manual, que es el completo **en el dominio operativo** — el README puede llevar un puntero, nunca el caso. Toda afirmación publicada es hecho derivable, limitación declarada u opinión marcada | El repo es público **sin licencia** —legalmente «todos los derechos reservados»— lo que contradice de frente «instalalo en tu proyecto»; y el README está escrito para quien ya decidió usar axel, no para quien lo descubre (69 líneas, unas 50 de casuística). El idioma sale de la audiencia y no de la ubicación en el árbol: `docs/install.md` es la última milla del embudo, y un corte de idioma justo en el punto de conversión es el peor lugar donde ponerlo. Es una **excepción explícita** a la convención «docs, commits y comunicación en español»; corregir esa línea de `AGENTS.md` es tocar el método y la ruta autorizada no lo incluye, así que el pipeline **termina con esa deuda normativa declarada** en vez de ampliar el scope en silencio. Detalle: [design/public-surface.md](design/public-surface.md). |
| 2026-07-29 | La evidencia del loop como artefacto | **Foto fechada y versionada, no contador vivo**: snapshot de `rounds-log` bajo `docs/` (sin desversionar `.claude/state/`, que el loop escribe en cada ronda) + **commit de corte** + **comando de derivación** que corre con el toolchain de stock, con **fuente única** para todo número de la vidriera y contra-chequeo contra los SHA de una historia verificablemente lineal. La **unidad** se declara siempre —ronda (80 registradas) / hito (27) / ciclo (16 completos observables) al corte `e1e1282`—, la **ronda es la del contrato y no la fila del log** (dedup por ciclo y número, porque un reintento deja `PROC_FAIL` antes del veredicto), y los `APPROVED` son **hitos**, no features | Las métricas viven en `.claude/state/rounds-log`, que **no está versionado**: nadie de afuera puede auditar lo que no puede leer, y una cifra tipeada a mano envejece en silencio con cada corrida. Tres hallazgos obligan a fijar el criterio y no solo el número: «cero aprobados en ronda 1» es **cierto por ciclo y falso por hito** (hay uno, `2dbbdfc`) y la mediana por ciclo es **4,5** y no 4 —el 4 salía de contar como completo el ciclo parcial del arranque—; el log **arranca el día 2**, pero el faltante es recuperable de dos fuentes que se citan por separado — **30** rondas en los review logs de features y **5** del ciclo de plan en commits y STATUS histórico ⇒ **115** históricas contra 80 registradas, así que se publican las dos cifras rotuladas y no un piso mudo — y el «cero en ronda 1» se demuestra con el veredicto de **cada r1**, no con la ronda de cierre del ciclo; y las cifras **se movieron durante la propia review de este delta**, que es la demostración en vivo de por qué la evidencia es una foto con corte. Detalle: [design/public-surface.md](design/public-surface.md). |
| 2026-07-29 | El transcript de la vidriera | **Dos bloques citables, cada uno con su procedencia — no una sesión mezclada**: la sesión completa renderizada desde el repo —los cinco hitos que la sección promete (pedido sin comando, gate y autorización, el `CHANGES_REQUESTED` verdadero, la corrección hasta el `APPROVED`, y RECAP → OK), cada uno con su fuente durable; un pipeline los tiene todos en su ledger— y, al lado, la adopción real de inquirylab rotulada como lo que es — un repo, del mismo autor, una adopción y no el loop completo. **Ninguna línea inventada** | Pregunta que el gate del pipeline delegó explícitamente a la unidad de diseño. En axel el chat es efímero y la memoria es el repo: **no hay transcript de sesión que capturar**, y publicar un log de chat con aspecto de reconstruido, en un proyecto cuya propuesta es la auditabilidad, es lo más dañino que podría hacer. Renderizarlo desde el repo no es un sustituto — es exactamente lo que una sesión deja atrás. Separarlo en dos bloques resuelve además la recursión (axel sobre axel es el gancho, no un defecto) sin fingir que una adopción prueba el ciclo de review. Detalle: [design/public-surface.md](design/public-surface.md). |
| 2026-08-05 | Estructura del README: un párrafo y cuatro bloques | El README contesta las preguntas del lector en su orden: **párrafo de apertura** que dice para qué sirve y por qué usarlo —sin vocabulario interno del método, sin cifras, sin SHAs—, y **cuatro bloques**: ¿por qué lo usaría? · ¿cómo lo instalo? · ¿cómo lo uso? · lo esencial. Las métricas dejan la apertura: mención única más abajo, con la unidad en la misma oración y `docs/metrics.md` como fuente única; el glosario de unidades no vuelve al README. El render de la sesión se muda entero a un doc propio de la vidriera (path: bajada del 15); «What this is not» se comprime en «lo esencial» sin despublicar ninguna limitación declarada; requisitos antes del comando, corte vidriera/manual, contrato editorial e idioma quedan como estaban. Revierte el refinamiento «cifras en el título» del delta 2026-07-29. **Procedencia**: unidad `design-delta` del pipeline `/build` del 2026-08-05, autorizado por su gate ese día — ledger: [implementation/pipeline-2026-08-05.md](implementation/pipeline-2026-08-05.md); pedido literal del humano (saltos de línea marcados con «/»): «Revisa el README y hacelo FACIL. El usuario debe entender en un parrafo para que sirve, y por qué. No se por que en el README de hoy se habla de commits, y despues habla de rounds y no se que más. POR QUE ESTA DICIENDO ESTO? deberia ser simple, deberia ser: / Para que sirve? Por Que lo usaria? / Como lo instalo? / Como lo uso? / Lo esencial» | El README vigente abría con la línea de métricas y su glosario — el contexto más caro del doc, cobrado en la primera línea a un lector que todavía no sabe qué es axel; el pedido del humano es la factura de ese costo. La estructura por preguntas pone cada término después de definirse y cada cifra con su contexto ya pagado. Detalle: [design/public-surface.md](design/public-surface.md) §«Estructura del README». |

## Profundizaciones

- [design/public-surface.md](design/public-surface.md) — superficie pública: idioma por audiencia, licencia, corte vidriera/manual, evidencia versionada, transcript y problemas conocidos.
- [design/review-contract.md](design/review-contract.md) — contrato generador↔reviewer: transporte, prompt, sesiones, veredictos, deadlock.
- [design/batch-features.md](design/batch-features.md) — modo lote de `/feature`: gate de lote, contrato padre↔hijo, condiciones de corte, evidencia del spike.
- [design/implicit-entry.md](design/implicit-entry.md) — entrada implícita: ruteo por contexto (nivel 1) y pipeline `/build` (nivel 2), con POC-first como caso particular.
