# STATUS

- **Fase**: **pipeline `/build` en curso** — autorizado el 2026-07-29 · ledger: [implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md) (`gate_base` `39b377e`)
- **Unidad `design`**: **APPROVED — pendiente OK de pipeline** (r4, base `780f4a7`) — superficie pública de axel: posicionamiento, política de idioma, licencia y evidencia auditable. Delta en [design/public-surface.md](design/public-surface.md) + tres filas de decisión y la fila de componentes en [DESIGN.md](DESIGN.md). Corrió con `opus` (degradación anunciada: `fable` rechazado por falta de créditos en la cuenta; registrada en el ledger)
- **Unidad `plan`**: **APPROVED — pendiente OK de pipeline** (r4, base `fa12f41`, head `b0bdf4d`, sin observaciones accionables) — el backlog **deja de estar vacío**: entran los features **13** (vidriera: LICENSE MIT + README en inglés + `docs/install.md`) y **14** (onboarding y feedback: CONTRIBUTING + `.github/` + métricas versionadas), en ese orden, con el pedido literal del gate transcrito completo y el **contrato entre features** que cierra las dos referencias hacia adelante del README sobre **un solo corte** de métricas: el 13 deja referencias no activas y marcadas —cero links rotos—, y el 14 elige los paths, reconstruye ese corte, crea los artefactos y las activa. Corrió con `opus` (tercera degradación anunciada consecutiva: `fable` rechazado por falta de créditos; registrada en el ledger). Sigue la unidad `13` (feature)
- **Unidad en curso**: `13` (feature) — vidriera: `LICENSE` MIT, `README.md` reescrito en inglés y `docs/install.md`. SHA inicio `284ace4`, modelo `opus` (el de su tipo de unidad: sin degradación posible acá). **Bajada fina escrita**: [implementation/13-public-showcase.md](implementation/13-public-showcase.md) — las cuatro preguntas del plan resueltas con evidencia (titular/año MIT derivados del repo; «cero pérdida» partida en chequeo mecánico de tokens invariantes de idioma + tabla cerrada de 15 casos de mudanza; transcript = pipeline 2026-07-29 (2), que tiene los cinco hitos, más inquirylab como prueba externa; one-liner y workaround **probados en cuatro corridas** sobre repos descartables). **Corte de métricas declarado: `b0bdf4d`** — último SHA del `rounds-log`, así que el 14 lo reconstruye filtrando el log hasta esa fila
- **Ronda de review**: 4 · lanzada (ciclo de la bajada fina del feature 13)
- **Veredicto anterior**: CHANGES_REQUESTED · ronda 3 · base `284ace4` (head `528a4e0`) — **cuatro puntos, los cuatro aceptados; diez en tres rondas, ninguno argumentado y ninguno que moviera una cifra**. Codex reprodujo por su cuenta las 88 rondas, 59 rechazos, 29 hitos, 18 ciclos cerrados, las medianas, el único hito de largo 1, 25/5/5, 212 commits, 3 días y 13 features. De las dos dudas que le planteé sobre el normalizador **descartó una y confirmó la otra reproduciéndola**: el `rounds-log` es best-effort, así que una fila `new` perdida es posible, y sin guarda el normalizador fusionaba dos ciclos **sobreescribiendo en silencio** sus rondas ⇒ guarda fail-closed de topología, con prueba negativa y con la aclaración de que un ciclo incompleto no se rechaza. Los otros tres: el ciclo parcial salía con campo vacío en vez de `0` (las cifras coincidían solo por coerción de awk); el extractor **todavía** omitía una clase —los paths desnudos, y el caso concreto era `CLAUDE.md`, así que C5 podía aprobar la pérdida del symlink sembrado— ⇒ regla 4 y **45 tokens**; y STATUS quedó fechado un día atrás
- **Último veredicto**: —  · ronda 4 en vuelo — **tres puntos, los tres aceptados sin argumentar; seis en dos rondas, ninguno argumentado y ninguno que moviera una cifra**. Codex dio por cerrada la r1 ejecutando: el recorte rechaza ausente, duplicado y vacío y reconstruye 88 filas desde el log vivo de 89, el mapa cubre exactamente las 36 líneas con contenido, C6–C9 cierran la completitud y V8 es la resolución correcta. Los tres nuevos: (1) **los reductores contaban filas, no rondas del contrato** — durante un reintento los dos intentos conservan `mode=new`, así que se contaban dos ciclos y dos «r1», una con veredicto `PROC_FAIL`; lo reprodujo sintéticamente. Hoy el log no tiene reintentos y ninguna cifra cambia, pero el procedimiento habría mentido justo cuando nadie mira: ahora se normaliza a `(ciclo, ronda, desenlace terminal)` **antes** de contar, con prueba sintética. (2) El extractor no veía **destinos de links**, así que cuatro paths que el mapa dice conservar podían desaparecer y aprobar igual; y C5 verificaba el mapa en vez de los artefactos. (3) Dos correcciones documentales. **El patrón común de las dos rondas** quedó anotado como riesgo 8: procedimientos que verifican su propio enunciado en vez del artefacto
- **Esperando**: nada del humano — pipeline autorizado, corriendo sus unidades; el OK llega con el RECAP consolidado del final
- **Pipeline**: cuatro unidades (`design` → `plan` → `13` → `14`); frena en el RECAP consolidado, cuyo OK las cierra. Ajuste de alcance del gate: el pipeline **no pushea ni toca los settings de GitHub** (topics/homepage) — eso lo hace el humano, con los comandos que le deja listos la unidad `14`. Sigue sin registrar el fix de la colisión `build/` del instalador (fuera de alcance por decisión del humano)
- **Actualizado**: 2026-07-30

> Vigente desde este delta (APPROVED en r4; lo cierra el OK del RECAP consolidado del pipeline): la **superficie pública** tiene diseño propio — idioma por audiencia en tres planos (vidriera en inglés, método en español, **prosa instalada** en el destino declarada como limitación), licencia MIT, corte README/manual, y la evidencia del reviewer como foto versionada con commit de corte y comando de derivación. Detalle: [design/public-surface.md](design/public-surface.md).

> **Para el RECAP consolidado — deuda normativa declarada**: la política de idioma vuelve falsa la línea de `AGENTS.md` §Convenciones «Docs, commits y comunicación en español», y corregirla (más su espejo en `templates/AGENTS.md`) **está fuera de la ruta autorizada** — ni el feature 13 ni el 14 la incluyen. El pipeline **termina con esa deuda abierta** en vez de ampliar el scope en silencio. Sus dos únicas vías de cierre, por contrato: quererla antes del cierre del pipeline es cambio de scope ⇒ corte; autorizarla junto con el OK manda la ejecución a una unidad autorizada aparte, posterior — nunca dentro del commit mecánico que registra el OK.

> Vigente desde el feature 12: el cierre de `/adopt` reporta el **inventario derivado de git** de los archivos que tocó, separando lo mecánico de lo que el humano debe ratificar. Contrato reusable (hoy con un único consumidor) en `.claude/skills/adopt/SKILL.md` §«Reporte de cierre (contrato)».

> Vigente desde el feature 11: el modelo de los **hijos** (subagentes de lote o pipeline) lo fija la maquinaria por tipo de unidad —`fable` para `design`/`plan`, `opus` para `feature`—, no se hereda de la sesión, y el humano lo overridea por gate. Tabla canónica: skill `build`, §«Modelos por unidad».
