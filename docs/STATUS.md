# STATUS

- **Fase**: **pipeline `/build` en curso** — reanudado tras el desempate humano «a)» del deadlock que dejó al descubierto la reversión del reset indebido · ledger: [implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md) (`gate_base` `39b377e`)
- **Unidad `design`**: **APPROVED — pendiente OK de pipeline** (r4, base `780f4a7`) — superficie pública de axel: posicionamiento, política de idioma, licencia y evidencia auditable. Delta en [design/public-surface.md](design/public-surface.md) + tres filas de decisión y la fila de componentes en [DESIGN.md](DESIGN.md). Corrió con `opus` (degradación anunciada: `fable` rechazado por falta de créditos en la cuenta; registrada en el ledger)
- **Unidad `plan`**: **APPROVED — pendiente OK de pipeline** (r4, base `fa12f41`, head `b0bdf4d`, sin observaciones accionables) — el backlog dejó de estar vacío: entraron los features **13** y **14**, en ese orden, con el pedido literal del gate transcrito completo y el **contrato entre features** sobre **un solo corte** de métricas. Corrió con `opus` (tercera degradación anunciada consecutiva por la misma causa)
- **Unidad `13`**: **APPROVED — pendiente OK de pipeline** (r20, head `1ff5413`) — vidriera entregada: `LICENSE` MIT, `README.md` en inglés y `docs/install.md`. Ciclo de 20 rondas con dos cortes por tope, los dos desempatados con «a)» y **ninguno por desacuerdo**: 51 pedidos de Codex, los 51 aceptados sin argumentar ninguno. **Corte de métricas declarado: `b0bdf4d`**
- **Unidad `14`**: **en curso** — última de la ruta autorizada. SHA inicio `6ec4b48`, modelo `opus` (el de su tipo de unidad: sin degradación posible acá)
- **Paso actual**: **bajada fina en review** — [implementation/14-onboarding-feedback.md](implementation/14-onboarding-feedback.md). Cerrada la bajada sigue el **ciclo de implementación**, donde se escriben los artefactos **y se construyen los verificadores**, por decisión humana registrada
- **Ronda de review**: 21 · lanzada — **última antes del tope**: la racha sigue en 4 y **no se reseteó**, porque no hubo corte y el contrato no lo contempla
- **Último veredicto**: `CHANGES_REQUESTED` · ronda 20 · head `69bd9e2` — **6 bloqueantes, tres de ellos sobre `B`**, lo que **falsó la condición que el hijo había declarado**: la causa de que `B` no converja no era la multiplicidad de formas. Detalle abajo. Veredicto anterior: ronda 19 · head `315ea17` — **7 bloqueantes**. **Seis son absorbibles como filas o correcciones de especificación**; uno **no**: `docs/STATUS.md` seguía **contradiciéndose consigo mismo** —declaraba la ronda en curso arriba y «esperando OK con racha 5» abajo, conservaba un pendiente ya resuelto y enumeraba tablas viejas—, porque mi reconstrucción de la r17 **parcheó líneas sueltas en vez de auditar el archivo entero**. Los otros seis: `N0–N11` cerró `C` pero no llegó a `B` ni a `D`; la prosa seguía llamando «biyección» al invariante ya refutado; la especificación de `B` **falla abierto** en las referencias compuestas (el sufijo no se comprobaba); **`B` no tenía consumidor de cierre** —ningún criterio exigía construirlo, así que el feature podía cerrar sin el barrido—; y seguía viva prosa que afirma un validador «real y corrido»
- **Regresión propia, nombrada**: al convertir las tablas perdí el negativo del **directorio con nombre de plantilla**, reproducido desde la r12; sobrevivía en la tabla histórica y no en los criterios. Restaurado como `C6`
- **Resuelto por decisión humana «a)» (2026-07-30)**: la construcción de los verificadores **se mueve al ciclo de implementación**, con los **criterios intactos**. La bajada cierra sobre *qué tiene que ser verdad* —criterios, literales canónicos, listas cerradas, especificación completa de los tres YAML y las tres sustituciones del README— y las herramientas se construyen y depuran **contra artefactos que existan**. Registrado en el ledger (`b05359c`) y declarado en el doc del feature, §«Las herramientas se construyen en la implementación», para que ninguna ronda futura lo lea como un hueco
- **Lo que las rondas de bajada produjeron, y es el producto real**: las tablas `A1–A8` (alcance), `C1–C6` (plantillas) y `D1–D3` (edición del README), **cada fila un modo de falla reproducido**, con su inventario cerrado de casos por fila
- **Respuesta a «¿el andamiaje quedó cerrado?»: no, seguía produciendo** — y por eso valía preguntarlo. Las dos fallas eran caminos no probados de código que yo mismo había escrito la ronda anterior
- **Cerrado en la misma ronda, por observación del padre**: los puntos 1 y 2 eran ambos «mecanismo declarado que no ejecuta», y había un **tercero** esperando —la tabla de «familias de mutación», descrita en prosa sin generador—. Disuelto: las mutaciones son ahora los **casos negativos ejecutados** del validador (la tabla del doc es la lista; la cantidad no se publica porque crece cada vez que una ronda encuentra una vía nueva)
- **Veredicto previo**: `CHANGES_REQUESTED` · ronda 17 · head `47f02c0` — 6 bloqueantes; ahí se descubrió que `STATUS` había quedado destruido y que la biyección publicada era falsa. Detalle de todas las rondas en el Review log del doc del feature
- **Los pendientes del ledger, resueltos por el padre y todos en `AUTORIZADOS`** (la lista completa vive en §Procedencia del doc del feature): `e5ee8f2` (el `## Cierre` decía que la corrida se reanuda en la unidad `13`, y describía el primer corte como si fuera el único), `68a2fe5` (esa corrección había publicado «tres cortes», un contador móvil, con el pipeline abierto) y `7f57494` (el comando que reemplazó a ese contador **matcheaba su propia línea**; anclado al encabezado de evento y verificado corriéndolo, no razonándolo). El hijo **no tocó ninguno** y frenó el lanzamiento de la ronda en cada ocasión antes que mandar una review con un defecto conocido adentro. **Todos pertenecen al mismo bloque, y cada uno fue introducido por la corrección del anterior** — es la sección «El defecto dentro del acto de corregirlo» ocurriendo en el archivo del padre mientras el hijo la escribía
- **Corte por indisponibilidad del reviewer, resuelto**: la r10 salió `PROC_FAIL` en sus **dos** intentos porque Codex agotó su cuota de uso — causa **externa**, `exit 2` persistente (condición 1), **no deadlock**. El humano recargó los créditos y la unidad se reanudó. **La disponibilidad efectiva del reviewer se verifica recién en la ronda relanzada**: no hay forma barata de sondear la cuota sin invocarlo
- **`AUTORIZADOS` atrapó algo por primera vez**: el padre declaró el SHA de la reanudación (`140ad61`) y **no el del corte** (`ae28842`), que también toca el ledger. Lo encontró la auditoría por commit, y se incorporó atribuido al padre **por evidencia** —formato, contenido y paths— con la anotación pedida para que él lo confirme. Además, **el propio chequeo fallaba abierto**: recorría los commits en un `for … done | head`, así que el flag de violación moría en el subshell y el chequeo imprimía «pasa» dos líneas después de haber impreso la violación. Reescrito sin pipeline y **con prueba negativa** que lo hace devolver `rc=1`
- **El patrón, ahora con sección propia en el doc del feature** (§«El defecto dentro del acto de corregirlo»): en esta unidad, un defecto apareció repetidamente **dentro del acto de corregir otro del mismo tipo** — una premisa sin fuente en la tabla que declaraba haber ido a la fuente; un contador móvil en el párrafo que demuestra el mecanismo que lo prohíbe; el padre publicando una cifra móvil en el commit con que arreglaba otra falsedad del mismo bloque; la matriz de fixtures incompleta en la corrección que la declaraba completa; y «buscada y no encontrada» en el barrido cuyo propósito era no afirmar sin fuente. **No es descuido**: corregir *es* escribir, y escribir es donde el defecto nace. Por eso la salida es mover la garantía **del autor al mecanismo**, no poner más cuidado
- **Barrido de premisas externas**: inventario en el doc del feature, §«Barrido de premisas sobre sistemas externos» — cada premisa sobre GitHub, git, `gh`, el instalador y el entorno, con su estado (verificada contra la fuente, retirada, o buscada y no encontrada). **El tamaño no se publica: crece con cada ronda que encuentra una premisa nueva.** La r7 mostró que el barrido de la r6 **no había agotado la clase** — encontró cuatro más—, así que la lista es viva y la tabla es la lista
- **Racha**: **4** — derivada de `.claude/state/changes-streak`, coincidente con `last-verdict`
- **CONDICIÓN FALSADA, y el hijo la aplica tal como la declaró antes de conocer el resultado**: dijo que si la r20 volvía con hallazgos sobre `B`, la causa **no** era la multiplicidad de formas y esto **no cierra en el margen**. Volvió con **tres**. La lectura, entonces: **`B` es autorreferencial de un modo que `A`, `C` y `D` no son** —su sujeto es el doc que se edita en cada ronda para corregir los otros criterios—, así que **cada intento de arreglarlo es una edición al documento que `B` especifica**. La corrección estructural de la r19 fue ella misma aplicada de forma incompleta y generó tres inconsistencias nuevas: las cruzadas no salieron del dominio `§`, `M4` quedó inimplementable bajo la forma única, y el inventario se desincronizó de `C16`
- **Decisión humana (2026-07-31): `B` y `C16` salen del alcance.** Fundamento registrado: el barrido de referencias es **higiene de un doc de trabajo interno**, no toca ningún archivo publicable —README, CONTRIBUTING, plantillas, doc de métricas— y **no formaba parte de la vidriera que el gate autorizó**; se agregó durante la bajada. **`A`, `C` y `D` quedan intactos** y sus inventarios siguen cerrando tras la extracción (`N` 12/12, `P` 6/6), comprobado y no asumido
- **Pendiente del padre**: el `## Cierre` del ledger dice que no enumera las correcciones e **inmediatamente enumera sus formas**. Es una falsedad conocida dentro del rango bajo review, no bookkeeping futuro. Territorio del padre
- **Pendiente del ledger, resuelto**: el `## Cierre` volvía a agregar lo que declaraba haber retirado y publicaba «dos naturalezas» sobre un conjunto que el contrato no acota. Corregido por el padre en `b05359c`, que entra a `AUTORIZADOS`
- **Pendiente del ledger, resuelto**: el padre **reescribió el `## Cierre` entero** en vez de parchearlo por sexta vez, separando los dos dominios que su comando mezclaba (deadlock contra indisponibilidad del reviewer). Dejó enumeradas las **cinco correcciones sucesivas al mismo párrafo, cada una introduciendo el defecto de la siguiente** — el caso más denso del patrón que produjo el pipeline. `7c0aa7c` entra a `AUTORIZADOS`, C12 re-corrido: pasa
- **La lección de hermanos-del-síntoma dejó de ser lección**: el padre observó que se la había formulado al hijo como aprendizaje y no como paso obligatorio, y que **una lección que depende de acordarse es la clase de garantía que esta unidad viene moviendo del autor al mecanismo**. Ahora es un chequeo corrido en cada ronda: toda referencia `§«…»` del doc debe resolver a un encabezado existente — lo que la r12 rompió—, probado en los dos sentidos
- **Pendiente del ledger, resuelto**: el `## Cierre` había dejado viva una segunda universal —«ninguno terminó la corrida»— sobre el mismo conjunto creciente. **La causa que el padre declaró es la misma clase**: su barrido anterior buscaba esos cuantificadores **solo en negrita**, y ésta no la tenía, así que declaró «cero universales vivas» sobre un barrido que no podía verlas. Rehecho sin depender del formato y revisando los matches **caso por caso** en vez de declarar cero. `84002a0` entra a `AUTORIZADOS`, C12 re-corrido: pasa
- **Pendiente del ledger, resuelto**: el `## Cierre` afirmaba que **todos** los cortes se resolvieron con «a)» y el último fue «c)». El padre lo nombró mejor de lo que lo marqué: no era «una letra mal» sino una **afirmación universal sobre un conjunto que la corrida sigue ampliando** — el contador móvil con otra forma. Retirada, con la letra viviendo en cada evento y su comando de derivación verificado corriéndolo. `380eb74` entra a `AUTORIZADOS`, C12 re-corrido: pasa. La mitad del hijo —los ordinales de desempate— ya estaba corregida **dejando de numerarlos**
- **Refinamiento de método que dejó la r12**: *hermanos de la clase* **no reemplaza** a *hermanos del síntoma* — son dos barridos distintos y hay que hacer los dos. Barrer la clase responde «¿dónde más cometí este tipo de error?»; barrer el síntoma responde «¿qué más apunta al texto que acabo de cambiar?». La segunda es mecánica y es la que se saltea cuando la primera sale bien, que es exactamente lo que pasó con C6 y C15
- **`AUTORIZADOS`**: la lista vive en el doc del feature, §Procedencia, y **no se duplica acá** — duplicarla contradecía la decisión de que sea la única lista, y la r7 lo marcó. Auditoría por commit de C12 re-corrida en cada incorporación: pasa, y ningún commit del hijo toca el ledger. **Con el diff agregado que la r2 descartó, esos commits habrían roto el criterio** — es la primera vez que el mecanismo se ejerce con commits reales del padre en el rango, y es evidencia de la tesis de la r2, no una anécdota
- **Mi predicción de que una ronda más alcanzaba falló, y la causa importa**: no falló porque el ciclo girara en falso —la profundidad venía bajando 6 → 3 → 2 → 1 y la r4 apuntaba al plan original— sino porque apareció un **defecto de clase nueva**: una afirmación sobre el mundo que resultó falsa y era verificable contra documentación externa, no un criterio que prometía de más. La lección queda escrita en el doc: el contrato editorial pide que toda afirmación sea derivable con su fuente, y yo lo apliqué a las cifras y no al esquema de un tercero
- **Hallazgo reusable, escrito en el doc del feature**: el diff agregado aplana commits de autores con permisos distintos, así que no puede distinguir una violación de una excepción autorizada — la información se pierde en el **instrumento**, antes de que el criterio la mire. Generalización: cuando un criterio no cierra tras dos redacciones, sospechar del instrumento antes que de las palabras. Explica en parte las veinte rondas de la unidad `13`, donde los commits del padre y los del hijo se mezclaban en un solo rango
- **Lo que resuelve esta bajada**: las cinco preguntas que el plan dejó abiertas —cuántas plantillas de issue y cuáles (**dos** más `config.yml`), cómo se reconstruye el corte (filtrado fail-closed por SHA, reusando lo que el 13 probó), si el snapshot es copia literal o proyección (**copia literal**), cómo se citan las 35 rondas previas a la instrumentación (rotuladas, con las dos fuentes separadas) y qué valores se proponen para topics y homepage (ocho topics justificados uno por uno; homepage al manual, con dos alternativas completas)— más dos decisiones que el plan no pedía y que la implementación necesita fijadas de antemano: los **paths** (`docs/metrics.md` para el informe, `docs/metrics/rounds-log-b0bdf4d.tsv` para el snapshot, más `cut.awk` y `normalize.awk` versionados junto a él desde la r1 — un comando de derivación declarado que invoca archivos inexistentes no es un comando declarado) y la **línea exacta de «fuente única»**, acotada en la r1 a la superficie pública, con su clase incluida, su clase excluida y el chequeo mecánico que la aplica
- **La inconsistencia del corte, resuelta y no heredada**: la §14 del plan cita «80 rondas al corte `e1e1282`» y la unidad `13` declaró `b0bdf4d`. **Gana `b0bdf4d` por contrato** —el 13 declara y el 14 reconstruye—; la cita del plan es una foto anterior y se trata como histórica, sin corregir el plan. Verificado en esta unidad: la reconstrucción del corte devuelve **88** filas sobre un `rounds-log` vivo que hoy tiene más, que es el fenómeno entero que el corte existe para neutralizar
- **Esperando**: nada del humano — decisión recibida: `B` y `C16` **fuera del alcance**. La bajada cierra sobre `A`, `C` y `D`. **La racha no se reseteó** (sigue en 4, sin cobertura contractual para hacerlo): queda **una ronda** antes del tope
