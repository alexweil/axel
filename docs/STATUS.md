# STATUS

- **Fase**: **pipeline `/build` en curso** — autorizado el 2026-07-29 · ledger: [implementation/pipeline-2026-07-29-2.md](implementation/pipeline-2026-07-29-2.md) (ruta: plan-delta → feature 12) · `gate_base` `88020af`
- **Unidad en curso**: `12` (feature) — bajada fina e implementación del reporte de cierre de `/adopt`; hijo lanzado con modelo `opus`. Unidad previa `plan` (plan-delta): **APPROVED — pendiente OK de pipeline** (agregó el feature 12 como único ítem del backlog nuevo; corrió con el modelo de la sesión por la **degradación anunciada** de `fable`, registrada en el ledger y a listar en el RECAP). El próximo checkpoint humano es el OK del RECAP consolidado del pipeline
- **Ronda de review**: 3 · consumida
- **Último veredicto**: APPROVED · ronda 3 · base `74d3f5c` (head `bcf34f3`, sin observaciones accionables)
- **Esperando**: nada del humano — pipeline autorizado, corriendo sus unidades; el OK llega con el RECAP consolidado del final
- **Actualizado**: 2026-07-29

> Vigente desde el feature 11: el modelo de los **hijos** (subagentes de lote o pipeline) lo fija la maquinaria por tipo de unidad —`fable` para `design`/`plan`, `opus` para `feature`—, no se hereda de la sesión, y el humano lo overridea por gate. Tabla canónica: skill `build`, §«Modelos por unidad».
