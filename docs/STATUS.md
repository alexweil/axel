# STATUS

- **Fase**: **pipeline `/build` en curso** — reanudado tras el **tercer** desempate humano («a)», 2026-07-30); el corte fue de la unidad `14` en su ciclo de bajada (r1–r5) · ledger: [implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md) (`gate_base` `39b377e`)
- **Unidad `design`**: **APPROVED — pendiente OK de pipeline** (r4, base `780f4a7`) — superficie pública de axel: posicionamiento, política de idioma, licencia y evidencia auditable. Delta en [design/public-surface.md](design/public-surface.md) + tres filas de decisión y la fila de componentes en [DESIGN.md](DESIGN.md). Corrió con `opus` (degradación anunciada: `fable` rechazado por falta de créditos en la cuenta; registrada en el ledger)
- **Unidad `plan`**: **APPROVED — pendiente OK de pipeline** (r4, base `fa12f41`, head `b0bdf4d`, sin observaciones accionables) — el backlog dejó de estar vacío: entraron los features **13** y **14**, en ese orden, con el pedido literal del gate transcrito completo y el **contrato entre features** sobre **un solo corte** de métricas. Corrió con `opus` (tercera degradación anunciada consecutiva por la misma causa)
- **Unidad `13`**: **APPROVED — pendiente OK de pipeline** (r20, head `1ff5413`) — vidriera entregada: `LICENSE` MIT, `README.md` en inglés y `docs/install.md`. Ciclo de 20 rondas con dos cortes por tope, los dos desempatados con «a)» y **ninguno por desacuerdo**: 51 pedidos de Codex, los 51 aceptados sin argumentar ninguno. **Corte de métricas declarado: `b0bdf4d`**
- **Unidad `14`**: **en curso** — última de la ruta autorizada. SHA inicio `6ec4b48`, modelo `opus` (el de su tipo de unidad: sin degradación posible acá)
- **Paso actual**: **bajada fina en review** — [implementation/14-onboarding-feedback.md](implementation/14-onboarding-feedback.md), corregida tras la r5. Cerrada la bajada sigue el **ciclo de implementación**, que es donde se escriben los artefactos
- **Ronda de review**: 8 · **consumida** — la r9 **no** se lanza todavía: espera una corrección del ledger, que es del padre (tercera consecutiva; ver abajo)
- **Último veredicto**: `CHANGES_REQUESTED` · ronda 8 · head `03947c2` — **4 bloqueantes, cero preferencias**. El de fondo obligó a un **cambio estructural del criterio**: enumerar las causas por las que GitHub descarta un formulario resultó una condición de cierre **inalcanzable** —cuatro rondas seguidas encontraron causas nuevas, porque el conjunto es de GitHub y el hijo no lo acota—, así que **C6 invierte la carga**: deja de preguntar «¿viola alguna regla ajena?» y pasa a «¿coincide con la especificación literal que fija C15?». Las reglas documentadas se satisfacen **por construcción**, con cada elección registrada contra su regla. Además: el desacuerdo sobre `name` se resolvió **contra el hijo** —la doc sí exige más de tres caracteres—, la biyección era entre identificadores y no entre sitios ejecutados, y STATUS volvió a desincronizarse
- **Veredicto previo**: `CHANGES_REQUESTED` · ronda 7 · head `7903df2` — 3 bloqueantes; ahí el pedido adversarial mostró que el barrido de la r6 **no había agotado la clase**. Detalle de todas las rondas en el Review log del doc del feature
- **Pendiente del padre — bloquea el lanzamiento de la r9, y es el tercero consecutivo sobre el mismo bloque**: el comando de enumeración que `68a2fe5` dejó en el `## Cierre` **matchea su propia línea**, así que devuelve un resultado de más. Se ancla el patrón a los encabezados de evento. Territorio del padre; el hijo no lo toca
- **Pendientes anteriores del ledger, resueltos por el padre**: `e5ee8f2` (el `## Cierre` decía que la corrida se reanuda en la unidad `13`, y describía el primer corte como si fuera el único) y `68a2fe5` (esa corrección había publicado «tres cortes», un contador móvil, con el pipeline abierto). El hijo **no tocó ninguno** y frenó el lanzamiento de la ronda en cada ocasión antes que mandar una review con un defecto conocido adentro. **Los dos están en `AUTORIZADOS`**. Los tres son del mismo bloque y de la misma forma — cada corrección introdujo el defecto siguiente, que es el patrón de la sección «El defecto dentro del acto de corregirlo»
- **El patrón, ahora con sección propia en el doc del feature** (§«El defecto dentro del acto de corregirlo»): en esta unidad, un defecto apareció repetidamente **dentro del acto de corregir otro del mismo tipo** — una premisa sin fuente en la tabla que declaraba haber ido a la fuente; un contador móvil en el párrafo que demuestra el mecanismo que lo prohíbe; el padre publicando una cifra móvil en el commit con que arreglaba otra falsedad del mismo bloque; la matriz de fixtures incompleta en la corrección que la declaraba completa; y «buscada y no encontrada» en el barrido cuyo propósito era no afirmar sin fuente. **No es descuido**: corregir *es* escribir, y escribir es donde el defecto nace. Por eso la salida es mover la garantía **del autor al mecanismo**, no poner más cuidado
- **Barrido de premisas externas**: inventario en el doc del feature, §«Barrido de premisas sobre sistemas externos» — cada premisa sobre GitHub, git, `gh`, el instalador y el entorno, con su estado (verificada contra la fuente, retirada, o buscada y no encontrada). **El tamaño no se publica: crece con cada ronda que encuentra una premisa nueva.** La r7 mostró que el barrido de la r6 **no había agotado la clase** — encontró cuatro más—, así que la lista es viva y la tabla es la lista
- **Racha**: **3** — derivada de `.claude/state/changes-streak`
- **`AUTORIZADOS`**: la lista vive en el doc del feature, §Procedencia, y **no se duplica acá** — duplicarla contradecía la decisión de que sea la única lista, y la r7 lo marcó. Auditoría por commit de C12 re-corrida en cada incorporación: pasa, y ningún commit del hijo toca el ledger. **Con el diff agregado que la r2 descartó, esos commits habrían roto el criterio** — es la primera vez que el mecanismo se ejerce con commits reales del padre en el rango, y es evidencia de la tesis de la r2, no una anécdota
- **Mi predicción de que una ronda más alcanzaba falló, y la causa importa**: no falló porque el ciclo girara en falso —la profundidad venía bajando 6 → 3 → 2 → 1 y la r4 apuntaba al plan original— sino porque apareció un **defecto de clase nueva**: una afirmación sobre el mundo que resultó falsa y era verificable contra documentación externa, no un criterio que prometía de más. La lección queda escrita en el doc: el contrato editorial pide que toda afirmación sea derivable con su fuente, y yo lo apliqué a las cifras y no al esquema de un tercero
- **Hallazgo reusable, escrito en el doc del feature**: el diff agregado aplana commits de autores con permisos distintos, así que no puede distinguir una violación de una excepción autorizada — la información se pierde en el **instrumento**, antes de que el criterio la mire. Generalización: cuando un criterio no cierra tras dos redacciones, sospechar del instrumento antes que de las palabras. Explica en parte las veinte rondas de la unidad `13`, donde los commits del padre y los del hijo se mezclaban en un solo rango
- **Lo que resuelve esta bajada**: las cinco preguntas que el plan dejó abiertas —cuántas plantillas de issue y cuáles (**dos** más `config.yml`), cómo se reconstruye el corte (filtrado fail-closed por SHA, reusando lo que el 13 probó), si el snapshot es copia literal o proyección (**copia literal**), cómo se citan las 35 rondas previas a la instrumentación (rotuladas, con las dos fuentes separadas) y qué valores se proponen para topics y homepage (ocho topics justificados uno por uno; homepage al manual, con dos alternativas completas)— más dos decisiones que el plan no pedía y que la implementación necesita fijadas de antemano: los **paths** (`docs/metrics.md` para el informe, `docs/metrics/rounds-log-b0bdf4d.tsv` para el snapshot, más `cut.awk` y `normalize.awk` versionados junto a él desde la r1 — un comando de derivación declarado que invoca archivos inexistentes no es un comando declarado) y la **línea exacta de «fuente única»**, acotada en la r1 a la superficie pública, con su clase incluida, su clase excluida y el chequeo mecánico que la aplica
- **La inconsistencia del corte, resuelta y no heredada**: la §14 del plan cita «80 rondas al corte `e1e1282`» y la unidad `13` declaró `b0bdf4d`. **Gana `b0bdf4d` por contrato** —el 13 declara y el 14 reconstruye—; la cita del plan es una foto anterior y se trata como histórica, sin corregir el plan. Verificado en esta unidad: la reconstrucción del corte devuelve **88** filas sobre un `rounds-log` vivo que hoy tiene más, que es el fenómeno entero que el corte existe para neutralizar
- **Esperando**: nada del humano — tercer desempate recibido («a)»), pipeline reanudado con la unidad `14` completa; el OK consolidado llega con el RECAP del final. El padre había recomendado cerrar sin el `14`; el humano decidió completarlo
- **Regla adoptada de la unidad `13`, vigente para esta**: ningún contador que el propio proceso mueve se publica en prosa — va como **foto anclada a un SHA** o como **comando de derivación**. Aplica a los artefactos publicados y también a los docs internos de esta unidad. El compromiso del padre sobre commits al ledger estaba acotado al ciclo de la `13`, que ya cerró
- **Pipeline**: cuatro unidades (`design` → `plan` → `13` → `14`) — las tres primeras **APPROVED e intactas**, la `14` en curso. Ajuste de alcance del gate: el pipeline **no pushea ni toca los settings de GitHub** (topics/homepage) — eso lo hace el humano, con los comandos que le deja listos esta unidad. Sigue sin registrar el fix de la colisión `build/` del instalador (fuera de alcance por decisión del humano)
- **Actualizado**: 2026-07-30

> **Para el RECAP consolidado — deuda normativa declarada**: la política de idioma vuelve falsa la línea de `AGENTS.md` §Convenciones «Docs, commits y comunicación en español», y corregirla (más su espejo en `templates/AGENTS.md`) **está fuera de la ruta autorizada** — ni el feature 13 ni el 14 la incluyen. El pipeline **termina con esa deuda abierta** en vez de ampliar el scope en silencio. Sus dos únicas vías de cierre, por contrato: quererla antes del cierre del pipeline es cambio de scope ⇒ corte; autorizarla junto con el OK manda la ejecución a una unidad autorizada aparte, posterior — nunca dentro del commit mecánico que registra el OK.

> Vigente desde el delta de diseño de este pipeline (APPROVED en r4; lo cierra el OK del RECAP consolidado): la **superficie pública** tiene diseño propio — idioma por audiencia en tres planos (vidriera en inglés, método en español, **prosa instalada** en el destino declarada como limitación), licencia MIT, corte README/manual, y la evidencia del reviewer como foto versionada con commit de corte y comando de derivación. Detalle: [design/public-surface.md](design/public-surface.md).

> Vigente desde el feature 12: el cierre de `/adopt` reporta el **inventario derivado de git** de los archivos que tocó, separando lo mecánico de lo que el humano debe ratificar. Contrato reusable —con un único consumidor **al cierre del feature 12**, que es la foto de ese momento y no un conteo vigente— en `.claude/skills/adopt/SKILL.md` §«Reporte de cierre (contrato)».

> Vigente desde el feature 11: el modelo de los **hijos** (subagentes de lote o pipeline) lo fija la maquinaria por tipo de unidad —`fable` para `design`/`plan`, `opus` para `feature`—, no se hereda de la sesión, y el humano lo overridea por gate. Tabla canónica: skill `build`, §«Modelos por unidad».
