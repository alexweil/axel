# STATUS

- **Fase**: **pipeline `/build` en curso** — reanudado el 2026-07-30 tras el desempate humano («a)») del corte por deadlock de la unidad `13` · ledger: [implementation/pipeline-2026-07-29-3.md](implementation/pipeline-2026-07-29-3.md) (`gate_base` `39b377e`)
- **Unidad `design`**: **APPROVED — pendiente OK de pipeline** (r4, base `780f4a7`) — superficie pública de axel: posicionamiento, política de idioma, licencia y evidencia auditable. Delta en [design/public-surface.md](design/public-surface.md) + tres filas de decisión y la fila de componentes en [DESIGN.md](DESIGN.md). Corrió con `opus` (degradación anunciada: `fable` rechazado por falta de créditos en la cuenta; registrada en el ledger)
- **Unidad `plan`**: **APPROVED — pendiente OK de pipeline** (r4, base `fa12f41`, head `b0bdf4d`, sin observaciones accionables) — el backlog **deja de estar vacío**: entran los features **13** (vidriera: LICENSE MIT + README en inglés + `docs/install.md`) y **14** (onboarding y feedback: CONTRIBUTING + `.github/` + métricas versionadas), en ese orden, con el pedido literal del gate transcrito completo y el **contrato entre features** que cierra las dos referencias hacia adelante del README sobre **un solo corte** de métricas: el 13 deja referencias no activas y marcadas —cero links rotos—, y el 14 elige los paths, reconstruye ese corte, crea los artefactos y las activa. Corrió con `opus` (tercera degradación anunciada consecutiva: `fable` rechazado por falta de créditos; registrada en el ledger). Sigue la unidad `13` (feature)
- **Unidad en curso**: `13` (feature) — vidriera: `LICENSE` MIT, `README.md` reescrito en inglés y `docs/install.md`. SHA inicio `284ace4`, modelo `opus` (el de su tipo de unidad: sin degradación posible acá). **Bajada fina escrita**: [implementation/13-public-showcase.md](implementation/13-public-showcase.md) — las cuatro preguntas del plan resueltas con evidencia (titular/año MIT derivados del repo; «cero pérdida» partida en chequeo mecánico de **45 tokens** invariantes de idioma producidos por un extractor versionado + mapa de las **36 líneas con contenido** del baseline `284ace4:README.md`; transcript = pipeline 2026-07-29 (2), que tiene los cinco hitos, más inquirylab como prueba externa; one-liner y workaround **probados en cuatro corridas** sobre repos descartables). **Corte de métricas declarado: `b0bdf4d`** — último SHA del `rounds-log`, así que el 14 lo reconstruye filtrando el log hasta esa fila
- **Ronda de review**: 14 · lanzada (implementación del feature 13)
- **Último veredicto**: CHANGES_REQUESTED · ronda 12 · base `61a6c32` (head `b7bf446`) — **tres puntos: dos corregidos, uno escalado**. Codex dio por cerrado el agujero sustantivo de C14 ejecutando el bloque publicado. (1) STATUS no había consumido la r11: racha en 0 cuando era 1, anunciaba una ronda ya lanzada, y **conservaba «veintinueve» mientras la línea contigua afirmaba que STATUS estaba corregido a 28** — corregido. (2) **El conteo falso sigue vigente en el ledger**: Codex coincide en que el hijo no debe tocar territorio del padre, pero marca que reportarlo no corrige el estado versionado y que **no aprueba un doc que sabe falso**; su salida es que lo corrija el padre y que ese commit se incorpore a la lista cerrada de C14. **Escalado.** (3) La evidencia de C14 prometía un `diff` que el script no imprimía — la versión chica del defecto de siempre, afirmar sobre el artefacto sin correrlo; corregido y verificado
- **Racha**: **3** — r11, r12 y r13, los tres `CHANGES_REQUESTED` posteriores al reset. Hubo además **caídas de la sesión por `529 Overloaded`**, todas de servidor y ninguna es una ronda sin converger, así que **no cuentan** para el tope. **No se publica su número**: las caídas no dejan rastro en ningún artefacto versionado —el ledger no las registra— y una cifra que no se puede derivar del repo no se publica, que es la regla que este mismo feature le exige a la vidriera. El conteo lo lleva el padre en su supervisión; lo que sí es derivable, y es lo que importa acá, es la racha contractual, que `.claude/state/changes-streak` fija en **3** tras la r13. Historia del corte: racha **5** (r6 a r10). **No fue deadlock de desacuerdo** — el hijo aceptó **los veintiocho** pedidos de Codex sin argumentar ninguno (bajada r1–r4 = 3+3+4+3 = **13**, implementación r6–r10 = 4+4+4+1+2 = **15**), y la r9 ya traía un único punto y cero preferencias; el tope se alcanzó por acumulación de bookkeeping. **Corregido en la r12**: acá decía «veintinueve», que era falso. La misma cifra estaba también en el ledger del pipeline, territorio del padre: **corregida por él en `f6fce39`**, que entra a la lista cerrada de C14 —ahora **seis** SHA— y deja el original tachado y conservado. Alcance de la reanudación: **solo los puntos abiertos**, no una reapertura del feature; el criterio que lo decide es que corregir una falsedad vigente en un doc que la unidad ya posee **no** es trabajo nuevo, y sí lo sería reescribir una sección, agregar un criterio o tocar un archivo fuera de la unidad
- **Implementación escrita**: `LICENSE` (MIT, `Copyright (c) 2026 alexweil`), `README.md` reescrito en inglés (**183** líneas / **10 989 bytes**; el baseline tenía 69 líneas sin wrappear de hasta 1070 bytes y 8142 bytes en total, así que el desvío contra el objetivo de «~120 líneas» es de unidad de medida y está declarado) y `docs/install.md` (**374** líneas, 21 148 bytes, 15 secciones). Los dieciséis criterios verificados en el doc del feature: **0 tokens faltantes de 45** con las tres traducciones de placeholder declaradas, **0 links rotos** con las siete anclas internas chequeadas contra los encabezados reales, las citas del transcript **comparadas mecánicamente** contra sus fuentes, y `lint` limpio · `loop` 293/0 · `install` 460/0 (el reviewer obtiene 287 en `loop` porque su sandbox saltea los seis asserts del smoke no contractual de `caffeinate`)
- **Esperando**: nada del humano — desempate recibido («a)»), pipeline reanudado; el OK consolidado llega con el RECAP del final. **El desempate no es el OK**: `design` y `plan` siguen en «APPROVED — pendiente OK de pipeline»
- **Compromiso del padre durante este ciclo**: **ningún commit adicional a este ledger después de `f6fce39`**, mientras el ciclo de la `13` esté abierto. Reformulado en la r13: la versión anterior decía «no commitea al ledger mientras el ciclo esté abierto» y **`f6fce39` la volvió falsa** — ese commit fue una **excepción necesaria y autorizada**, la corrección del conteo del propio ledger, que el hijo no podía hacer y que Codex no dejaba pasar sin corregir. La causa del compromiso original sigue en pie: los dos commits de la anomalía del id movieron el rango bajo el que el hijo había escrito su evidencia C14 y consumieron una ronda del tope. Los eventos pendientes se registran al cerrar la unidad. **La formulación equivalente dentro del ledger la sincroniza el padre** — es su archivo y su compromiso; el hijo la reporta, no la reescribe
- **Pipeline**: cuatro unidades (`design` → `plan` → `13` → `14`) — **`design` y `plan` APPROVED e intactas, `13` reanudada, `14` pendiente**. Ajuste de alcance del gate: el pipeline **no pushea ni toca los settings de GitHub** (topics/homepage) — eso lo hace el humano, con los comandos que le deja listos la unidad `14`. Sigue sin registrar el fix de la colisión `build/` del instalador (fuera de alcance por decisión del humano)
- **Actualizado**: 2026-07-30

> Vigente desde este delta (APPROVED en r4; lo cierra el OK del RECAP consolidado del pipeline): la **superficie pública** tiene diseño propio — idioma por audiencia en tres planos (vidriera en inglés, método en español, **prosa instalada** en el destino declarada como limitación), licencia MIT, corte README/manual, y la evidencia del reviewer como foto versionada con commit de corte y comando de derivación. Detalle: [design/public-surface.md](design/public-surface.md).

> **Para el RECAP consolidado — deuda normativa declarada**: la política de idioma vuelve falsa la línea de `AGENTS.md` §Convenciones «Docs, commits y comunicación en español», y corregirla (más su espejo en `templates/AGENTS.md`) **está fuera de la ruta autorizada** — ni el feature 13 ni el 14 la incluyen. El pipeline **termina con esa deuda abierta** en vez de ampliar el scope en silencio. Sus dos únicas vías de cierre, por contrato: quererla antes del cierre del pipeline es cambio de scope ⇒ corte; autorizarla junto con el OK manda la ejecución a una unidad autorizada aparte, posterior — nunca dentro del commit mecánico que registra el OK.

> Vigente desde el feature 12: el cierre de `/adopt` reporta el **inventario derivado de git** de los archivos que tocó, separando lo mecánico de lo que el humano debe ratificar. Contrato reusable (hoy con un único consumidor) en `.claude/skills/adopt/SKILL.md` §«Reporte de cierre (contrato)».

> Vigente desde el feature 11: el modelo de los **hijos** (subagentes de lote o pipeline) lo fija la maquinaria por tipo de unidad —`fable` para `design`/`plan`, `opus` para `feature`—, no se hereda de la sesión, y el humano lo overridea por gate. Tabla canónica: skill `build`, §«Modelos por unidad».
