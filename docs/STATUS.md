# STATUS

- **Fase**: **pipeline `/build` en curso** — autorizado el 2026-07-29 · ledger: [implementation/pipeline-2026-07-29-2.md](implementation/pipeline-2026-07-29-2.md) (ruta: plan-delta → feature 12) · `gate_base` `88020af`
- **Unidad en curso**: `plan` (plan-delta) — delta escrito: el feature 12 («Reporte de cierre de `/adopt`: inventario de archivos tocados») como único ítem del backlog nuevo — párrafo de extensión + fila + sección en IMPLEMENTATION.md; ciclo de review abierto, r1 y r2 corregidas. Corre con el modelo de la sesión (`opus`): **degradación anunciada** — `fable` rechazado por el harness (sin créditos), registrada en el ledger y a listar en el RECAP. El próximo checkpoint humano es el OK del RECAP consolidado del pipeline
- **Ronda de review**: 3 · lanzada
- **Último veredicto**: CHANGES_REQUESTED · ronda 2 · base `74d3f5c` (dos puntos: prosa de más en la línea «Esperando», y la cobertura de los bullets del cierre anterior atribuida a la r1 en vez de al APPROVED final del ciclo)
- **Esperando**: esperando el desenlace de la review (ronda 3)
- **Actualizado**: 2026-07-29

> Vigente desde el feature 11: el modelo de los **hijos** (subagentes de lote o pipeline) lo fija la maquinaria por tipo de unidad —`fable` para `design`/`plan`, `opus` para `feature`—, no se hereda de la sesión, y el humano lo overridea por gate. Tabla canónica: skill `build`, §«Modelos por unidad».
